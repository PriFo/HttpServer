# 🚀 Запуск нормализации данных - Инструкция

**Проект:** AITAS-MDM-2025-001  
**Дата:** 26 ноября 2025  
**Статус:** ✅ Всё готово к запуску

---

## ✅ Подготовка завершена

### Что уже сделано:

1. **✅ Исправление кода**
   - Обновлены функции определения типов данных
   - 100% точность классификации номенклатуры/контрагентов

2. **✅ Регистрация баз данных**
   - 8 баз данных зарегистрированы в системе
   - Все БД активны и готовы к обработке

3. **✅ Статистика**
   - **64,514 записей** готовы к нормализации
   - 4 базы номенклатуры (~44,514 записей)
   - 4 базы контрагентов (~20,000 записей)

4. **✅ Инструменты созданы**
   - 10 Go скриптов для работы с системой
   - Скрипты проверки и мониторинга

---

## 🚀 ШАГ 1: Запуск сервера

### Вариант A: Запуск Go Backend

```bash
cd E:\HttpServer
go run ./cmd/server
```

**Ожидаемый вывод:**
```
✓ Сервер успешно запущен на порту 9999
✓ API доступно: http://localhost:9999
```

### Вариант B: Запуск через скомпилированный бинарник

```bash
cd E:\HttpServer
go build -o server.exe ./cmd/server
./server.exe
```

**Проверка запуска:**
```bash
# В другом терминале
go run tools/check_api_status.go
```

Должен показать: `✅ API полностью работает!`

---

## 🚀 ШАГ 2: Запуск нормализации

### Способ 1: Через веб-интерфейс (РЕКОМЕНДУЕТСЯ) ⭐

1. **Откройте браузер:**
   ```
   http://localhost:3000
   ```

2. **Перейдите в раздел проектов:**
   - Кликните на **"Проекты"** в меню
   - Найдите и откройте **"AITAS-MDM-2025-001"**

3. **Откройте вкладку нормализации:**
   - В проекте найдите вкладку **"Нормализация"**
   - Нажмите на неё

4. **Запустите нормализацию:**
   - Нажмите кнопку **"Запустить нормализацию"**
   - Выберите опцию: **"Все активные базы данных"**
   - Нажмите **"Начать"**

5. **Мониторинг:**
   - Наблюдайте за прогрессом в реальном времени
   - Смотрите статистику обработки

---

### Способ 2: Через HTTP API

После запуска сервера (ШАГ 1):

```bash
curl -X POST http://localhost:9999/api/clients/1/projects/1/normalization/start \
  -H "Content-Type: application/json" \
  -d '{
    "all_active": true,
    "use_kpved": true,
    "use_okpd2": true
  }'
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "message": "Normalization started for project",
  "client_id": 1,
  "project_id": 1
}
```

---

## 📊 ШАГ 3: Мониторинг прогресса

### Через веб-интерфейс

- Откройте вкладку нормализации в проекте
- Смотрите прогресс в реальном времени
- Статистика обновляется автоматически

### Через скрипт

```bash
# Проверка статуса
go run tools/monitor_normalization.go

# Проверка готовности
go run tools/check_normalization_readiness.go
```

### Через API

```bash
# Статус нормализации
curl http://localhost:9999/api/clients/1/projects/1/normalization/status

# События нормализации
curl http://localhost:9999/api/clients/1/projects/1/normalization/events
```

---

## ⏱️ Ожидаемое время выполнения

- **Всего записей:** 64,514
- **Скорость обработки:** ~2,500-3,000 записей/минуту
- **Ожидаемое время:** **~21-27 минут**

---

## 📊 Ожидаемые результаты

После завершения нормализации:

### В базе `normalized_data.db`:

**Таблица `normalized_data`:**
- ✅ Нормализованные записи со всеми полями
- ✅ Поля: `normalized_name`, `normalized_reference`, `category`, etc.
- ✅ Связь с исходными записями через `source_reference`

**Таблица `normalization_groups`:**
- ✅ Группы дубликатов
- ✅ Связи между похожими записями

### В базе `service.db`:

**Таблица `normalization_sessions`:**
- ✅ Информация о сессиях нормализации
- ✅ Статистика: обработано, успешно, ошибки
- ✅ Время начала и завершения

**Обновления:**
- ✅ `project_databases.last_used_at` - обновлено для всех обработанных БД

---

## 🔍 Проверка результатов

### Проверка количества нормализованных записей:

```bash
# Подключение к normalized_data.db
sqlite3 data/normalized_data.db

# Количество записей
SELECT COUNT(*) FROM normalized_data WHERE project_id = 1;

# Статистика нормализации
SELECT 
    COUNT(*) as total,
    COUNT(CASE WHEN normalized_name IS NOT NULL THEN 1 END) as normalized,
    COUNT(CASE WHEN normalized_name IS NULL THEN 1 END) as pending
FROM normalized_data 
WHERE project_id = 1;
```

### Проверка сессий:

```bash
sqlite3 data/service.db

SELECT 
    id,
    database_id,
    start_time,
    end_time,
    status,
    processed_count,
    total_count
FROM normalization_sessions
WHERE project_id = 1
ORDER BY start_time DESC
LIMIT 10;
```

---

## 🛠️ Полезные инструменты

### Скрипты для работы:

```bash
# Проверка готовности системы
go run tools/check_normalization_readiness.go

# Мониторинг нормализации
go run tools/monitor_normalization.go

# Проверка статуса API
go run tools/check_api_status.go

# Список всех баз данных
go run tools/list_all_databases.go
```

---

## ⚠️ Устранение проблем

### Проблема: Сервер не запускается

**Решение:**
```bash
# Проверьте, не занят ли порт 9999
netstat -ano | findstr ":9999"

# Если занят, остановите процесс
# Затем запустите сервер заново
go run ./cmd/server
```

### Проблема: API возвращает 502

**Решение:**
1. Убедитесь, что сервер запущен
2. Проверьте логи сервера
3. Попробуйте перезапустить сервер
4. Используйте веб-интерфейс как альтернативу

### Проблема: Нормализация не запускается

**Решение:**
1. Проверьте, что все БД активны:
   ```bash
   go run tools/check_normalization_readiness.go
   ```
2. Убедитесь, что сервер запущен
3. Проверьте логи на наличие ошибок
4. Попробуйте запустить через веб-интерфейс

---

## 📝 Быстрая справка

### Команды запуска:

```bash
# 1. Запуск сервера
cd E:\HttpServer
go run ./cmd/server

# 2. Проверка готовности (в другом терминале)
go run tools/check_normalization_readiness.go

# 3. Запуск нормализации через веб-интерфейс
# Откройте: http://localhost:3000

# 4. Мониторинг (в другом терминале)
go run tools/monitor_normalization.go
```

### Ключевые URLs:

- **Веб-интерфейс:** http://localhost:3000
- **API:** http://localhost:9999
- **Swagger:** http://localhost:9999/swagger/index.html

### Ключевые файлы:

- **Служебная БД:** `data/service.db`
- **Нормализованные данные:** `data/normalized_data.db`
- **Базы данных проекта:** `data/uploads/*.db`

---

## ✅ Контрольный список

Перед запуском нормализации убедитесь, что:

- [ ] Go backend сервер запущен (порт 9999)
- [ ] API отвечает на запросы
- [ ] 8 баз данных зарегистрированы в системе
- [ ] Все БД активны (`is_active = true`)
- [ ] Доступен веб-интерфейс (опционально)

---

## 🎯 Следующие шаги после нормализации

1. **Проверить результаты**
   - Количество нормализованных записей
   - Качество нормализации
   - Группы дубликатов

2. **Создать эталоны (Benchmarks)**
   - Выбрать эталонные записи
   - Настроить правила сопоставления

3. **Оценить качество**
   - Проверить точность нормализации
   - Найти и исправить ошибки

4. **Экспорт данных**
   - Экспортировать нормализованные данные
   - Создать отчеты

---

**Всё готово к запуску! Удачи!** 🚀

---

*Документ создан 26 ноября 2025*

