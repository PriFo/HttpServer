# Проверка цепочки нормализации номенклатуры по всем базам проекта

## Дата проверки
2025-01-XX

## Цель
Провести полный цикл тестирования и проверки цепочки кода нормализации номенклатуры сразу по всему проекту со всех баз.

---

## 1. Анализ цепочки обработки

### 1.1 API Endpoint
**Путь:** `POST /api/clients/{id}/projects/{projectId}/normalization/start`

**Файл:** `server/server.go:6108`

**Обработчик:** `handleStartClientNormalization` (строка 8726)

**Параметры запроса:**
```json
{
  "all_active": true,  // Обработать все активные БД
  "database_path": "", // Или конкретный путь к БД
  "use_kpved": false,
  "use_okpd2": false
}
```

### 1.2 Обработка запроса

**Файл:** `server/server.go:8726-9112`

**Логика:**
1. Проверка существования проекта (строка 8755)
2. Получение списка баз данных проекта (строка 8774 или 8798)
3. Создание сессии нормализации для каждой БД (строка 8876)
4. Открытие подключения к базе данных (строка 8890)
5. **Для номенклатуры (не counterparty):**
   - Получение всех записей из `catalog_items` (строка 9003)
   - Создание `ClientNormalizer` (строка 9021)
   - Запуск `ProcessWithClientBenchmarks` (строка 9057)

### 1.3 Нормализация номенклатуры

**Файл:** `normalization/client_normalizer.go:97`

**Метод:** `ProcessWithClientBenchmarks(items []*database.CatalogItem)`

**Что делает:**
1. ✅ Проверяет эталоны клиента (`client_benchmarks`)
2. ✅ Выполняет базовую нормализацию имен
3. ✅ Применяет AI-усиление при необходимости
4. ✅ Группирует записи по категории и нормализованному имени
5. ❌ **НЕ сохраняет результаты в `normalized_data`**

### 1.4 Сохранение результатов

**ПРОБЛЕМА:** После вызова `ProcessWithClientBenchmarks` в `server.go:9057` нет кода для сохранения результатов в `normalized_data`.

**Сравнение с контрагентами:**
- Для контрагентов используется `CounterpartyNormalizer.ProcessNormalization`, который сохраняет результаты через `SaveNormalizedCounterparty` (строка 749 в `counterparty_normalizer.go`)
- Для номенклатуры `ClientNormalizer.ProcessWithClientBenchmarks` только группирует данные, но не сохраняет их

---

## 2. Проверка цепочки данных

### 2.1 Источник данных
✅ **Работает:** `sourceDB.GetAllCatalogItems()` (строка 9003)
- Получает все записи из таблицы `catalog_items` базы данных проекта

### 2.2 Обработка данных
✅ **Работает:** `ClientNormalizer.ProcessWithClientBenchmarks`
- Нормализует имена
- Категоризирует товары
- Применяет AI-усиление
- Группирует по категории и нормализованному имени

### 2.3 Сохранение результатов
❌ **НЕ РАБОТАЕТ:** Результаты не сохраняются в `normalized_data`

**Ожидаемое поведение:**
- Сохранение в таблицу `normalized_data` в `normalized_data.db`
- Связь с сессией нормализации через `normalization_session_id`
- Сохранение в `client_benchmarks` для новых эталонов

**Текущее поведение:**
- Результаты только группируются в памяти
- После завершения функции результаты теряются
- Сессия нормализации обновляется как "completed", но данных нет

---

## 3. Проверка альтернативных путей

### 3.1 Старый процессор номенклатуры
**Файл:** `nomenclature/processor.go`

**Метод:** `ProcessAll()` (строка 118)

**Что делает:**
- Обрабатывает записи из `catalog_items` в `normalized_data.db`
- Сохраняет результаты в `catalog_items` с полями `normalized_name`, `kpved_code`, `kpved_name`
- **НЕ использует** `normalized_data` таблицу
- **НЕ работает** с проектами и базами данных клиентов

**API:** `POST /api/nomenclature/process` (строка 3149)

### 3.2 Нормализатор через Normalizer
**Файл:** `normalization/normalizer.go`

**Метод:** `ProcessNormalization(uploadID int)` (строка 175)

**Что делает:**
- Сохраняет результаты в `normalized_data` через `InsertNormalizedItemsWithAttributesBatch` (строка 429, 493)
- Работает с одной базой данных
- **НЕ используется** в `handleStartClientNormalization` для номенклатуры

---

## 4. Выявленные проблемы

### 4.1 Критическая проблема: Отсутствие сохранения результатов

**Описание:**
`ClientNormalizer.ProcessWithClientBenchmarks` не сохраняет нормализованные данные в `normalized_data`.

**Последствия:**
- Нормализация выполняется, но результаты не сохраняются
- API `/api/clients/{id}/projects/{projectId}/nomenclature` не возвращает нормализованные данные
- Сессия нормализации помечается как "completed", но данных нет

**Решение:**
Необходимо добавить сохранение результатов после вызова `ProcessWithClientBenchmarks`:

```go
// После строки 9057 в server.go
result, err := clientNormalizer.ProcessWithClientBenchmarks(items)
if err == nil {
    // Сохранить результаты в normalized_data
    normalizedItems := convertGroupsToNormalizedItems(groups, projectID, sessionID)
    _, err = normalizedDB.InsertNormalizedItemsWithAttributesBatch(normalizedItems, attributes, &sessionID)
}
```

### 4.2 Проблема: Неиспользуемый код

**Описание:**
В `server.go:9086-9104` есть код для классификации КПВЭД и ОКПД2, который ищет файл `normalized_{filename}.db`, но этот файл не создается при нормализации через `ClientNormalizer`.

**Последствия:**
- Классификация КПВЭД/ОКПД2 не выполняется
- Данные не сохраняются в отдельную нормализованную БД

---

## 5. Рекомендации

### 5.1 Немедленные действия

1. **Добавить сохранение результатов:**
   - После `ProcessWithClientBenchmarks` преобразовать группы в `NormalizedItem`
   - Сохранить в `normalized_data` через `InsertNormalizedItemsWithAttributesBatch`
   - Использовать `normalized_data.db` (глобальная БД) или создать отдельную БД для проекта

2. **Исправить сохранение в normalized_data.db:**
   - Добавить `project_id` в `normalized_data` при сохранении
   - Связать с сессией нормализации через `normalization_session_id`

### 5.2 Долгосрочные улучшения

1. **Унифицировать нормализацию:**
   - Использовать единый подход для контрагентов и номенклатуры
   - `ClientNormalizer` должен сохранять результаты аналогично `CounterpartyNormalizer`

2. **Добавить проверку результатов:**
   - После нормализации проверять, что данные сохранены
   - Валидировать количество сохраненных записей

3. **Улучшить тестирование:**
   - Добавить интеграционные тесты для полной цепочки
   - Проверять сохранение данных в БД после нормализации

---

## 6. Тестирование

### 6.1 Текущее состояние

**Что работает:**
- ✅ API endpoint принимает запросы
- ✅ Получение данных из баз проекта
- ✅ Нормализация имен и категоризация
- ✅ Создание сессий нормализации

**Что не работает:**
- ❌ Сохранение результатов в `normalized_data`
- ❌ Получение нормализованных данных через API
- ❌ Классификация КПВЭД/ОКПД2 после нормализации

### 6.2 Тестовый сценарий

1. **Подготовка:**
   - Создать клиента и проект
   - Загрузить базу данных с номенклатурой
   - Убедиться, что в БД есть записи в `catalog_items`

2. **Запуск нормализации:**
   ```bash
   curl -X POST http://localhost:9999/api/clients/1/projects/1/normalization/start \
     -H "Content-Type: application/json" \
     -d '{"all_active": true}'
   ```

3. **Проверка результатов:**
   - Проверить сессии нормализации: `GET /api/clients/1/projects/1/normalization/sessions`
   - Проверить нормализованные данные: `GET /api/clients/1/projects/1/nomenclature`
   - **Ожидается:** Данные должны быть в `normalized_data`
   - **Реально:** Данных нет, так как они не сохраняются

---

## 7. Заключение

**Статус:** ❌ Цепочка нормализации номенклатуры **НЕ РАБОТАЕТ ПОЛНОСТЬЮ**

**Основная проблема:** Результаты нормализации не сохраняются в базу данных.

**Приоритет исправления:** 🔴 КРИТИЧЕСКИЙ

**Время на исправление:** ~2-4 часа (добавление сохранения результатов)

---

## 8. Следующие шаги

1. Исправить `ClientNormalizer.ProcessWithClientBenchmarks` или добавить сохранение после вызова
2. Добавить тесты для проверки сохранения данных
3. Провести повторное тестирование полной цепочки
4. Обновить документацию API

