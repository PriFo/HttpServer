# Быстрый старт: Исправление ошибок 500

## 🚀 Быстрая инструкция

### Шаг 1: Проверка изменений

Убедитесь, что следующие файлы изменены:
- ✅ `server/server.go` - исправлены обработчики
- ✅ `server/server_dashboard.go` - исправлен обработчик статуса
- ✅ `frontend/components/processes/normalization-process-tab.tsx` - улучшена обработка ошибок
- ✅ `frontend/components/processes/normalization-results-table.tsx` - улучшена обработка ошибок

### Шаг 2: Перезапуск сервера

**Windows:**
```powershell
# Остановите текущий процесс (Ctrl+C или завершите процесс)
# Затем запустите заново
go run .
# или
.\httpserver.exe
```

**Linux/Docker:**
```bash
# Остановите
docker-compose down
# или
sudo systemctl stop httpserver

# Запустите
docker-compose up -d
# или
sudo systemctl start httpserver
```

### Шаг 3: Тестирование

**Windows PowerShell:**
```powershell
.\test_fixed_endpoints.ps1 9999
```

**Linux/WSL Bash:**
```bash
chmod +x test_fixed_endpoints.sh
./test_fixed_endpoints.sh 9999
```

**Или вручную:**
```bash
# Тест 1: Поиск проекта
curl "http://localhost:9999/api/databases/find-project?file_path=Выгрузка_Номенклатура_ERPWE_Unknown_Unknown_2025_11_20_10_18_55.db"

# Тест 2: Статус нормализации
curl "http://localhost:9999/api/normalization/status"

# Тест 3: Статус для дашборда
curl "http://localhost:9999/api/dashboard/normalization-status"
```

### Шаг 4: Проверка логов

Проверьте логи сервера на наличие:
- ✅ Сообщений о запросах (не ошибок 500)
- ✅ Отсутствие паник (panic)
- ✅ Информативные сообщения об ошибках

## ✅ Ожидаемые результаты

После исправлений:
- ✅ Нет ошибок 500 (вместо них - понятные сообщения)
- ✅ Корректные HTTP статусы (200, 404, 400)
- ✅ Детальное логирование в консоли сервера
- ✅ Информативные сообщения об ошибках на фронтенде

## 📚 Дополнительная документация

- **Подробности исправлений:** `BUGFIX_500_ERRORS.md`
- **Чеклист развертывания:** `DEPLOYMENT_CHECKLIST.md`
- **Краткая сводка:** `CHANGES_SUMMARY.md`
- **Финальный отчет:** `FINAL_BUGFIX_REPORT.md`

## ⚠️ Если что-то пошло не так

1. Проверьте логи сервера
2. Убедитесь, что сервер запущен на правильном порту
3. Проверьте, что изменения применены
4. См. `DEPLOYMENT_CHECKLIST.md` раздел "Диагностика проблем"

---

**Время выполнения:** ~5 минут  
**Сложность:** Низкая

