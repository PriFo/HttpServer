# HTTP Server - Система нормализации данных

Профессиональная система для нормализации и обработки данных из 1С, включающая мульти-провайдерную AI-классификацию, управление качеством данных, мониторинг и аналитику.

## 📋 Проект

### Описание

Система предназначена для приема данных из 1С (константы и справочники) через HTTP-интерфейс, их нормализации с использованием AI-технологий, обнаружения дубликатов, классификации по различным справочникам (КПВЭД, ОКПД2) и предоставления аналитики качества данных.

### Ключевые особенности

- **Мульти-провайдерная нормализация**: Поддержка множества AI-провайдеров (OpenRouter, Hugging Face, Arliai, Eden AI, DaData, Adata.kz) для нормализации имен контрагентов и номенклатуры
- **AI-классификация**: Автоматическая классификация номенклатуры по справочникам КПВЭД и ОКПД2 с использованием иерархических алгоритмов
- **Обнаружение дубликатов**: Интеллектуальное обнаружение и объединение дубликатов в нормативно-справочной информации
- **Управление качеством данных**: Комплексный анализ качества данных с оценкой полноты, уникальности и корректности
- **Мониторинг в реальном времени**: Отслеживание процессов нормализации и производительности AI-провайдеров через Server-Sent Events
- **Отчетность**: Генерация детальных отчетов по нормализации и качеству данных с возможностью экспорта
- **Управление клиентами и проектами**: Многоуровневая система управления клиентами, проектами и базами данных

### Технологический стек

**Backend:**
- Go 1.25+
- SQLite (основная БД, нормализованная БД, сервисная БД)
- Gin (HTTP framework)
- Swagger/OpenAPI для документации API

**Frontend:**
- Next.js 16 (App Router)
- TypeScript
- Tailwind CSS
- shadcn/ui компоненты
- React Server Components

---

## 🏗 Архитектура

### Общая схема взаимодействия

```
┌─────────────────┐      ┌──────────────┐      ┌─────────────────┐
│  1С System      │─────▶│  HTTP Server │─────▶│  SQLite DB      │
│  (XML Export)   │      │  (Go Backend)│      │  (Primary)      │
└─────────────────┘      └──────────────┘      └─────────────────┘
                                │
                                │ API
                                ▼
                         ┌──────────────┐
                         │ Next.js      │
                         │ Frontend     │
                         │ (Port 3000)  │
                         └──────────────┘
```

### Backend архитектура

Модульная архитектура Go-приложения:

```
server/
├── handlers/          # HTTP-обработчики (API endpoints)
│   ├── databases.go   # Управление базами данных
│   ├── normalization.go # Нормализация данных
│   ├── classification.go # Классификация
│   ├── quality.go     # Качество данных
│   ├── monitoring.go  # Мониторинг
│   └── ...
├── services/          # Бизнес-логика
│   ├── normalization_service.go
│   ├── classification_service.go
│   ├── quality_service.go
│   └── ...
├── models/            # Модели данных
├── middleware/        # Middleware (аутентификация, логирование, CORS)
├── config.go          # Конфигурация
└── server.go          # Основной HTTP сервер
```

**Основные компоненты:**

- **Handlers**: Обработка HTTP-запросов, валидация входных данных
- **Services**: Бизнес-логика, работа с данными, интеграция с AI-провайдерами
- **Models**: Структуры данных, DTO
- **Middleware**: Логирование, CORS, обработка ошибок

### Legacy рефакторинг (фазы 7–8)

- Основной прогресс документируется в `docs/ENTERPRISE_REFACTORING_PHASES.md`, `docs/MIGRATION.md` и `docs/server_reduction.md`.
- Структура для legacy-хендлеров располагается в `server/handlers/legacy/` и `server/services/legacy/` (см. `server/handlers/legacy/README.md`).
- Используйте утилиты:
  - `scripts/migrate_server_file.sh [--dry-run] server/server_patterns.go server/handlers/legacy` — копирование и переименование `server_*.go`.
  - `scripts/server_refactor_status.sh` — контроль размеров `server.go` (этапы 4000/2000/<1000 строк).
- Централизованная регистрация старых маршрутов: `internal/api/routes/legacy_routes.go` + `server/legacy_routes_adapter.go` (`/api/legacy/...`).
- Качество контролируется workflow `.github/workflows/refactoring-checks.yml` и smoke-тестами `tests/regression/smoke_test.go`.

### Frontend архитектура

Next.js 16 с App Router:

```
frontend/
├── app/               # App Router страницы
│   ├── page.tsx       # Главная страница
│   ├── normalization/ # Страница нормализации
│   ├── monitoring/    # Мониторинг
│   ├── quality/       # Качество данных
│   └── api/           # API routes (прокси к Go backend)
├── components/        # React компоненты
│   ├── ui/            # shadcn/ui компоненты
│   ├── dashboard/     # Компоненты дашборда
│   ├── normalization/ # Компоненты нормализации
│   └── ...
├── hooks/             # React hooks
├── lib/               # Утилиты, API клиент
└── types/             # TypeScript типы
```

**Ключевые технологии:**

- **Next.js 16 App Router**: Server Components, Server Actions
- **TypeScript**: Типобезопасность
- **Tailwind CSS**: Стилизация
- **shadcn/ui**: UI компоненты
- **Zustand**: Управление состоянием
- **React Query**: Управление серверным состоянием
- **Server-Sent Events**: Реальное время обновления

### База данных

**SQLite** используется для хранения данных в трех отдельных базах:

1. **Основная БД** (`data.db` или `1c_data.db`):
   - `uploads` - выгрузки из 1С
   - `constants` - константы
   - `catalogs` - справочники
   - `catalog_items` - элементы справочников
   - `nomenclature_items` - номенклатура с характеристиками

2. **Нормализованная БД** (`normalized_data.db`):
   - Нормализованные данные контрагентов
   - Нормализованная номенклатура
   - Группы нормализации

3. **Сервисная БД** (`service.db`):
   - `clients` - клиенты
   - `client_projects` - проекты клиентов
   - `client_benchmarks` - эталонные записи
   - `project_databases` - базы данных проектов
   - `normalization_config` - конфигурации нормализации

---

## 🚀 Начало работы

### Требования

- **Go**: 1.25 или выше
- **Node.js**: 18 или выше
- **pnpm**: Менеджер пакетов для Node.js
- **CGO**: Требуется для работы с SQLite (GCC/MinGW-w64 на Windows)
- **Docker** (опционально): Для запуска через docker-compose

### Установка бэкенда

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd HttpServer
```

2. Установите зависимости:
```bash
go mod download
go mod tidy
```

3. Настройте переменные окружения (создайте `.env` файл или экспортируйте переменные):
```env
SERVER_PORT=9999
DATABASE_PATH=data.db
NORMALIZED_DATABASE_PATH=normalized_data.db
SERVICE_DATABASE_PATH=service.db
ARLIAI_API_KEY=your_arliai_api_key
DADATA_API_KEY=your_dadata_api_key
DADATA_SECRET_KEY=your_dadata_secret_key
```

4. Запустите сервер:
```bash
go run main.go
```

Или скомпилируйте и запустите:
```bash
go build -o httpserver.exe .
./httpserver.exe
```

Сервер будет доступен на `http://localhost:9999`

### CLI-утилиты для поддержки контрагентов

Для быстрой проверки состояния мэппинга и нормализации используйте готовые команды:

- Просмотр сводной статистики (общее количество, уникальные названия, группы дублей, записи без ИНН/БИН):
  ```bash
  go run ./cmd/check_stats --db service.db --project 3
  ```
- Очистка названий и приведение юридических форм (с `--dry-run` для предварительного просмотра):
  ```bash
  go run ./cmd/normalize_counterparties --db service.db --project 3 --dry-run
  ```

Уберите `--dry-run`, чтобы применить изменения. Обе утилиты пишут детальные логи и подходят для регрессионных проверок на продакшн-дампах.

### Установка фронтенда

1. Перейдите в директорию фронтенда:
```bash
cd frontend
```

2. Установите зависимости:
```bash
pnpm install
```

3. Создайте файл `.env.local` для разработки:
```env
BACKEND_URL=http://localhost:9999
SERVICE_DB_NAME=Сервисная БД
```

4. Запустите dev-сервер:
```bash
pnpm run dev
```

Frontend будет доступен на `http://localhost:3000`

### Настройка переменных окружения

**Backend (.env или переменные окружения):**

```env
# Сервер
SERVER_PORT=9999

# Базы данных
DATABASE_PATH=data.db
NORMALIZED_DATABASE_PATH=normalized_data.db
SERVICE_DATABASE_PATH=service.db

# AI провайдеры
ARLIAI_API_KEY=your_key_here
ARLIAI_MODEL=GLM-4.5-Air

# DaData
DADATA_API_KEY=your_key_here
DADATA_SECRET_KEY=your_secret_here
DADATA_ENABLED=true

# Adata.kz
ADATA_API_KEY=your_key_here
ADATA_ENABLED=true

# Connection pooling
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=300s
```

**Frontend (.env.local):**

```env
BACKEND_URL=http://localhost:9999
SERVICE_DB_NAME=Сервисная БД
```

### Docker (быстрый старт)

1. Запустите все сервисы через docker-compose:
```bash
docker-compose up -d
```

Backend будет доступен на `http://localhost:9999`, Frontend на `http://localhost:3000`

2. Остановка:
```bash
docker-compose down
```

---

## 📁 Структура проекта

```
HttpServer/
├── cmd/                    # CLI утилиты
│   ├── normalize/          # Утилита нормализации
│   ├── classify_nomenclature/ # Классификация номенклатуры
│   ├── load_kpved/         # Загрузка справочника КПВЭД
│   └── ...
├── server/                 # Go бэкенд
│   ├── handlers/           # HTTP-обработчики
│   │   ├── databases.go    # Управление БД
│   │   ├── normalization.go # Нормализация
│   │   ├── classification.go # Классификация
│   │   ├── quality.go      # Качество данных
│   │   ├── monitoring.go   # Мониторинг
│   │   └── ...
│   ├── services/           # Бизнес-логика
│   │   ├── normalization_service.go
│   │   ├── classification_service.go
│   │   └── ...
│   ├── models/             # Модели данных
│   ├── middleware/         # Middleware
│   ├── config.go           # Конфигурация
│   └── server.go           # Основной сервер
├── database/               # Работа с БД
│   ├── db.go               # Основные функции БД
│   ├── schema.go           # Схема БД
│   └── service_db.go       # Сервисная БД
├── frontend/               # Next.js фронтенд
│   ├── app/                # App Router страницы
│   │   ├── page.tsx        # Главная
│   │   ├── normalization/  # Нормализация
│   │   ├── monitoring/     # Мониторинг
│   │   └── api/            # API routes (прокси)
│   ├── components/         # React компоненты
│   │   ├── ui/             # shadcn/ui
│   │   ├── dashboard/      # Дашборд
│   │   └── ...
│   ├── hooks/              # React hooks
│   ├── lib/                # Утилиты, API клиент
│   └── package.json
├── tests/                  # Тесты
│   ├── integration/        # Интеграционные тесты (Go)
│   └── e2e/                # E2E тесты (Playwright)
├── docs/                   # Документация
├── .todos/                 # Система управления TODO
├── main.go                 # Точка входа
├── go.mod                  # Go зависимости
├── docker-compose.yml      # Docker конфигурация
├── Dockerfile              # Docker образ backend
└── README.md               # Этот файл
```

---

## 💻 Руководство по разработке

### Стандарты кода

**Backend (Go):**
- Используйте `gofmt` для форматирования кода
- Запускайте `golangci-lint` для проверки качества кода
- Следуйте [Effective Go](https://go.dev/doc/effective_go) рекомендациям

**Frontend (TypeScript/React):**
- Используйте ESLint для проверки кода
- Следуйте правилам Next.js и React best practices
- Используйте TypeScript для типобезопасности

### Добавление нового API эндпоинта

1. **Создайте handler** в `server/handlers/`:
```go
// server/handlers/example.go
package handlers

func (s *Server) HandleExample(c *gin.Context) {
    // Обработка запроса
    c.JSON(http.StatusOK, gin.H{"message": "success"})
}
```

2. **Зарегистрируйте маршрут** в `server/server.go` в методе `setupRouter()`:
```go
router.GET("/api/example", s.HandleExample)
```

3. **Добавьте Swagger аннотации** (см. раздел Swagger):
```go
// @Summary Example endpoint
// @Description Пример эндпоинта
// @Tags example
// @Accept json
// @Produce json
// @Success 200 {object} map[string]interface{}
// @Router /api/example [get]
```

4. **Напишите тесты** в `server/handlers/example_test.go`:
```go
func TestHandleExample(t *testing.T) {
    // Тест
}
```

5. **Обновите документацию API** (Swagger обновится автоматически после генерации)

### Добавление нового фронтенд компонента

1. **Создайте компонент** в `frontend/components/`:
```typescript
// frontend/components/Example.tsx
export function Example() {
  return <div>Example Component</div>
}
```

2. **Используйте в странице** в `frontend/app/`:
```typescript
import { Example } from '@/components/Example'

export default function Page() {
  return <Example />
}
```

3. **Добавьте типы** в `frontend/types/` если необходимо

### Запуск тестов

**Backend:**
```bash
# Все тесты
go test ./...

# Конкретный пакет
go test ./server/handlers

# С покрытием
go test -cover ./...

# Интеграционные тесты
go test ./tests/integration/...
```

**Frontend:**
```bash
cd frontend

# E2E тесты (Playwright)
pnpm test:e2e

# E2E тесты с UI
pnpm test:e2e:ui

# Конкретный тест
pnpm test:e2e tests/e2e/normalization.spec.ts
```

### Система TODO управления

Проект использует автоматизированную систему управления TODO:

```bash
# Сканирование кодовой базы на TODO/FIXME/HACK
npm run todos:scan

# Генерация отчета
npm run todos:report

# Все операции (scan + assign + workload + report)
npm run todos:all
```

Подробнее: `.todos/README.md`

---

## 📚 API документация

### Swagger UI

После запуска сервера Swagger UI доступен по адресу:

```
http://localhost:9999/swagger/index.html
```

Swagger UI предоставляет интерактивную документацию API, позволяющую:
- Просматривать все доступные эндпоинты
- Тестировать API запросы прямо из браузера
- Просматривать схемы запросов и ответов
- Видеть примеры использования

### Генерация документации

1. Убедитесь, что установлен `swag`:
```bash
go install github.com/swaggo/swag/cmd/swag@latest
```

2. Сгенерируйте документацию:
```bash
make swagger
```

Или вручную:
```bash
swag init -g main.go -o ./docs
```

**Примечание:** После добавления или изменения Swagger аннотаций в handlers необходимо перегенерировать документацию с помощью `make swagger` или `swag init -g main.go -o ./docs`.

### Добавление новых эндпоинтов с аннотациями

При добавлении нового API эндпоинта добавьте Swagger аннотации:

```go
// @Summary Краткое описание эндпоинта
// @Description Детальное описание эндпоинта
// @Tags тег
// @Accept json
// @Produce json
// @Param id path int true "ID параметра"
// @Success 200 {object} ResponseModel "Успешный ответ"
// @Failure 400 {object} ErrorResponse "Ошибка запроса"
// @Router /api/example/{id} [get]
func (h *Handler) HandleExample(c *gin.Context) {
    // Обработка запроса
}
```

### Основные эндпоинты

**Управление базами данных:**
- `GET /api/databases` - Список всех баз данных
- `POST /api/databases` - Добавление новой БД
- `GET /api/databases/:id` - Детали БД
- `DELETE /api/databases/:id` - Удаление БД

**Нормализация:**
- `POST /api/normalization/start` - Запуск нормализации
- `GET /api/normalization/status` - Статус нормализации
- `POST /api/normalization/stop` - Остановка нормализации
- `GET /api/normalization/stats` - Статистика нормализации

**Загрузка данных:**
- `POST /api/v1/upload/handshake` - Рукопожатие для новой выгрузки
- `POST /api/v1/upload/metadata` - Метаданные выгрузки
- `POST /api/v1/upload/nomenclature/batch` - Пакетная загрузка номенклатуры

**Классификация:**
- `POST /api/kpved/classify-hierarchical` - Иерархическая классификация
- `GET /api/kpved/stats` - Статистика классификации
- `POST /api/kpved/reclassify` - Переклассификация

**Качество данных:**
- `GET /api/quality/report` - Отчет о качестве данных
- `GET /api/quality/score/:databaseId` - Оценка качества БД

**Мониторинг:**
- `GET /api/monitoring/providers` - Статус AI-провайдеров
- `GET /api/monitoring/metrics` - Метрики производительности

### Примеры запросов

**Получить список баз данных:**
```bash
curl http://localhost:9999/api/databases
```

**Запустить нормализацию:**
```bash
curl -X POST http://localhost:9999/api/normalization/start \
  -H "Content-Type: application/json" \
  -d '{"database_id": 1}'
```

**Получить статус нормализации:**
```bash
curl http://localhost:9999/api/normalization/status
```

Полная документация доступна в Swagger UI после запуска сервера.

---

## 🚢 Развертывание

### Docker

**Сборка и запуск:**
```bash
# Сборка образов
docker-compose build

# Запуск в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

**Переменные окружения для Docker:**
Создайте файл `.env` в корне проекта:
```env
ARLIAI_API_KEY=your_key_here
DADATA_API_KEY=your_key_here
DADATA_SECRET_KEY=your_secret_here
```

### Production развертывание

**Рекомендации:**

1. **HTTPS**: Используйте reverse proxy (nginx, Traefik) с SSL сертификатами (Let's Encrypt)

2. **Reverse Proxy (nginx пример):**
```nginx
server {
    listen 80;
    server_name api.example.com;

    location / {
        proxy_pass http://localhost:9999;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

3. **Systemd service** для backend:
```ini
[Unit]
Description=HTTP Server Backend
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/httpserver
ExecStart=/opt/httpserver/httpserver
Restart=always
Environment="SERVER_PORT=9999"
Environment="DATABASE_PATH=/opt/httpserver/data.db"

[Install]
WantedBy=multi-user.target
```

4. **Переменные окружения для production:**
```env
SERVER_PORT=9999
DATABASE_PATH=/var/lib/httpserver/data.db
NORMALIZED_DATABASE_PATH=/var/lib/httpserver/normalized_data.db
SERVICE_DATABASE_PATH=/var/lib/httpserver/service.db
ARLIAI_API_KEY=your_production_key
DADATA_API_KEY=your_production_key
DADATA_SECRET_KEY=your_production_secret
DB_MAX_OPEN_CONNS=50
DB_MAX_IDLE_CONNS=10
```

5. **Резервное копирование**: Настройте регулярное резервное копирование SQLite баз данных

6. **Мониторинг**: Используйте систему мониторинга (Prometheus, Grafana) для отслеживания метрик

7. **Логирование**: Настройте централизованное логирование (ELK, Loki)

---

## 📖 Дополнительная документация

### Основная документация
- [Quick Start Guide](./docs/QUICK_START.md) - Быстрый старт проекта
- [API Documentation](./API_DOCUMENTATION.md) - Детальная документация API
- [Frontend README](./frontend/README.md) - Документация фронтенда

### Swagger и разработка
- [Swagger Usage Guide](./docs/SWAGGER_USAGE.md) - Руководство по использованию Swagger
- [Swagger Annotations Examples](./docs/SWAGGER_ANNOTATIONS_EXAMPLES.md) - Примеры Swagger аннотаций
- [Migration to Gin Guide](./docs/MIGRATION_TO_GIN.md) - Руководство по миграции на Gin Framework
- [Testing Gin Handlers](./docs/TESTING_GIN_HANDLERS.md) - Руководство по тестированию Gin handlers

### Функциональные руководства
- [Duplicates Guide](./DUPLICATES_START_HERE.md) - Руководство по обнаружению дубликатов
- [1C Integration Guide](./1c_integration_guide.md) - Интеграция с 1С
- [Testing Guide](./frontend/tests/README.md) - Руководство по тестированию

---

## 🤝 Вклад в проект

1. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
2. Внесите изменения и закоммитьте (`git commit -m 'Add amazing feature'`)
3. Запушьте в ветку (`git push origin feature/amazing-feature`)
4. Создайте Pull Request

---

## 📄 Лицензия

Проект для внутреннего использования.

---

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи сервера
2. Убедитесь, что все переменные окружения установлены корректно
3. Проверьте, что порты не заняты другими приложениями
4. Обратитесь к разделу "Возможные проблемы" ниже

### Возможные проблемы

**Порт 9999 занят:**
- Измените `SERVER_PORT` в переменных окружения
- Или остановите приложение, использующее этот порт

**Ошибка компиляции CGO:**
- Убедитесь, что установлен GCC (MinGW-w64 на Windows)
- Установите переменную окружения: `CGO_ENABLED=1`

**База данных не создается:**
- Проверьте права на запись в директорию проекта
- Убедитесь, что CGO компиляция прошла успешно

---

**Версия**: 1.0.0  
**Последнее обновление**: 2025
