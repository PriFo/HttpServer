# Анализ процесса загрузки файлов и их индексации по проектам и клиентам

**Дата:** 2025-01-20  
**Статус:** ✅ Полный анализ выполнен

---

## 📋 Обзор процесса

Процесс загрузки и индексации файлов состоит из нескольких этапов:

1. **Загрузка файла** (Frontend → API Route → Backend)
2. **Сохранение файла** (Backend → File System)
3. **Создание записи в БД** (Backend → ServiceDB)
4. **Связывание с проектом/клиентом** (Backend → ServiceDB)
5. **Индексация** (опционально, для pending databases)

---

## 🔄 Детальный процесс

### Этап 1: Загрузка файла (Frontend)

**Файл:** `frontend/app/clients/[clientId]/projects/[projectId]/page.tsx`

**Процесс:**
1. Пользователь выбирает файл или перетаскивает его
2. Вызывается `handleFileUpload(file, autoConfirm)`
3. Выполняется валидация:
   - Размер файла (минимум 1KB, максимум 500MB)
   - Тип файла (.db, .sqlite, .sqlite3)
   - Имя файла (проверка на недопустимые символы)
4. Создается `FormData` с полем `file`
5. Отправляется POST запрос на `/api/clients/${clientId}/projects/${projectId}/databases`

**Код:**
```typescript
const formData = new FormData()
formData.append('file', file)
formData.append('auto_create', 'false') // или 'true' для автоматического создания

const response = await fetch(`/api/clients/${clientId}/projects/${projectId}/databases`, {
  method: 'POST',
  body: formData,
})
```

---

### Этап 2: Проксирование запроса (API Route)

**Файл:** `frontend/app/api/clients/[clientId]/projects/[projectId]/databases/route.ts`

**Процесс:**
1. API Route получает `multipart/form-data` запрос
2. Извлекает `FormData` из запроса
3. Создает новый `FormData` для передачи на бэкенд
4. Копирует все поля (файлы и текстовые поля)
5. Проксирует запрос на бэкенд: `${API_BASE_URL}/api/clients/${clientId}/projects/${projectId}/databases`
6. Устанавливает таймаут 10 минут для больших файлов

**Особенности:**
- Поддержка множественной загрузки файлов
- Логирование всех этапов для диагностики
- Обработка ошибок с детальными сообщениями

---

### Этап 3: Обработка на бэкенде (Backend)

**Файл:** `server/server.go` → `handleUploadProjectDatabase`

**Процесс:**

#### 3.1. Валидация и очистка имени файла
```go
// Проверка расширения
if !strings.HasSuffix(strings.ToLower(fileName), ".db") {
    return error
}

// Очистка от опасных символов
fileName = strings.ReplaceAll(fileName, "..", "_")
fileName = strings.ReplaceAll(fileName, "/", "_")
// ... и т.д.

// Ограничение длины (максимум 255 символов)
if len(fileName) > 255 {
    // Обрезка с сохранением расширения
}
```

#### 3.2. Создание директории uploads
```go
uploadsDir, err := EnsureUploadsDirectory(".")
// Создает data/uploads/ если не существует
```

#### 3.3. Проверка безопасности (Path Traversal Protection)
```go
// Проверка, что файл находится внутри uploadsDir
absFilePath, _ := filepath.Abs(filePath)
absUploadsDir, _ := filepath.Abs(uploadsDir)

if !strings.HasPrefix(absFilePath, absUploadsDir+string(filepath.Separator)) {
    return error // Path traversal attempt
}
```

#### 3.4. Обработка дубликатов
```go
// Если файл с таким именем уже существует, добавляется timestamp
if _, err := os.Stat(filePath); err == nil {
    timestamp := time.Now().Format("20060102_150405")
    fileName = fmt.Sprintf("%s_%s%s", nameWithoutExt, timestamp, ext)
}
```

#### 3.5. Сохранение файла
```go
dst, err := os.Create(filePath)
bytesWritten, err := io.Copy(dst, file)
// Логирование метрик загрузки
```

#### 3.6. Автоматическое создание записи в БД (если auto_create=true)
```go
if autoCreate {
    database, err := s.serviceDB.CreateProjectDatabase(
        projectID, 
        suggestedName, 
        filePath, 
        description, 
        header.Size
    )
}
```

---

### Этап 4: Создание записи в БД

**Файл:** `database/service_db.go` → `CreateProjectDatabase`

**SQL запрос:**
```sql
INSERT INTO project_databases 
    (client_project_id, name, file_path, description, is_active, file_size, created_at, updated_at)
VALUES (?, ?, ?, ?, TRUE, ?, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
```

**Структура таблицы `project_databases`:**
- `id` - PRIMARY KEY
- `client_project_id` - FOREIGN KEY → `client_projects(id)`
- `name` - Название базы данных
- `file_path` - Путь к файлу (обычно `data/uploads/filename.db`)
- `description` - Описание
- `is_active` - Активна ли база данных
- `file_size` - Размер файла в байтах
- `last_used_at` - Время последнего использования
- `created_at` - Время создания
- `updated_at` - Время обновления

**Связь с проектом и клиентом:**
```
clients (id)
  └── client_projects (id, client_id)
       └── project_databases (id, client_project_id)
```

---

### Этап 5: Перемещение файла в uploads (если необходимо)

**Файл:** `server/database_scanner.go` → `MoveDatabaseToUploads`

**Процесс:**
1. Проверяется, находится ли файл уже в `data/uploads/`
2. Если нет - файл перемещается в `data/uploads/`
3. Обновляется путь в БД

**Код:**
```go
// В handleCreateProjectDatabase
if !strings.HasPrefix(absFilePath, absUploadsDir) {
    newPath, moveErr := MoveDatabaseToUploads(req.FilePath, uploadsDir)
    finalPath = newPath
}
```

---

## 🔍 Pending Databases (Ожидающие индексации)

### Что такое Pending Databases?

Pending databases - это файлы, которые были обнаружены системой (например, через сканирование), но еще не привязаны к конкретному проекту/клиенту.

**Таблица:** `pending_databases`

**Поля:**
- `id` - PRIMARY KEY
- `file_path` - Путь к файлу
- `file_name` - Имя файла
- `file_size` - Размер файла
- `detected_at` - Время обнаружения
- `indexing_status` - Статус индексации (pending, indexing, completed, failed)
- `indexing_started_at` - Время начала индексации
- `indexing_completed_at` - Время завершения индексации
- `error_message` - Сообщение об ошибке (если есть)
- `client_id` - ID клиента (опционально)
- `project_id` - ID проекта (опционально)
- `moved_to_uploads` - Перемещен ли файл в uploads
- `original_path` - Оригинальный путь (до перемещения)

### Процесс привязки Pending Database к проекту

**Эндпоинт:** `POST /api/pending-databases/{id}/bind`

**Файл:** `server/server.go` → `handleBindPendingDatabase`

**Процесс:**
1. Получение pending database по ID
2. Проверка существования проекта и принадлежности клиенту
3. Определение нового пути:
   - Если указан `custom_path` - используется он
   - Иначе - файл перемещается в `data/uploads/`
4. Обновление записи в `pending_databases`:
   ```go
   s.serviceDB.BindPendingDatabaseToProject(
       id, clientID, projectID, newFilePath, movedToUploads
   )
   ```
5. Создание записи в `project_databases`:
   ```go
   s.serviceDB.CreateProjectDatabase(
       projectID, fileName, newFilePath, description, fileSize
   )
   ```
6. Удаление из `pending_databases`:
   ```go
   s.serviceDB.DeletePendingDatabase(id)
   ```

---

## 📊 Схема данных

```
┌─────────────┐
│   clients   │
│  (id, name) │
└──────┬──────┘
       │
       │ 1:N
       │
┌──────▼──────────────┐
│  client_projects   │
│ (id, client_id,    │
│  name, type)       │
└──────┬─────────────┘
       │
       │ 1:N
       │
┌──────▼──────────────────┐
│   project_databases     │
│ (id, client_project_id,│
│  name, file_path,      │
│  file_size, is_active) │
└─────────────────────────┘

┌─────────────────────┐
│  pending_databases   │
│ (id, file_path,     │
│  file_name,         │
│  indexing_status,   │
│  client_id,         │
│  project_id)        │
└─────────────────────┘
```

---

## 🔐 Безопасность

### Реализованные меры защиты:

1. **Path Traversal Protection**
   - Проверка абсолютных путей
   - Очистка имени файла от опасных символов
   - Ограничение пути файла директорией uploads

2. **Валидация файлов**
   - Проверка расширения (.db, .sqlite, .sqlite3)
   - Проверка размера (минимум 1KB, максимум 500MB)
   - Ограничение длины имени файла (255 символов)

3. **Очистка имени файла**
   - Удаление символов: `..`, `/`, `\`, `:`, `*`, `?`, `"`, `<`, `>`, `|`
   - Замена на `_`

4. **Проверка прав доступа**
   - Проверка принадлежности проекта клиенту
   - Валидация client_id и project_id

---

## ⚠️ Потенциальные проблемы и улучшения

### 1. Дублирование файлов

**Проблема:** Если файл с таким же именем уже существует, добавляется timestamp, но не проверяется содержимое файла.

**Решение:** Можно добавить проверку хеша файла для обнаружения дубликатов.

### 2. Отсутствие транзакций

**Проблема:** При создании записи в БД и перемещении файла нет атомарности. Если перемещение файла успешно, но создание записи в БД провалилось, файл останется в uploads без записи в БД.

**Решение:** Добавить откат (rollback) при ошибках.

### 3. Нет проверки целостности файла

**Проблема:** Не проверяется, является ли файл валидной SQLite базой данных.

**Решение:** Добавить проверку с помощью `sql.Open` и `Ping()`.

### 4. Нет ограничения на количество файлов

**Проблема:** Нет ограничения на количество баз данных в проекте.

**Решение:** Добавить проверку лимита (например, максимум 100 баз данных на проект).

### 5. Нет автоматической очистки старых pending databases

**Проблема:** Pending databases могут накапливаться, если не привязываются к проектам.

**Решение:** Добавить автоматическую очистку старых pending databases (например, старше 30 дней).

---

## 📝 Рекомендации по улучшению

### 1. ✅ Реализовано: Проверка целостности файла

**Файл:** `server/database_scanner.go` → `ValidateSQLiteDatabase`

```go
// ValidateSQLiteDatabase проверяет целостность SQLite файла
func ValidateSQLiteDatabase(filePath string) error {
    db, err := sql.Open("sqlite3", filePath)
    if err != nil {
        return fmt.Errorf("failed to open database: %w", err)
    }
    defer db.Close()

    if err := db.Ping(); err != nil {
        return fmt.Errorf("failed to ping database: %w", err)
    }

    // Проверяем целостность простым запросом
    var result int
    err = db.QueryRow("SELECT 1").Scan(&result)
    if err != nil {
        return fmt.Errorf("database integrity check failed: %w", err)
    }

    return nil
}
```

**Использование:** Вызывается в `handleUploadProjectDatabase` после сохранения файла.

### 2. Добавить транзакции

```go
// Начать транзакцию
tx, err := db.conn.Begin()
if err != nil {
    return err
}
defer tx.Rollback()

// Переместить файл
newPath, err := MoveDatabaseToUploads(...)
if err != nil {
    return err
}

// Создать запись в БД
_, err = tx.Exec("INSERT INTO project_databases ...")
if err != nil {
    // Файл уже перемещен, но можно откатить
    return err
}

// Зафиксировать транзакцию
return tx.Commit()
```

### 3. ✅ Реализовано: Проверка дубликатов и вычисление хеша файла

**Файл:** `server/database_scanner.go` → `CalculateFileHash`

```go
// CalculateFileHash вычисляет SHA256 хеш файла
func CalculateFileHash(filePath string) (string, error) {
    file, err := os.Open(filePath)
    if err != nil {
        return "", fmt.Errorf("failed to open file: %w", err)
    }
    defer file.Close()

    hash := sha256.New()
    if _, err := io.Copy(hash, file); err != nil {
        return "", fmt.Errorf("failed to read file: %w", err)
    }

    return hex.EncodeToString(hash.Sum(nil)), nil
}
```

**Проверка дубликатов:**
- Функция `GetProjectDatabaseByFilePath` проверяет дубликаты по пути к файлу
- Используется в `handleUploadProjectDatabase` перед созданием записи в БД
- При обнаружении дубликата возвращается существующая база данных

**Примечание:** Для проверки дубликатов по содержимому файла (хешу) потребуется добавить поле `file_hash` в таблицу `project_databases` и миграцию.

### 4. ✅ Реализовано: Лимиты на количество баз данных

**Файл:** `database/service_db.go` → `GetProjectDatabaseCount`

```go
// GetProjectDatabaseCount получает количество баз данных в проекте
func (db *ServiceDB) GetProjectDatabaseCount(projectID int, activeOnly bool) (int, error) {
    query := `SELECT COUNT(*) FROM project_databases WHERE client_project_id = ?`
    if activeOnly {
        query += " AND is_active = TRUE"
    }
    
    var count int
    err := db.conn.QueryRow(query, projectID).Scan(&count)
    if err != nil {
        return 0, fmt.Errorf("failed to get project database count: %w", err)
    }
    return count, nil
}
```

**Использование:** Проверка выполняется в `handleUploadProjectDatabase` перед созданием записи в БД.

**Лимит:** Максимум 100 баз данных на проект (константа `MAX_DATABASES_PER_PROJECT`).

### 5. ✅ Реализовано: Автоматическая очистка старых pending databases

**Файл:** `database/service_db.go` → `CleanupOldPendingDatabases`

```go
// CleanupOldPendingDatabases удаляет старые pending databases (старше указанного количества дней)
func (db *ServiceDB) CleanupOldPendingDatabases(daysOld int) (int, error) {
    cutoffDate := time.Now().AddDate(0, 0, -daysOld)
    
    query := `
        DELETE FROM pending_databases 
        WHERE detected_at < ? 
        AND indexing_status = 'pending'
        AND client_id IS NULL
        AND project_id IS NULL
    `
    
    result, err := db.conn.Exec(query, cutoffDate)
    if err != nil {
        return 0, fmt.Errorf("failed to cleanup old pending databases: %w", err)
    }
    
    deleted, err := result.RowsAffected()
    if err != nil {
        return 0, fmt.Errorf("failed to get rows affected: %w", err)
    }
    
    return int(deleted), nil
}
```

**Эндпоинт:** `POST /api/databases/pending/cleanup?days=30`

**Использование:**
- По умолчанию удаляет pending databases старше 30 дней
- Можно указать количество дней через параметр `days`
- Удаляются только pending databases без привязки к проекту/клиенту

---

## ✅ Текущее состояние

### Что работает хорошо:

1. ✅ Безопасная загрузка файлов с защитой от path traversal
2. ✅ Валидация файлов на фронтенде и бэкенде
3. ✅ Автоматическое перемещение файлов в `data/uploads/`
4. ✅ Связывание файлов с проектами и клиентами
5. ✅ Поддержка pending databases для файлов без привязки
6. ✅ Обработка дубликатов имен файлов
7. ✅ Логирование всех этапов процесса
8. ✅ Метрики загрузки (скорость, время, размер)
9. ✅ **НОВОЕ:** Проверка целостности SQLite файлов через `sql.Open` и `Ping()`
10. ✅ **НОВОЕ:** Лимит на количество баз данных в проекте (максимум 100)
11. ✅ **НОВОЕ:** Проверка дубликатов по пути к файлу
12. ✅ **НОВОЕ:** Автоматическая очистка старых pending databases
13. ✅ **НОВОЕ:** Функция для вычисления хеша файлов (SHA256)

### Что можно улучшить:

1. ✅ **Добавлено:** Проверка целостности SQLite файлов через `sql.Open` и `Ping()`
2. ⚠️ Добавить транзакции для атомарности операций
3. ✅ **Добавлено:** Проверка дубликатов по пути к файлу
4. ✅ **Добавлено:** Лимиты на количество баз данных (максимум 100 на проект)
5. ✅ **Добавлено:** Автоматическая очистка старых pending databases
6. ✅ **Добавлено:** Функция для вычисления хеша файлов (готово к использованию)

---

## 📚 Связанные файлы

### Frontend:
- `frontend/app/clients/[clientId]/projects/[projectId]/page.tsx` - Загрузка файлов
- `frontend/app/api/clients/[clientId]/projects/[projectId]/databases/route.ts` - API Route
- `frontend/app/databases/pending/page.tsx` - Управление pending databases

### Backend:
- `server/server.go` - Обработка загрузки и привязки
- `server/database_scanner.go` - Утилиты для работы с файлами
- `database/service_db.go` - Работа с БД
- `database/schema.go` - Схема БД

---

**Анализ завершен. Все этапы процесса задокументированы.** ✅

