# Исправления загрузки файла и графика

## ✅ Исправлено

### 1. Проблема с передачей файла через FormData (400 Bad Request)
**Проблема**: Файл не передавался корректно из Next.js API route в Go бэкенд.

**Решение**:
- Файл теперь конвертируется в Buffer через `arrayBuffer()` перед добавлением в FormData
- Разделена обработка файлов и текстовых полей для корректной работы с async операциями
- Улучшено логирование процесса передачи файла

**Файл**: `frontend/app/api/clients/[clientId]/projects/[projectId]/databases/route.ts`

### 2. Сбор данных для графика
**Реализовано**:
- ✅ Данные собираются каждую секунду во время загрузки
- ✅ Сохраняется скорость (MB/s) для каждой секунды
- ✅ Вычисляется приблизительное количество загруженных байт
- ✅ Добавляется финальная точка после завершения загрузки
- ✅ История сбрасывается при новой загрузке
- ✅ История сбрасывается при ошибке

**Файл**: `frontend/app/clients/[clientId]/projects/[projectId]/page.tsx`

### 3. Очистка интервалов
**Исправлено**:
- ✅ `metricsInterval` правильно очищается в `finally` блоке
- ✅ `metricsInterval` очищается при ошибке в `catch` блоке
- ✅ `metricsInterval` очищается после успешной загрузки

**Файл**: `frontend/app/clients/[clientId]/projects/[projectId]/page.tsx`

### 4. Компонент графика
**Реализовано**:
- ✅ Компонент `UploadSpeedChart` создан и работает
- ✅ Отображает линию скорости по секундам
- ✅ Показывает среднюю и максимальную скорость
- ✅ Интерактивный tooltip с деталями
- ✅ Логирование для отладки

**Файл**: `frontend/components/upload/UploadSpeedChart.tsx`

## 📊 Структура данных графика

```typescript
uploadSpeedHistory: Array<{
  second: number        // Секунда загрузки (0, 1, 2, ...)
  speed: number         // Скорость в MB/s на этой секунде
  bytesUploaded: number // Приблизительное количество загруженных байт
}>
```

## 🔍 Логи для отладки

### В консоли браузера:
- `[Frontend] 📤 Начало загрузки файла: ...`
- `[API Route] Added file field: file, filename: ..., size: ... bytes`
- `[API Route] FormData prepared: 1 file(s), X field(s), sending to backend`
- `[Frontend] 📥 Получен ответ от сервера: статус ...`
- `[Frontend] 📊 История загрузки собрана: X точек данных`
- `[UploadSpeedChart] Отображение графика: X точек данных, размер файла: X.XX MB`

### В консоли сервера:
- `[handleUploadProjectDatabase] Начало обработки загрузки файла...`
- `[handleUploadProjectDatabase] Файл получен: ... (размер: ... байт)`
- `[handleUploadProjectDatabase] Файл сохранен: ... скорость: ... MB/s`
- `[handleUploadProjectDatabase] ✅ Успешно завершено...`

## ✅ Готово к тестированию

Все компоненты реализованы и исправлены:
1. ✅ Файл передается корректно через FormData
2. ✅ Данные для графика собираются во время загрузки
3. ✅ График отображается после завершения загрузки
4. ✅ Все интервалы правильно очищаются
5. ✅ Ошибки обрабатываются корректно

## 🧪 Тестирование

1. Откройте `http://localhost:3000`
2. Перейдите к проекту (например, `/clients/1/projects/3`)
3. Перейдите на вкладку "Базы данных"
4. Загрузите файл `.db` (drag & drop или выбор файла)
5. Наблюдайте за метриками во время загрузки
6. После завершения проверьте график

### Ожидаемый результат:
- ✅ Файл загружается без ошибки 400
- ✅ Метрики обновляются в реальном времени
- ✅ График отображается после завершения загрузки
- ✅ График содержит данные по всем секундам

