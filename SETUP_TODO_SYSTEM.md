# 🎯 Настройка автоматизированной системы TODO

## ✅ Система успешно установлена!

Автоматизированная система управления TODO задачами готова к использованию.

## 📁 Созданные файлы

```
.todos/
├── tasks.json              # База данных задач
├── team.json              # Конфигурация команды
├── config.json            # Настройки системы
├── dashboard.html         # Веб-дашборд
├── README.md              # Документация
├── scripts/
│   ├── scan-todos.sh      # Скрипт сканирования
│   ├── generate-report.js  # Генерация отчетов
│   ├── assign-tasks.py    # Автоназначение задач
│   └── automated-system.sh # Автоматический демон
└── .gitignore             # Игнорируемые файлы

.git/hooks/
└── pre-commit             # Git hook для проверки

.cursorrules               # Конфигурация для Cursor
```

## 🚀 Быстрый старт

### 1. Первое сканирование

Из корня проекта:
```bash
bash .todos/scripts/scan-todos.sh
```

Или из frontend:
```bash
cd frontend
npm run todos:scan
```

### 2. Генерация отчета

```bash
npm run todos:report
```

Или напрямую:
```bash
node .todos/scripts/generate-report.js
```

### 3. Автоназначение задач

```bash
npm run todos:assign
```

Автоматически назначает нераспределенные задачи командам на основе типа файла.

### 4. Обновление загруженности

```bash
npm run todos:workload
```

Обновляет статистику загруженности каждого разработчика в `team.json`.

### 5. Просмотр дашборда

Откройте в браузере:
```
.todos/dashboard.html
```

### 6. Полный цикл (рекомендуется)

```bash
npm run todos:all
```

Выполняет все операции: сканирование → назначение → обновление загруженности → отчет.

## 📊 Использование

### Регулярное сканирование

Добавьте в cron (Linux/Mac) или Task Scheduler (Windows):

**Linux/Mac:**
```bash
# Каждый час
0 * * * * cd /path/to/project && bash .todos/scripts/scan-todos.sh

# Ежедневный отчет в 9:00
0 9 * * * cd /path/to/project && node .todos/scripts/generate-report.js
```

**Windows (PowerShell):**
```powershell
# Создать задачу в Task Scheduler
$action = New-ScheduledTaskAction -Execute "bash" -Argument ".todos/scripts/scan-todos.sh" -WorkingDirectory "E:\HttpServer"
$trigger = New-ScheduledTaskTrigger -Once -At (Get-Date) -RepetitionInterval (New-TimeSpan -Hours 1) -RepetitionDuration (New-TimeSpan -Days 365)
Register-ScheduledTask -TaskName "TODO Scanner" -Action $action -Trigger $trigger
```

### Автоматический демон

Запуск фонового процесса (Linux/Mac):
```bash
nohup bash .todos/scripts/automated-system.sh > .todos/system.log 2>&1 &
```

## ⚙️ Настройка

### Команда разработчиков

Отредактируйте `.todos/team.json`:
```json
{
  "team": {
    "backend-team": ["ваш-dev-1", "ваш-dev-2"],
    "frontend-team": ["ваш-dev-3", "ваш-dev-4"]
  }
}
```

### Приоритеты и цвета

Настройки в `.todos/config.json`:
```json
{
  "priorities": {
    "CRITICAL": {
      "color": "RED",
      "label": "Блокирующие",
      "blockCommit": true
    }
  }
}
```

## 🔍 Формат TODO в коде

Система автоматически находит:

```go
// TODO(CRITICAL): This needs immediate attention
// TODO(HIGH): Important task
// TODO(MEDIUM): Regular task
// TODO(LOW): Cosmetic improvement
// FIXME: Something broken
// HACK: Temporary solution
```

## 🎯 Git Hook

Pre-commit hook автоматически проверяет критические TODO.

Для обхода:
```bash
git commit --no-verify
```

## 📈 Метрики

После сканирования вы получите:

- ✅ Общее количество задач
- ✅ Распределение по приоритетам
- ✅ Распределение по типам
- ✅ Распределение по командам
- ✅ Список критических задач

## 🛠️ Управление задачами

### Обновление статуса

Отредактируйте `.todos/tasks.json`:
```json
{
  "status": "IN_PROGRESS",
  "updatedAt": "2025-01-20T10:00:00Z"
}
```

### Автоназначение

```bash
python3 .todos/scripts/assign-tasks.py
```

## 📝 Примеры использования

### Найти все критические задачи

```bash
jq '.tasks[] | select(.priority == "CRITICAL" and .status == "OPEN")' .todos/tasks.json
```

### Статистика по файлам

```bash
jq '.tasks | group_by(.fileType) | map({type: .[0].fileType, count: length})' .todos/tasks.json
```

## 🔗 Интеграция с Cursor

Система автоматически интегрирована через `.cursorrules`.

Команды доступны через:
- Command Palette: `npm run todos:*`
- Terminal: прямое выполнение скриптов

## 📞 Поддержка

При проблемах:

1. Проверьте права доступа: `chmod +x .todos/scripts/*.sh`
2. Убедитесь, что установлены: `bash`, `node`, `jq` (опционально)
3. Проверьте логи: `.todos/system.log`

## 🎉 Готово!

Система полностью настроена и готова к использованию.

**Следующие шаги:**
1. Запустите первое сканирование: `npm run todos:scan`
2. Откройте дашборд: `.todos/dashboard.html`
3. Настройте команду в `.todos/team.json`
4. Настройте автоматическое сканирование (cron/Task Scheduler)

---

*Система будет автоматически обновляться при каждом сканировании*

