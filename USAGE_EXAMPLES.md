# Примеры использования системы генерации плана исправления ошибок

## 🚀 Быстрый старт

### Пример 1: Автоматическая сборка и генерация плана

```powershell
# Просто запустите главный скрипт
.\build-and-fix.ps1
```

**Что произойдет:**
1. Скрипт проверит наличие Go
2. Соберет проект
3. Сохранит ошибки в `build_errors_full.log`
4. Автоматически выберет Python или PowerShell парсер
5. Создаст план в `build_fix_plan.md`
6. Создаст отдельные промпты в `build_fix_prompts/`

**Вывод:**
```
=== Проверка окружения ===
✅ go version go1.21.0 windows/amd64

=== Сборка проекта Go ===
Выполняю сборку проекта...
Найдено ошибок: 11

❌ Обнаружены ошибки компиляции
Ошибки сохранены в build_errors_full.log

=== Поиск Python ===
✅ Найден Python: python (Python 3.11.0)

=== Генерация плана с помощью Python ===
Парсинг ошибок из build_errors_full.log...

План сохранен в build_fix_plan.md
Отдельные промпты сохранены в директории build_fix_prompts/

✅ План успешно создан!
```

---

### Пример 2: Ручная сборка и генерация плана

```powershell
# Шаг 1: Сборка проекта
go build ./... 2>&1 | Tee-Object -FilePath build_errors_full.log

# Шаг 2: Генерация плана (автоматический выбор)
.\generate-build-fix-plan.ps1
```

**Или напрямую PowerShell парсер:**
```powershell
.\parse-go-errors.ps1
```

---

### Пример 3: Использование через AI

1. Откройте файл `BUILD_FIX_PROMPT_COPY.txt`
2. Скопируйте весь текст
3. Вставьте в новый чат с AI
4. AI автоматически выполнит все шаги

---

## 📋 Примеры результатов

### Структура созданных файлов:

```
HttpServer/
├── build_errors_full.log          # Все ошибки компиляции
├── build_fix_plan.md              # План с промптами
└── build_fix_prompts/            # Отдельные промпты
    ├── step_01_client_repository.txt
    ├── step_02_server.txt
    └── step_03_container.txt
```

### Пример содержимого `build_fix_plan.md`:

```markdown
# План исправления ошибок компиляции

Всего файлов с ошибками: 3
Всего ошибок: 15
Всего промптов: 3

**Примечание:** Шаги можно выполнять параллельно, так как каждый шаг исправляет отдельный файл.

---

## Шаг 1: Исправление `internal/infrastructure/persistence/client_repository.go`

**Количество ошибок в файле:** 11

**Промпт:**

```
Исправь все ошибки компиляции в файле `internal/infrastructure/persistence/client_repository.go`.

## Файл для исправления:
`internal/infrastructure/persistence/client_repository.go`

## Ошибки компилятора в этом файле:

Ошибка 1:
  Строка: 43, Колонка: 14
  Сообщение компилятора: cannot use strconv.Itoa(createdClient.ID) (value of type string) as int value in assignment
  Пакет: httpserver/internal/infrastructure/persistence

...
```

---

## 🔧 Примеры использования промптов

### Вариант 1: Параллельное исправление

1. Откройте несколько чатов с AI
2. В каждом чате используйте промпт из `build_fix_prompts/step_XX_*.txt`
3. Каждый AI исправит свой файл параллельно

### Вариант 2: Последовательное исправление

1. Откройте `build_fix_plan.md`
2. Используйте промпты по порядку
3. После каждого исправления проверяйте компиляцию

### Вариант 3: Передача другому разработчику

1. Скопируйте промпт из `build_fix_prompts/`
2. Отправьте коллеге
3. Он может исправить файл независимо

---

## ⚠️ Обработка ошибок

### Если Go не найден:

```
❌ Go не найден! Установите Go и добавьте его в PATH.
```

**Решение:** Установите Go с https://golang.org/dl/

### Если файл с ошибками не найден:

```
❌ Файл build_errors_full.log не найден!
Сначала выполните сборку проекта:
  go build ./... 2>&1 | Tee-Object -FilePath build_errors_full.log
```

**Решение:** Выполните сборку проекта или запустите `.\build-and-fix.ps1`

### Если проект собрался без ошибок:

```
✅ Проект собрался без ошибок!
Никаких действий не требуется.
```

**Действие:** Ничего не требуется, все в порядке!

---

## 🎯 Рекомендации

1. **Используйте главный скрипт** - `.\build-and-fix.ps1` - самый простой способ
2. **Параллельное исправление** - используйте отдельные промпты для ускорения
3. **Проверяйте после исправления** - каждый промпт содержит инструкции по проверке
4. **Сохраняйте промпты** - они могут пригодиться для документации

---

## 📊 Статистика использования

После выполнения скрипта вы увидите:

```
Статистика:
  - Файлов с ошибками: 3
  - Всего ошибок: 15
  - Промптов создано: 3
```

Эта информация помогает понять масштаб работы.

