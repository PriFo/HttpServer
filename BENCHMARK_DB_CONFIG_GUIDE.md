# 📚 Руководство: API ключ в сервисной базе данных

## ✅ Текущая реализация

### Архитектура

```
Фронтенд (Next.js)
    ↓
API Route (/api/models/benchmark)
    ↓
Бэкенд (Go) → WorkerConfigManager
    ↓
Сервисная БД (service.db) → таблица worker_config
```

### Как работает получение API ключа

1. **Бэкенд** (`server/server_models_benchmark.go`):
   ```go
   // Приоритет 1: Конфигурация из БД
   if s.workerConfigManager != nil {
       apiKey, _, err = s.workerConfigManager.GetModelAndAPIKey()
       // Получает из таблицы worker_config в service.db
   }
   
   // Приоритет 2: Переменная окружения (fallback)
   if apiKey == "" {
       apiKey = os.Getenv("ARLIAI_API_KEY")
   }
   ```

2. **WorkerConfigManager** (`server/worker_config.go`):
   - Загружает конфигурацию из БД при старте: `loadConfig()`
   - Сохраняет конфигурацию в БД при обновлении: `saveConfig()`
   - Использует методы `serviceDB.GetWorkerConfig()` и `serviceDB.SaveWorkerConfig()`

3. **База данных** (`database/service_db.go`):
   - Таблица: `worker_config`
   - Поле: `config_json` (JSON с полной конфигурацией, включая API ключи)
   - Методы: `GetWorkerConfig()`, `SaveWorkerConfig()`

## 🗄️ Структура таблицы worker_config

```sql
CREATE TABLE IF NOT EXISTS worker_config (
    id INTEGER PRIMARY KEY CHECK (id = 1), -- Только одна конфигурация
    config_json TEXT NOT NULL, -- JSON с полной конфигурацией
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Пример содержимого config_json:

```json
{
  "providers": {
    "arliai": {
      "name": "arliai",
      "api_key": "sk-ваш-ключ-здесь",
      "base_url": "https://api.arliai.com",
      "enabled": true,
      "priority": 1,
      "max_workers": 2,
      "rate_limit": 2.0,
      "timeout": "30s",
      "models": [
        {
          "name": "GLM-4.5-Air",
          "enabled": true,
          "priority": 1
        }
      ]
    }
  },
  "default_provider": "arliai",
  "default_model": "GLM-4.5-Air",
  "global_max_workers": 2
}
```

## 🔧 Настройка API ключа

### Способ 1: Через веб-интерфейс (рекомендуется)

1. Откройте страницу **"Воркеры"** (`/workers`)
2. Найдите провайдер **"arliai"**
3. Введите API ключ в поле "API Key"
4. Нажмите "Сохранить"
5. Ключ автоматически сохранится в БД

### Способ 2: Через API

```bash
curl -X POST http://localhost:9999/api/workers/config/update \
  -H "Content-Type: application/json" \
  -d '{
    "action": "update_provider",
    "data": {
      "name": "arliai",
      "api_key": "sk-ваш-ключ",
      "enabled": true,
      "priority": 1,
      "max_workers": 2,
      "rate_limit": 2.0,
      "timeout": "30s"
    }
  }'
```

### Способ 3: Прямое обновление в БД (не рекомендуется)

```sql
-- ВНИМАНИЕ: Используйте только для отладки!
UPDATE worker_config 
SET config_json = '{"providers":{"arliai":{"api_key":"sk-ваш-ключ",...}}}',
    updated_at = CURRENT_TIMESTAMP
WHERE id = 1;
```

**⚠️ Не рекомендуется**, так как нужно правильно сформировать весь JSON.

### Способ 4: Переменная окружения (fallback)

Если ключ не найден в БД, используется переменная окружения:

```bash
# Windows PowerShell
$env:ARLIAI_API_KEY="sk-ваш-ключ"

# Linux/Mac
export ARLIAI_API_KEY="sk-ваш-ключ"
```

## 🔍 Проверка конфигурации

### 1. Проверить через API:

```bash
# Получить конфигурацию (API ключ скрыт)
curl http://localhost:9999/api/workers/config | jq '.providers.arliai.has_api_key'

# Должно вернуть: true
```

### 2. Проверить в БД:

```bash
# Используя sqlite3
sqlite3 service.db "SELECT config_json FROM worker_config WHERE id = 1;" | jq '.providers.arliai.api_key'
```

### 3. Проверить на странице бенчмарка:

- Если API ключ не настроен, появится красное предупреждение
- При попытке запустить бенчмарк будет понятное сообщение об ошибке

## 📊 Жизненный цикл конфигурации

### При старте сервера:

1. `NewWorkerConfigManager(serviceDB)` создает менеджер
2. `loadConfig()` загружает конфигурацию из БД
3. Если конфигурации нет в БД, используется дефолтная
4. Логируется: `"Config loaded from service database"` или `"using default config"`

### При обновлении конфигурации:

1. Пользователь обновляет через API или веб-интерфейс
2. `UpdateProvider()` или `UpdateModel()` вызывается
3. `saveConfig()` сохраняет в БД
4. Логируется: `"Config saved to service database"`

### При запуске бенчмарка:

1. `handleModelsBenchmark()` вызывается
2. `workerConfigManager.GetModelAndAPIKey()` получает ключ из БД
3. Если не найден, используется `os.Getenv("ARLIAI_API_KEY")`
4. Если оба способа не сработали, возвращается ошибка

## 🔐 Безопасность

### Текущая реализация:

- ✅ API ключ хранится в БД в JSON формате
- ✅ При получении конфигурации через API ключ скрыт (`has_api_key: true/false`)
- ✅ Ключ передается только внутри сервера

### Рекомендации для продакшена:

- 🔒 Добавить шифрование API ключей в БД
- 🔒 Использовать секреты/менеджеры паролей
- 🔒 Ограничить доступ к БД
- 🔒 Регулярно ротировать API ключи

## 🐛 Диагностика проблем

### Проблема: API ключ не сохраняется

**Проверка:**
1. Проверьте логи сервера: должно быть `"Config saved to service database"`
2. Проверьте доступность БД: `sqlite3 service.db "SELECT 1;"`
3. Проверьте права на запись в БД

### Проблема: API ключ не загружается

**Проверка:**
1. Проверьте логи при старте: должно быть `"Config loaded from service database"`
2. Проверьте наличие записи: `SELECT COUNT(*) FROM worker_config WHERE id = 1;`
3. Проверьте формат JSON: `SELECT config_json FROM worker_config WHERE id = 1;`

### Проблема: Бенчмарк не работает

**Проверка:**
1. Проверьте конфигурацию: `curl http://localhost:9999/api/workers/config`
2. Проверьте логи сервера при запуске бенчмарка
3. Проверьте, что API ключ валидный

## 📝 Примеры использования

### Получить текущую конфигурацию:

```bash
curl http://localhost:9999/api/workers/config
```

### Обновить API ключ:

```bash
curl -X POST http://localhost:9999/api/workers/config/update \
  -H "Content-Type: application/json" \
  -d '{
    "action": "update_provider",
    "data": {
      "name": "arliai",
      "api_key": "sk-новый-ключ",
      "enabled": true
    }
  }'
```

### Проверить наличие ключа:

```bash
curl http://localhost:9999/api/workers/config | jq '.providers.arliai.has_api_key'
```

## ✅ Итог

- ✅ API ключ хранится в сервисной БД (`service.db`, таблица `worker_config`)
- ✅ Конфигурация автоматически загружается при старте сервера
- ✅ Конфигурация автоматически сохраняется при обновлении
- ✅ Бенчмарк использует ключ из БД с fallback на переменную окружения
- ✅ Веб-интерфейс показывает статус наличия ключа
- ✅ Понятные сообщения об ошибках с инструкциями

