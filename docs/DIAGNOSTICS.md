# Система диагностики цепочки данных

## Описание

Система диагностики позволяет проверить все этапы цепочки извлечения данных: от исходных баз данных до отображения на фронтенде. Это помогает быстро находить и исправлять проблемы, когда записи номенклатуры и контрагентов не отображаются.

## Доступ

Диагностика доступна по адресу:
```
/clients/{clientId}/projects/{projectId}/diagnostics
```

Также есть кнопка "Диагностика" на странице проекта.

## Этапы диагностики

### 1. Базы данных
Проверяет:
- Существование файлов баз данных
- Наличие ключевых таблиц (catalog_items, nomenclature_items)
- Наличие исходных таблиц 1С
- Количество записей в таблицах

**Проблемы:**
- Файл базы данных не существует
- Отсутствуют таблицы catalog_items, nomenclature_items или исходные таблицы 1С
- Таблицы пусты

### 2. Upload записи
Проверяет:
- Наличие upload записей для каждой базы данных
- Валидность client_id и project_id
- Количество записей в upload

**Проблемы:**
- Upload запись отсутствует
- Несоответствие client_id или project_id

**Автоматическое исправление:**
- Создание недостающих upload записей

### 3. Извлечение данных
Проверяет:
- Наличие данных в catalog_items
- Наличие данных в nomenclature_items
- Метод извлечения (auto/manual/none)

**Проблемы:**
- Данные не извлечены из исходных таблиц 1С
- Таблицы catalog_items или nomenclature_items пусты

**Рекомендации:**
- Использовать обработку 1С для извлечения данных
- Загрузить данные через API upload

### 4. Нормализация
Проверяет:
- Количество нормализованных записей
- Сессии нормализации
- Связи с базами данных проекта

**Проблемы:**
- Нет нормализованных записей
- Нет сессий нормализации

## Функции

### Запуск всех проверок
Кнопка "Запустить все проверки" выполняет диагностику всех этапов последовательно и обновляет сводку.

### Исправление проблем
Кнопка "Исправить проблемы" автоматически создает недостающие upload записи.

### Экспорт отчета
Кнопка "Экспорт отчета" сохраняет полный отчет диагностики в JSON формате, включая:
- Сводку по всем этапам
- Детальные данные по каждой базе данных
- Статусы upload записей
- Статусы извлечения данных
- Статусы нормализации

## API Endpoints

### GET /api/clients/:clientId/projects/:projectId/diagnostics/databases
Возвращает диагностику всех баз данных проекта.

### GET /api/clients/:clientId/projects/:projectId/diagnostics/uploads
Возвращает статус upload записей для всех баз данных.

### POST /api/clients/:clientId/projects/:projectId/diagnostics/uploads/fix
Создает недостающие upload записи.

### GET /api/clients/:clientId/projects/:projectId/diagnostics/extraction
Возвращает статус извлечения данных для всех баз данных.

### GET /api/clients/:clientId/projects/:projectId/diagnostics/normalization
Возвращает статус нормализации для проекта.

## Типичные проблемы и решения

### Проблема: Нет upload записей
**Решение:** Используйте кнопку "Исправить проблемы" для автоматического создания upload записей.

### Проблема: Данные не извлечены
**Решение:** 
1. Убедитесь, что база данных содержит исходные таблицы 1С
2. Используйте обработку 1С для извлечения данных в catalog_items и nomenclature_items
3. Или загрузите данные через API upload

### Проблема: Нет нормализованных данных
**Решение:**
1. Убедитесь, что данные извлечены в catalog_items или nomenclature_items
2. Запустите нормализацию через страницу проекта

### Проблема: Файл базы данных не существует
**Решение:**
1. Проверьте путь к файлу базы данных
2. Убедитесь, что файл доступен и не был перемещен
3. При необходимости обновите путь к файлу в настройках базы данных

## Визуализация

Система использует цветовую индикацию:
- 🟢 Зеленый - все в порядке
- 🟡 Желтый - предупреждение
- 🔴 Красный - проблема

Сводка показывает общий статус всех компонентов и количество проблем в каждом этапе.

