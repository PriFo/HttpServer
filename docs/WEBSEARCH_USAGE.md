# Руководство по использованию веб-поиска для валидации

## Обзор

Модуль веб-поиска (`websearch`) позволяет валидировать данные номенклатуры через поисковые системы. Система поддерживает несколько провайдеров с автоматическим fallback при ошибках.

## Поддерживаемые провайдеры

### 1. DuckDuckGo (по умолчанию)
- ✅ **Бесплатный**, не требует API ключа
- ✅ **Готов к использованию** из коробки
- ✅ Поддерживает Instant Answer API и HTML-поиск
- ⚠️ Может не находить специфические технические документы

### 2. Bing Search API
- ⚠️ Требует API ключа (Microsoft Azure)
- ✅ Высокое качество результатов
- 📝 Настройка: `BING_API_KEY` в переменных окружения

### 3. Google Custom Search
- ⚠️ Требует API ключа и Search Engine ID
- ✅ Отличное качество результатов
- 📝 Настройка: `GOOGLE_API_KEY` и `GOOGLE_SEARCH_ENGINE_ID`

### 4. Yandex XML Search
- ⚠️ Требует платной подписки и API ключа
- ⚠️ **Не реализован полностью** (требует XML-парсинг)
- 📝 Настройка: `YANDEX_API_KEY` и `YANDEX_USER`

## Конфигурация

### Переменные окружения

```bash
# Включение веб-поиска
WEB_SEARCH_ENABLED=true

# Таймаут запросов (по умолчанию 5 секунд)
WEB_SEARCH_TIMEOUT=5s

# Кэширование результатов (по умолчанию 24 часа)
WEB_SEARCH_CACHE_TTL=24h
WEB_SEARCH_CACHE_ENABLED=true

# Лимит запросов в секунду (по умолчанию 1)
WEB_SEARCH_RATE_LIMIT_PER_SEC=1

# Базовый URL (по умолчанию DuckDuckGo)
WEB_SEARCH_BASE_URL=https://api.duckduckgo.com

# API ключи для провайдеров (опционально)
BING_API_KEY=your_bing_key
GOOGLE_API_KEY=your_google_key
GOOGLE_SEARCH_ENGINE_ID=your_engine_id
YANDEX_API_KEY=your_yandex_key
YANDEX_USER=your_yandex_user
```

### Конфигурация правил валидации

Правила валидации настраиваются в базе данных в таблице `normalization_config`:

```sql
UPDATE normalization_config 
SET websearch_rules = '{
  "product_name": {
    "validator": "existence",
    "enabled": true
  },
  "product_code": {
    "validator": "accuracy",
    "enabled": true
  }
}'
WHERE id = 1;
```

## API Endpoints

### 1. Валидация товара

**POST** `/api/validation/websearch`

```json
{
  "name": "Название товара",
  "code": "Код товара (опционально)"
}
```

**Ответ:**
```json
{
  "success": true,
  "validation": {
    "existence": {
      "status": "success",
      "found": true,
      "score": 0.85,
      "message": "Найдено 8 релевантных результатов из 10"
    },
    "accuracy": {
      "status": "success",
      "score": 0.92,
      "message": "Высокая точность данных"
    }
  }
}
```

### 2. Простой поиск

**GET** `/api/validation/websearch/search?query=название товара`

**Ответ:**
```json
{
  "success": true,
  "result": {
    "query": "название товара",
    "found": true,
    "results": [
      {
        "title": "Название товара - Официальный сайт",
        "url": "https://example.com/product",
        "snippet": "Описание товара...",
        "relevance": 0.95
      }
    ],
    "confidence": 0.90,
    "source": "duckduckgo"
  }
}
```

### 3. Управление провайдерами (админ)

**GET** `/api/admin/websearch/providers` - список провайдеров

**POST** `/api/admin/websearch/providers` - добавить провайдер

**PUT** `/api/admin/websearch/providers/{name}` - обновить провайдер

**DELETE** `/api/admin/websearch/providers/{name}` - удалить провайдер

**GET** `/api/admin/websearch/stats` - статистика использования

## Интеграция с валидацией

Веб-поиск автоматически интегрируется с `ValidationEngine` при запуске сервера, если:

1. `WEB_SEARCH_ENABLED=true`
2. Правила валидации включены в БД

### Правила валидации

1. **web_search_product_exists** (Severity: Medium)
   - Проверяет существование товара в интернете
   - Не блокирует обработку, только предупреждение

2. **web_search_data_accuracy** (Severity: Low)
   - Проверяет точность данных через сравнение с найденной информацией
   - Только предупреждение

## Примеры использования

### Пример 1: Валидация через API

```bash
curl -X POST http://localhost:9999/api/validation/websearch \
  -H "Content-Type: application/json" \
  -d '{
    "name": "ГОСТ 1.1-2020",
    "code": "1.1"
  }'
```

### Пример 2: Простой поиск

```bash
curl "http://localhost:9999/api/validation/websearch/search?query=ГОСТ%201.1-2020"
```

### Пример 3: Проверка статистики

```bash
curl http://localhost:9999/api/admin/websearch/stats
```

## Кэширование

Все результаты поиска кэшируются для:
- Ускорения повторных запросов
- Снижения нагрузки на API провайдеров
- Экономии API квот

TTL кэша: 24 часа (настраивается через `WEB_SEARCH_CACHE_TTL`)

## Обработка ошибок

- При ошибке одного провайдера система автоматически переключается на другой (fallback)
- Ошибки веб-поиска не блокируют основную валидацию
- Все ошибки логируются для отладки

## Рекомендации

1. **Для разработки**: Используйте DuckDuckGo (не требует настройки)
2. **Для production**: Настройте Bing или Google для лучших результатов
3. **Для специфических запросов**: Используйте HTML-поиск (автоматически включается в DuckDuckGo)

## Ограничения

- Rate limiting: 1 запрос/секунду по умолчанию
- Таймаут: 5 секунд на запрос
- Yandex провайдер не реализован полностью (требует XML-парсинг)

## Troubleshooting

### Провайдер не работает

1. Проверьте API ключи в переменных окружения
2. Проверьте логи сервера на наличие ошибок
3. Используйте `/api/admin/websearch/stats` для диагностики

### Низкое качество результатов

1. Попробуйте другой провайдер (Bing или Google)
2. Улучшите поисковые запросы (добавьте больше контекста)
3. Проверьте настройки кэша

### Медленные запросы

1. Увеличьте `WEB_SEARCH_TIMEOUT`
2. Проверьте скорость интернет-соединения
3. Используйте кэширование (включено по умолчанию)

