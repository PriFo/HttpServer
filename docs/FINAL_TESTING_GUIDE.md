# Руководство по финальному тестированию

**Версия:** 1.0  
**Дата:** 2025-11-23

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [Нагрузочное тестирование](#нагрузочное-тестирование)
3. [Проверка безопасности](#проверка-безопасности)
4. [Проверка производительности](#проверка-производительности)
5. [Чек-лист готовности к production](#чек-лист-готовности-к-production)

---

## Обзор

Финальное тестирование включает три основных направления:
- **Нагрузочное тестирование** - проверка работы под нагрузкой
- **Проверка безопасности** - выявление уязвимостей
- **Проверка производительности** - оптимизация и метрики

---

## Нагрузочное тестирование

### Использование скрипта

```bash
# Базовый запуск
./scripts/load_test.sh

# С параметрами
BASE_URL=http://localhost:9999 \
CONCURRENT_REQUESTS=20 \
TOTAL_REQUESTS=500 \
./scripts/load_test.sh
```

### Параметры

- `BASE_URL` - базовый URL сервера (по умолчанию: `http://localhost:9999`)
- `CONCURRENT_REQUESTS` - количество конкурентных запросов (по умолчанию: 10)
- `TOTAL_REQUESTS` - общее количество запросов на endpoint (по умолчанию: 100)
- `TEST_DURATION` - длительность теста в секундах (по умолчанию: 60)

### Тестируемые endpoints

1. `/health` - Health check
2. `/api/system/summary` - Системная сводка
3. `/api/monitoring/metrics` - Метрики мониторинга
4. `/api/monitoring/performance` - Метрики производительности
5. `/api/databases/list` - Список баз данных

### Интерпретация результатов

**Успешность:**
- Success rate > 99% - отлично
- Success rate 95-99% - хорошо
- Success rate < 95% - требуется оптимизация

**Время ответа:**
- Среднее время < 100ms - отлично
- Среднее время 100-500ms - хорошо
- Среднее время > 500ms - требуется оптимизация

**Пропускная способность:**
- > 100 RPS - отлично
- 50-100 RPS - хорошо
- < 50 RPS - требуется оптимизация

### Использование специализированных инструментов

#### Apache Bench (ab)

```bash
# Установка (Ubuntu/Debian)
sudo apt-get install apache2-utils

# Тестирование
ab -n 1000 -c 10 http://localhost:9999/health
```

#### wrk

```bash
# Установка
# Ubuntu/Debian: sudo apt-get install wrk
# macOS: brew install wrk

# Тестирование
wrk -t4 -c100 -d30s http://localhost:9999/health
```

#### k6

```bash
# Установка
# https://k6.io/docs/getting-started/installation/

# Создайте скрипт test.js
import http from 'k6/http';
import { check } from 'k6';

export let options = {
  stages: [
    { duration: '30s', target: 20 },
    { duration: '1m', target: 50 },
    { duration: '30s', target: 0 },
  ],
};

export default function () {
  let res = http.get('http://localhost:9999/health');
  check(res, {
    'status is 200': (r) => r.status === 200,
    'response time < 200ms': (r) => r.timings.duration < 200,
  });
}

# Запуск
k6 run test.js
```

---

## Проверка безопасности

### Использование скрипта

```bash
# Базовый запуск
./scripts/security_check.sh

# С параметрами
BASE_URL=http://localhost:9999 ./scripts/security_check.sh
```

### Проверяемые аспекты

1. **HTTPS** - использование защищенного соединения
2. **CORS** - правильная настройка CORS заголовков
3. **SQL Injection** - защита от SQL инъекций
4. **XSS** - защита от межсайтового скриптинга
5. **Security Headers** - наличие заголовков безопасности
6. **Аутентификация** - защита endpoints
7. **Rate Limiting** - ограничение частоты запросов
8. **Server Information** - скрытие информации о сервере

### Рекомендации по исправлению

#### CORS

```go
// Правильная настройка CORS
corsConfig := cors.Config{
    AllowOrigins:     []string{"https://yourdomain.com"},
    AllowMethods:     []string{"GET", "POST", "PUT", "DELETE"},
    AllowHeaders:     []string{"Content-Type", "Authorization"},
    ExposeHeaders:    []string{"Content-Length"},
    AllowCredentials: true,
    MaxAge:           12 * time.Hour,
}
```

#### Security Headers

```go
// Добавьте middleware для security headers
func SecurityHeadersMiddleware() gin.HandlerFunc {
    return func(c *gin.Context) {
        c.Header("X-Content-Type-Options", "nosniff")
        c.Header("X-Frame-Options", "DENY")
        c.Header("X-XSS-Protection", "1; mode=block")
        c.Header("Strict-Transport-Security", "max-age=31536000")
        c.Next()
    }
}
```

#### Rate Limiting

```go
// Используйте rate limiter
import "golang.org/x/time/rate"

limiter := rate.NewLimiter(rate.Every(time.Second), 10) // 10 запросов в секунду

func RateLimitMiddleware() gin.HandlerFunc {
    return func(c *gin.Context) {
        if !limiter.Allow() {
            c.JSON(429, gin.H{"error": "Too many requests"})
            c.Abort()
            return
        }
        c.Next()
    }
}
```

---

## Проверка производительности

### Метрики для мониторинга

1. **Время ответа API**
   - Среднее время ответа
   - P95, P99 перцентили
   - Максимальное время ответа

2. **Пропускная способность**
   - Запросов в секунду (RPS)
   - Одновременных подключений

3. **Использование ресурсов**
   - CPU usage
   - Memory usage
   - Database connections

4. **Ошибки**
   - Error rate
   - Типы ошибок
   - Время восстановления

### Получение метрик

#### Через API

```bash
# Метрики производительности
curl http://localhost:9999/api/monitoring/performance

# Метрики мониторинга
curl http://localhost:9999/api/monitoring/metrics

# Health check
curl http://localhost:9999/health
```

#### Через логи

```bash
# Просмотр логов сервера
docker-compose logs -f backend

# Фильтрация по уровню
docker-compose logs backend | grep ERROR
```

### Benchmark тесты

```bash
# Запуск benchmark тестов
go test -bench=. -benchmem ./server/...

# С профилированием CPU
go test -bench=. -cpuprofile=cpu.prof ./server/...

# С профилированием памяти
go test -bench=. -memprofile=mem.prof ./server/...

# Анализ профиля
go tool pprof cpu.prof
```

---

## Чек-лист готовности к production

### Функциональность

- [ ] Все основные функции работают корректно
- [ ] Обработка ошибок работает правильно
- [ ] Валидация входных данных реализована
- [ ] Логирование настроено и работает

### Производительность

- [ ] Среднее время ответа < 200ms для критических endpoints
- [ ] P95 время ответа < 500ms
- [ ] Система выдерживает ожидаемую нагрузку
- [ ] Нет утечек памяти
- [ ] Database connection pooling настроен правильно

### Безопасность

- [ ] HTTPS настроен (для production)
- [ ] CORS настроен правильно
- [ ] SQL injection защита реализована
- [ ] XSS защита реализована
- [ ] Security headers настроены
- [ ] Аутентификация работает (если требуется)
- [ ] Rate limiting настроен
- [ ] Чувствительные данные не логируются

### Мониторинг

- [ ] Health checks работают
- [ ] Метрики собираются
- [ ] Логи структурированы
- [ ] Алерты настроены
- [ ] Дашборды созданы

### Резервное копирование

- [ ] Автоматическое резервное копирование настроено
- [ ] Тестирование восстановления проведено
- [ ] План восстановления документирован

### Документация

- [ ] API документация актуальна
- [ ] Руководство по развертыванию создано
- [ ] Руководство по мониторингу создано
- [ ] Troubleshooting guide создан

### CI/CD

- [ ] CI/CD pipeline настроен
- [ ] Автоматические тесты проходят
- [ ] Автоматическое развертывание работает
- [ ] Rollback процедура протестирована

---

## Примеры команд

### Полный цикл тестирования

```bash
# 1. Запуск сервера
docker-compose up -d

# 2. Ожидание готовности
sleep 10

# 3. Проверка health
curl http://localhost:9999/health

# 4. Нагрузочное тестирование
./scripts/load_test.sh

# 5. Проверка безопасности
./scripts/security_check.sh

# 6. Проверка производительности
curl http://localhost:9999/api/monitoring/performance

# 7. Просмотр логов
docker-compose logs -f backend
```

### Автоматизация

```bash
#!/bin/bash
# run_all_tests.sh

set -e

echo "=== Запуск всех тестов ==="

# Нагрузочное тестирование
echo "1. Нагрузочное тестирование..."
./scripts/load_test.sh

# Проверка безопасности
echo "2. Проверка безопасности..."
./scripts/security_check.sh

# Проверка производительности
echo "3. Проверка производительности..."
curl -s http://localhost:9999/api/monitoring/performance | jq .

echo "=== Все тесты завершены ==="
```

---

## Troubleshooting

### Проблемы с нагрузочным тестированием

**Высокий процент ошибок:**
- Проверьте логи сервера
- Увеличьте лимиты ресурсов
- Проверьте database connection pool

**Медленное время ответа:**
- Проверьте database queries
- Используйте профилирование
- Проверьте network latency

### Проблемы с безопасностью

**SQL Injection обнаружена:**
- Проверьте все места с динамическими SQL запросами
- Используйте prepared statements
- Валидируйте входные данные

**XSS обнаружена:**
- Экранируйте пользовательский ввод
- Используйте Content Security Policy
- Валидируйте и санитизируйте данные

---

*Последнее обновление: 2025-11-23*

