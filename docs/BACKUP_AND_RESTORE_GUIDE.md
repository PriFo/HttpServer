# Руководство по резервному копированию и восстановлению

**Версия:** 1.0  
**Дата:** 2025-11-23

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [Автоматическое резервное копирование](#автоматическое-резервное-копирование)
3. [Ручное резервное копирование](#ручное-резервное-копирование)
4. [Восстановление](#восстановление)
5. [План восстановления (Disaster Recovery)](#план-восстановления-disaster-recovery)
6. [Мониторинг резервных копий](#мониторинг-резервных-копий)

---

## Обзор

Система резервного копирования поддерживает:
- **Автоматическое резервное копирование** через скрипты
- **Ручное резервное копирование** через API
- **Восстановление** из резервных копий
- **Ротацию** старых резервных копий

### Что копируется

- Основная база данных (`data.db`)
- Базы данных загрузок (`data/uploads/*.db`)
- Сервисная база данных (`service.db`) - опционально

---

## Автоматическое резервное копирование

### Linux/macOS

#### Настройка cron

```bash
# Редактируем crontab
crontab -e

# Добавляем задачу для ежедневного бэкапа в 2:00 ночи
0 2 * * * /path/to/scripts/backup.sh >> /path/to/logs/backup_cron.log 2>&1

# Или еженедельный бэкап по воскресеньям в 3:00
0 3 * * 0 /path/to/scripts/backup.sh >> /path/to/logs/backup_cron.log 2>&1
```

#### Запуск вручную

```bash
# Базовый запуск
./scripts/backup.sh

# С параметрами
BASE_URL=http://localhost:9999 \
BACKUP_DIR=./backups \
RETENTION_DAYS=60 \
./scripts/backup.sh
```

### Windows

#### Настройка Task Scheduler

1. Откройте **Task Scheduler** (Планировщик заданий)
2. Создайте новую задачу
3. Настройте триггер (например, ежедневно в 2:00)
4. Добавьте действие: запуск PowerShell скрипта
5. Укажите путь: `powershell.exe -File "E:\HttpServer\scripts\backup.ps1"`

#### Запуск вручную

```powershell
# Базовый запуск
.\scripts\backup.ps1

# С параметрами
.\scripts\backup.ps1 -BaseUrl "http://localhost:9999" -RetentionDays 60
```

### Параметры

- `BASE_URL` / `-BaseUrl` - URL сервера (по умолчанию: `http://localhost:9999`)
- `BACKUP_DIR` / `-BackupDir` - Директория для резервных копий (по умолчанию: `./data/backups`)
- `RETENTION_DAYS` / `-RetentionDays` - Количество дней хранения (по умолчанию: 30)
- `LOG_FILE` / `-LogFile` - Файл логов (по умолчанию: `./logs/backup.log`)

---

## Ручное резервное копирование

### Через API

```bash
# Создание резервной копии
curl -X POST http://localhost:9999/api/databases/backup \
  -H "Content-Type: application/json" \
  -d '{
    "include_main": true,
    "include_uploads": true,
    "include_service": false,
    "format": "both"
  }'
```

### Через CLI

```bash
# Использование db-manager
./db-manager backup --output ./backups/manual_backup.zip
```

### Форматы резервных копий

- `zip` - только ZIP архив
- `copy` - только копии файлов
- `both` - и ZIP, и копии файлов (по умолчанию)

---

## Восстановление

### Подготовка

⚠️ **ВАЖНО:** Перед восстановлением:
1. Остановите сервер
2. Создайте резервную копию текущего состояния
3. Убедитесь, что у вас есть доступ к резервной копии

### Linux/macOS

```bash
# Восстановление из последнего бэкапа
./scripts/restore.sh

# Восстановление из указанного файла
./scripts/restore.sh ./data/backups/backup_20231123_020000.zip
```

### Windows

```powershell
# Восстановление из последнего бэкапа
.\scripts\restore.ps1

# Восстановление из указанного файла
.\scripts\restore.ps1 -BackupFile ".\data\backups\backup_20231123_020000.zip"
```

### Шаги восстановления

1. **Остановите сервер**
   ```bash
   docker-compose down
   # или
   systemctl stop httpserver
   ```

2. **Запустите скрипт восстановления**
   ```bash
   ./scripts/restore.sh [путь_к_бэкапу]
   ```

3. **Проверьте восстановленные файлы**
   ```bash
   ls -lh ./data/restored/
   ```

4. **Замените файлы** (если необходимо)
   ```bash
   # Создайте резервную копию текущих файлов
   cp -r ./data ./data.backup.$(date +%Y%m%d)
   
   # Замените файлы
   cp -r ./data/restored/* ./data/
   ```

5. **Запустите сервер**
   ```bash
   docker-compose up -d
   # или
   systemctl start httpserver
   ```

6. **Проверьте работоспособность**
   ```bash
   curl http://localhost:9999/health
   ```

---

## План восстановления (Disaster Recovery)

### Сценарии восстановления

#### 1. Потеря данных базы данных

**Симптомы:**
- Ошибки при чтении из БД
- Поврежденные файлы БД
- Отсутствие файлов БД

**Действия:**
1. Остановите сервер
2. Определите, какие БД повреждены
3. Восстановите из резервной копии
4. Проверьте целостность восстановленных БД
5. Запустите сервер
6. Проверьте работоспособность

**Время восстановления:** 15-30 минут

#### 2. Полная потеря сервера

**Симптомы:**
- Сервер недоступен
- Потеря всех данных

**Действия:**
1. Разверните новую инфраструктуру
2. Восстановите конфигурацию
3. Восстановите данные из резервной копии
4. Настройте мониторинг
5. Проверьте работоспособность

**Время восстановления:** 1-4 часа

#### 3. Частичная потеря данных

**Симптомы:**
- Отсутствие некоторых записей
- Некорректные данные

**Действия:**
1. Определите период потери данных
2. Найдите соответствующую резервную копию
3. Восстановите только необходимые данные
4. Проверьте целостность данных

**Время восстановления:** 30-60 минут

### RTO и RPO

- **RTO (Recovery Time Objective):** 1-4 часа
- **RPO (Recovery Point Objective):** 24 часа (для ежедневных бэкапов)

### Контакты для экстренных ситуаций

- **On-call инженер:** [указать контакты]
- **DevOps команда:** [указать контакты]
- **Менеджер проекта:** [указать контакты]

---

## Мониторинг резервных копий

### Проверка наличия резервных копий

```bash
# Список резервных копий через API
curl http://localhost:9999/api/databases/backups

# Проверка последнего бэкапа
ls -lh ./data/backups/backup_*.zip | head -n 1
```

### Автоматическая проверка

Создайте скрипт для мониторинга:

```bash
#!/bin/bash
# check_backups.sh

BACKUP_DIR="./data/backups"
ALERT_DAYS=2  # Предупреждение, если последний бэкап старше 2 дней

last_backup=$(find "$BACKUP_DIR" -name "backup_*.zip" -type f -printf '%T@ %p\n' | sort -n | tail -1 | cut -d' ' -f2-)

if [ -z "$last_backup" ]; then
    echo "ОШИБКА: Резервные копии не найдены!"
    exit 1
fi

backup_age=$(($(date +%s) - $(stat -c %Y "$last_backup")))
backup_age_days=$((backup_age / 86400))

if [ $backup_age_days -gt $ALERT_DAYS ]; then
    echo "ПРЕДУПРЕЖДЕНИЕ: Последний бэкап старше $backup_age_days дней"
    exit 1
fi

echo "OK: Последний бэкап создан $backup_age_days дней назад"
exit 0
```

### Интеграция с мониторингом

Добавьте проверку в health check:

```go
// Проверка наличия свежих резервных копий
func (hc *HealthChecker) CheckBackups() ComponentHealth {
    backupDir := "data/backups"
    lastBackup := findLatestBackup(backupDir)
    
    if lastBackup == nil {
        return ComponentHealth{
            Name:   "backups",
            Status: HealthStatusUnhealthy,
            Message: "Резервные копии не найдены",
        }
    }
    
    age := time.Since(lastBackup.ModTime())
    if age > 48*time.Hour {
        return ComponentHealth{
            Name:   "backups",
            Status: HealthStatusDegraded,
            Message: fmt.Sprintf("Последний бэкап старше %v", age),
        }
    }
    
    return ComponentHealth{
        Name:   "backups",
        Status: HealthStatusHealthy,
        Message: "Резервные копии актуальны",
    }
}
```

---

## Best Practices

### Резервное копирование

1. **Регулярность:** Минимум ежедневно, лучше чаще
2. **Хранение:** Храните бэкапы в отдельном месте
3. **Проверка:** Регулярно проверяйте целостность бэкапов
4. **Тестирование:** Периодически тестируйте восстановление
5. **Шифрование:** Шифруйте бэкапы с чувствительными данными

### Восстановление

1. **Тестирование:** Регулярно тестируйте процедуру восстановления
2. **Документация:** Держите документацию актуальной
3. **Автоматизация:** Автоматизируйте процесс восстановления где возможно
4. **Мониторинг:** Мониторьте процесс восстановления

---

## Troubleshooting

### Проблемы с созданием резервных копий

**Ошибка: "Сервер недоступен"**
- Проверьте, что сервер запущен
- Проверьте URL в конфигурации
- Проверьте сетевые настройки

**Ошибка: "Не удалось создать директорию"**
- Проверьте права доступа
- Проверьте наличие места на диске

**Ошибка: "ZIP архив поврежден"**
- Проверьте целостность файла
- Попробуйте создать новый бэкап

### Проблемы с восстановлением

**Ошибка: "Файл не найден"**
- Проверьте путь к резервной копии
- Убедитесь, что файл существует

**Ошибка: "ZIP архив поврежден"**
- Попробуйте использовать другую резервную копию
- Проверьте целостность файла

---

*Последнее обновление: 2025-11-23*

