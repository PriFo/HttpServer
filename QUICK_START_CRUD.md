# Быстрый старт: Генерация интеграционных тестов CRUD API

Пошаговая инструкция для генерации интеграционных тестов, проверяющих полную цепочку вызовов CRUD-операций.

## ⏱️ Время выполнения: 5 минут

## 📋 Шаг 1: Подготовка (1 минута)

### 1.1. Определите сущность для тестирования

Выберите сущность, для которой нужно создать тесты. Например:
- `Client` (клиент)
- `Project` (проект)
- `Database` (база данных)
- Или любая другая сущность с CRUD операциями

### 1.2. Подготовьте значения для замены

Создайте таблицу соответствий:

| Placeholder | Пример для Client | Ваше значение |
|------------|-------------------|---------------|
| `{entity}` | `client` | ? |
| `{Entity}` | `Client` | ? |
| `{table}` | `clients` | ? |
| `{entities}` | `clients` | ? |

**Пример для Client:**
- `{entity}` → `client`
- `{Entity}` → `Client`
- `{table}` → `clients`
- `{entities}` → `clients`

**Пример для Project:**
- `{entity}` → `project`
- `{Entity}` → `Project`
- `{table}` → `projects`
- `{entities}` → `projects`

## 📋 Шаг 2: Откройте промпт (30 секунд)

Откройте файл **[CRUD_CHAIN_TEST_PROMPT_COPY.txt](CRUD_CHAIN_TEST_PROMPT_COPY.txt]**

## 📋 Шаг 3: Замените placeholders (2 минуты)

### 3.1. Найдите все вхождения placeholders

В файле найдите и замените:
- `{entity}` → ваше значение (например, `client`)
- `{Entity}` → ваше значение с заглавной буквы (например, `Client`)
- `{table}` → имя таблицы в БД (например, `clients`)
- `{entities}` → множественное число для URL (например, `clients`)

### 3.2. Используйте поиск и замену

**В VS Code / Cursor:**
1. Нажмите `Ctrl+H` (или `Cmd+H` на Mac)
2. Введите `{entity}` в поле поиска
3. Введите ваше значение в поле замены
4. Нажмите "Заменить все"
5. Повторите для всех placeholders

**Или вручную:**
- Используйте `Ctrl+F` для поиска каждого placeholder
- Заменяйте по одному

### 3.3. Проверьте замену

Убедитесь, что:
- ✅ Все `{entity}` заменены
- ✅ Все `{Entity}` заменены
- ✅ Все `{table}` заменены
- ✅ Все `{entities}` заменены
- ✅ Нет оставшихся фигурных скобок `{}`

## 📋 Шаг 4: Скопируйте промпт (30 секунд)

1. Выделите весь текст (`Ctrl+A`)
2. Скопируйте (`Ctrl+C`)
3. Откройте новый чат с AI (например, в Cursor, ChatGPT, Claude)
4. Вставьте промпт (`Ctrl+V`)

## 📋 Шаг 5: Дождитесь генерации (1 минута)

AI автоматически:
1. Проанализирует промпт
2. Сгенерирует полный файл интеграционных тестов
3. Создаст код для всех CRUD операций
4. Добавит тесты для негативных сценариев
5. Включит прямые проверки БД

## 📋 Шаг 6: Сохраните результат

1. Скопируйте сгенерированный код
2. Создайте файл `{entity}_service_integration_test.go` в нужной директории
3. Вставьте код в файл
4. Сохраните

## 📋 Шаг 7: Запустите тесты

```bash
# Запустите все тесты в файле
go test -v ./path/to/{entity}_service_integration_test.go

# Или запустите конкретный тест
go test -v -run Test{Entity}IntegrationSuite ./path/to/
```

## ✅ Что вы получите

После выполнения всех шагов у вас будет:

- ✅ Полный файл интеграционных тестов
- ✅ Тесты для всех CRUD операций (Create, Read, ReadAll, Update, Delete)
- ✅ Тесты для негативных сценариев
- ✅ Проверка цепочки вызовов на всех уровнях (HTTP → UseCase → Service → Repository → DB)
- ✅ Прямые проверки состояния базы данных после каждой операции
- ✅ Изоляция тестов через транзакции

## 🎯 Пример полного процесса

### Для сущности Client:

1. **Подготовка:**
   - `{entity}` → `client`
   - `{Entity}` → `Client`
   - `{table}` → `clients`
   - `{entities}` → `clients`

2. **Открыть:** `CRUD_CHAIN_TEST_PROMPT_COPY.txt`

3. **Заменить все placeholders**

4. **Скопировать и вставить в AI**

5. **Результат:** `client_service_integration_test.go` с тестами:
   - `TestClient_Create_Success`
   - `TestClient_GetByID_Success`
   - `TestClient_GetAll_Success`
   - `TestClient_Update_Success`
   - `TestClient_Delete_Success`
   - `TestClient_Create_InvalidData`
   - `TestClient_GetByID_NotFound`
   - И другие...

## 🐛 Решение проблем

### Проблема: AI не понимает промпт

**Решение:**
- Убедитесь, что все placeholders заменены
- Проверьте, что текст промпта полный (от "---" до "---")
- Попробуйте использовать полную версию из `CRUD_CHAIN_TEST_PROMPT.md`

### Проблема: Тесты не компилируются

**Решение:**
- Проверьте импорты - они должны соответствовать структуре проекта
- Убедитесь, что пути к пакетам корректны
- Проверьте, что все зависимости установлены

### Проблема: Тесты падают

**Решение:**
- Проверьте настройку тестовой БД
- Убедитесь, что миграции выполнены
- Проверьте, что транзакции настроены корректно

## 📚 Дополнительная помощь

- **[CRUD_CHAIN_TEST_README.md](CRUD_CHAIN_TEST_README.md)** - общая информация
- **[CRUD_CHAIN_TEST_PROMPT.md](CRUD_CHAIN_TEST_PROMPT.md)** - полная документация
- **[CRUD_CHAIN_TEST_EXAMPLES.md](CRUD_CHAIN_TEST_EXAMPLES.md)** - примеры использования

---

**🎉 Готово!** Теперь у вас есть полный набор интеграционных тестов для вашей CRUD API!

