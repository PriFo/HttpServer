/**
 * Константы для модуля качества данных
 */

// Таймауты для различных типов запросов (в миллисекундах)
export const QUALITY_TIMEOUTS = {
  // Быстрые запросы (статистика, статус)
  FAST: 7000,
  // Стандартные запросы (списки, отчеты)
  STANDARD: 10000,
  // Долгие операции (запуск анализа, слияние)
  LONG: 15000,
  // Очень долгие операции (запуск анализа качества, агрегация проектов)
  VERY_LONG: 60000, // 60 секунд для проектов с большим количеством БД
} as const

// Количество повторных попыток для сетевых ошибок
export const QUALITY_RETRY_CONFIG = {
  // Максимальное количество попыток
  MAX_RETRIES: 1,
  // Задержка между попытками (в миллисекундах)
  RETRY_DELAY: 1000,
} as const

// Сообщения об ошибках
export const QUALITY_ERROR_MESSAGES = {
  TIMEOUT: 'Превышено время ожидания ответа от сервера',
  NETWORK_ERROR: 'Не удалось подключиться к серверу после нескольких попыток',
  SERVER_UNAVAILABLE: 'Не удалось подключиться к серверу. Убедитесь, что backend сервер запущен.',
  NOT_FOUND: 'Данные не найдены или еще не обработаны.',
  SERVER_ERROR: 'Ошибка сервера. Попробуйте позже.',
  UNKNOWN_ERROR: 'Произошла неизвестная ошибка',
  CONNECTION_ERROR: 'Ошибка подключения к серверу',
} as const

// Причины отмены запросов
export const ABORT_REASONS = {
  TIMEOUT: 'timeout',
  USER_ACTION: 'user_action',
  COMPONENT_UNMOUNT: 'component_unmount',
  RETRY_LIMIT: 'retry_limit',
} as const

