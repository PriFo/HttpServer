# 🚀 Быстрое исправление: React DevTools в Production режиме

## Проблема
Вы видите сообщение:
> "This page is using the production build of React"

## Решение

### 1. Остановите текущий сервер
Нажмите `Ctrl+C` в терминале, где запущен сервер.

### 2. Запустите в development режиме

**Вариант A: Только фронтенд**
```bash
cd frontend
npm run dev
```

**Вариант B: Фронтенд + Бэкенд**
```bash
cd frontend
npm run dev:with-backend
```

**Вариант C: Автоматический скрипт (Windows)**
```bash
start-frontend.bat
```

**Вариант D: Автоматический скрипт (PowerShell)**
```powershell
.\start-all.ps1
```

### 3. Проверьте результат

1. Откройте http://localhost:3000
2. Откройте DevTools (F12)
3. Должна появиться вкладка **"⚛️ Components"**
4. Сообщение должно измениться на:
   > "This page is using the development build of React"

## Разница между режимами

| Режим | Команда | DevTools | Сообщение |
|-------|---------|----------|-----------|
| ❌ Production | `npm start` | ⚠️ Ограниченная | "production build" |
| ✅ Development | `npm run dev` | ✅ Полная | "development build" |

## Дополнительная помощь

См. полную документацию: [REACT_DEVTOOLS_SETUP.md](./REACT_DEVTOOLS_SETUP.md)

