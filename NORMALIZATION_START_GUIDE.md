# Руководство по Запуску Нормализации

**Дата:** 2025-11-26  
**Статус:** ✅ Система готова, требуется запуск backend

---

## 📊 Текущее Состояние

### ✅ Готово к Запуску:
- **7 баз данных** готовы к обработке
- **48,449 записей** найдено:
  - 27,878 записей номенклатуры
  - 20,571 записей контрагентов
- **Оценка времени:** ~21 мин 20 сек
- **Статус:** Все БД готовы (100%)

### ⚠️ Требуется:
- **Backend должен быть запущен** для работы нормализации

---

## 🚀 Инструкция по Запуску

### Шаг 1: Запустить Backend

Откройте терминал и выполните:

```powershell
cd E:\HttpServer
go run cmd/server/main.go
```

Дождитесь сообщения:
```
✓ Сервер успешно запущен на порту 9999
✓ API доступно: http://localhost:9999
```

### Шаг 2: Проверить Работу Backend

В другом терминале проверьте:

```powershell
# Health check
Invoke-RestMethod -Uri "http://localhost:9999/health"

# Должен вернуть: {"status":"ok"}
```

### Шаг 3: Запустить Нормализацию

В интерфейсе:
1. Убедитесь, что выбран проект "mdm aitas"
2. Нажмите кнопку **"Запустить нормализацию для 7 БД проекта"**
3. Дождитесь начала процесса

---

## 🔍 API Endpoints для Нормализации

### Запуск Нормализации:
```
POST /api/clients/{clientId}/projects/{projectId}/normalization/start
```

**Пример запроса:**
```json
{
  "all_active": true,
  "auto_classify_kpved": false,
  "auto_classify_okpd2": false
}
```

### Статус Нормализации:
```
GET /api/clients/{clientId}/projects/{projectId}/normalization/status
```

### Остановка Нормализации:
```
POST /api/clients/{clientId}/projects/{projectId}/normalization/stop
```

---

## 📋 Проверочный Список

Перед запуском нормализации убедитесь:

- [ ] Backend запущен и отвечает на `/health`
- [ ] Все 7 баз данных активны и готовы
- [ ] Статистика показывает корректные данные
- [ ] Нет активных процессов нормализации
- [ ] Достаточно места на диске для результатов

---

## ⚙️ Параметры Запуска

### Опции нормализации:

1. **Обработка всех активных БД:**
   - `all_active: true` - обработать все активные базы данных проекта

2. **Автоматическая классификация:**
   - `auto_classify_kpved: true` - классификация по КПВЭД после нормализации
   - `auto_classify_okpd2: true` - классификация по ОКПД2 после нормализации

3. **Обработка конкретной БД:**
   - `database_path: "путь/к/базе.db"` - обработать только указанную БД

---

## 🔧 Возможные Проблемы

### Проблема: "Остановлено" / "Не запущено"

**Причина:** Backend не запущен или процесс не был запущен

**Решение:**
1. Запустите backend
2. Обновите страницу
3. Попробуйте запустить нормализацию снова

### Проблема: Ошибка 500 при запуске

**Причина:** Backend не может обработать запрос

**Решение:**
1. Проверьте логи backend
2. Убедитесь, что все базы данных доступны
3. Проверьте права доступа к файлам

### Проблема: Процесс не запускается

**Причина:** Может быть активен другой процесс

**Решение:**
1. Проверьте статус нормализации
2. Остановите предыдущий процесс, если нужно
3. Попробуйте запустить снова

---

## 📊 Мониторинг Процесса

После запуска нормализации вы можете:

1. **Отслеживать прогресс** в интерфейсе
2. **Проверять логи** в консоли backend
3. **Смотреть статус** через API endpoint `/status`
4. **Останавливать процесс** при необходимости

---

## ⏱️ Ожидаемое Время

Для вашего проекта:
- **Всего записей:** 48,449
- **Оценка времени:** ~21 мин 20 сек
- **Баз данных:** 7
- **Параллельная обработка:** Да

---

## ✅ После Завершения

После завершения нормализации:

1. Результаты будут доступны в разделе **"Результаты нормализации"**
2. Можно будет просмотреть нормализованные группы
3. Доступна статистика по обработке
4. Можно экспортировать результаты

---

**Готово к запуску!** 🚀

Запустите backend и нажмите кнопку "Запустить нормализацию" в интерфейсе.
