# Инструкция по запуску Docker

**Проблема:** Docker Desktop не запущен  
**Решение:** Запустите Docker Desktop перед сборкой

---

## 🚀 Шаги для сборки контейнера

### 1. Запустите Docker Desktop

1. Найдите Docker Desktop в меню Пуск
2. Запустите приложение
3. Дождитесь полного запуска (иконка в системном трее станет зеленой)
4. Проверьте статус: Docker должен показать "Docker Desktop is running"

### 2. Проверьте, что Docker работает

```powershell
# Проверка версии
docker --version

# Проверка статуса
docker info
```

### 3. Соберите контейнер

```powershell
# Вариант 1: Docker Compose (рекомендуется)
docker-compose build

# Вариант 2: Только Backend
docker build -t httpserver-backend .

# Вариант 3: PowerShell скрипт
.\docker-build.ps1 -Build
```

### 4. Запустите контейнер

```powershell
# Запуск всех сервисов
docker-compose up -d

# Или через скрипт
.\docker-build.ps1 -Run
```

---

## 🔍 Проверка статуса Docker

### Windows

```powershell
# Проверка, запущен ли Docker
Get-Process "Docker Desktop" -ErrorAction SilentlyContinue

# Если не запущен, запустите вручную
Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"
```

### Проверка через Docker

```powershell
# Должно вернуть информацию о Docker
docker info

# Должно показать список контейнеров (может быть пустым)
docker ps -a
```

---

## ⚠️ Частые проблемы

### Проблема 1: "Docker Desktop is not running"

**Решение:**
1. Запустите Docker Desktop вручную
2. Дождитесь полного запуска (может занять 1-2 минуты)
3. Проверьте иконку в системном трее

### Проблема 2: "Cannot connect to Docker daemon"

**Решение:**
1. Перезапустите Docker Desktop
2. Проверьте, что WSL 2 установлен (для Windows)
3. Проверьте настройки Docker Desktop

### Проблема 3: "Port already in use"

**Решение:**
1. Остановите другие приложения на портах 9999 и 3000
2. Или измените порты в `docker-compose.yml`

---

## 📋 После запуска Docker Desktop

Выполните:

```powershell
# Сборка
docker-compose build

# Запуск
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f
```

---

## ✅ Готово!

После запуска Docker Desktop и выполнения команд выше, контейнер будет собран и запущен.

**Доступные сервисы:**
- Backend: http://localhost:9999
- Frontend: http://localhost:3000
- Health: http://localhost:9999/health

