# Статус запуска тестов

## Текущее состояние

### ✅ Готово
1. **Анализ сборки завершен** - создан `BUILD_ANALYSIS.md`
2. **Makefile исправлен** - обновлены пути к точкам входа
3. **Скрипты созданы**:
   - `quick_start_server.ps1` - быстрый запуск сервера
   - `start_server_and_test.ps1` - автозапуск сервера и тестов
   - `run_tests_windows.ps1` - запуск тестов на Windows

### ⏳ В процессе
1. **Запуск сервера** - сервер запускается в фоновом режиме
2. **Проверка доступности** - ожидание ответа от сервера

### 📋 Следующие шаги

1. **Проверить статус сервера**:
   ```powershell
   curl http://localhost:9999/health
   netstat -ano | findstr :9999
   ```

2. **Запустить диагностику** (если сервер не отвечает):
   ```powershell
   cd chaos_tests
   bash diagnostic.sh  # Если есть WSL/Git Bash
   ```

3. **Запустить тесты** (когда сервер готов):
   ```powershell
   cd chaos_tests
   .\run_tests_windows.ps1
   # или
   .\start_server_and_test.ps1
   ```

## Известные проблемы

1. **Python не в PATH** - скрипты ищут Python в стандартных местах
2. **Сервер может не запускаться** - проверьте логи и переменные окружения
3. **Порт 9999 может быть занят** - используйте `netstat` для проверки

## Решения

### Если сервер не запускается:
1. Проверьте наличие `httpserver_no_gui.exe`
2. Установите `ARLIAI_API_KEY` в переменные окружения
3. Проверьте логи сервера
4. Убедитесь, что порт 9999 свободен

### Если Python не найден:
1. Установите Python с python.org
2. Или используйте `py` launcher: `py chaos_monkey.py`
3. Или используйте WSL для Bash-скриптов

### Если тесты не запускаются:
1. Убедитесь, что сервер запущен и отвечает
2. Проверьте зависимости: `pip install -r requirements.txt`
3. Запустите `test_connection.py` для проверки подключения

