# Chaos Monkey Testing - Итоги реализации

## ✅ Что было создано

### 1. Диагностический инструмент
- **`diagnostic.sh`** - Bash-скрипт для проверки окружения
  - Проверка ОС и shell
  - Проверка зависимостей (curl, jq, bc, ps)
  - Проверка статуса бэкенда
  - Проверка предусловий

### 2. Python-версия тестов (кросс-платформенная) ⭐
- **`chaos_monkey.py`** - Полноценная Python-версия всех тестов
  - Работает на Windows/Linux/macOS
  - Не требует внешних утилит (curl, jq, bc)
  - Использует `requests` для HTTP
  - Использует `psutil` для мониторинга (опционально)
  - Цветное логирование
  - Автоматическая генерация отчетов

### 3. Bash-версия тестов (для WSL/Linux)
- **`test_concurrent_config.sh`** - Конкурентные обновления конфигурации
- **`test_invalid_normalization.sh`** - Невалидные данные
- **`test_ai_failure.sh`** - Устойчивость к сбоям AI
- **`test_large_data.sh`** - Работа с большими объемами
- **`run_all_tests.sh`** - Запуск всех тестов

### 4. Вспомогательные скрипты
- **`run_with_server.py`** - Автоматический запуск сервера и тестов
- **`run_tests.ps1`** - PowerShell скрипт для Windows
- **`utils.sh`** - Общие утилиты для Bash-скриптов

### 5. Документация
- **`README.md`** - Полная документация
- **`QUICKSTART.md`** - Быстрый старт
- **`RUN_INSTRUCTIONS.md`** - Инструкции по запуску
- **`CHAOS_TEST_REPORT.md`** - Шаблон отчета

## 🎯 Реализованные тесты

### 1. Конкурентные обновления конфигурации
- ✅ 10 параллельных PUT /api/config запросов
- ✅ Проверка ошибок блокировки БД
- ✅ Анализ истории конфигурации на пропуски версий
- ✅ Автоматическое восстановление исходной конфигурации

### 2. Нормализация с невалидными данными
- ✅ Несуществующий database_id
- ✅ Невалидный item_id (0)
- ✅ Пустой original_name
- ✅ Отрицательный лимит в истории
- ✅ Несуществующий session_id
- ✅ Невалидный JSON

### 3. Устойчивость к сбоям AI
- ✅ Установка невалидного API ключа
- ✅ Полный цикл нормализации (start → apply-patterns → apply-ai)
- ✅ Проверка статуса сессии после ошибки
- ✅ Проверка логов на наличие ошибок
- ✅ Восстановление валидного API ключа

### 4. Работа с большими объемами данных
- ✅ Базовый мониторинг ресурсов (60 секунд)
- ✅ Мониторинг во время нормализации
- ✅ Проверка таймаутов
- ✅ Анализ использования CPU и памяти
- ✅ Выявление утечек памяти

## 📋 Зависимости

### Python-версия (рекомендуется)
```bash
pip install requests psutil
```

### Bash-версия
- curl
- jq
- bc
- ps (для мониторинга)

## 🚀 Как запустить

### Вариант 1: Python (кросс-платформенный)

```bash
cd chaos_tests

# Установка зависимостей
pip install -r requirements.txt

# Запуск всех тестов
python chaos_monkey.py --test all

# Запуск отдельного теста
python chaos_monkey.py --test concurrent_config
```

### Вариант 2: Bash (требует WSL/Git Bash)

```bash
cd chaos_tests

# Проверка окружения
bash diagnostic.sh

# Запуск всех тестов
bash run_all_tests.sh
```

### Вариант 3: PowerShell (Windows)

```powershell
cd chaos_tests
.\run_tests.ps1 concurrent_config
```

## ⚠️ Известные проблемы

### 1. WSL и localhost
**Проблема:** WSL не может подключиться к localhost:9999 из Windows

**Решение:**
- Используйте Python-версию напрямую в Windows (не через WSL)
- Или используйте IP адрес Windows хоста в WSL

### 2. psutil не установлен
**Проблема:** Мониторинг ресурсов недоступен

**Решение:**
```bash
pip install psutil
```

**Примечание:** Тесты работают без psutil, только мониторинг ресурсов будет недоступен.

### 3. Сервер возвращает 502
**Проблема:** Сервер запущен, но возвращает Bad Gateway

**Решение:**
- Проверьте логи сервера
- Убедитесь, что БД доступна
- Проверьте конфигурацию сервера

## 📊 Структура результатов

После выполнения тестов:

```
chaos_tests/
├── logs/
│   └── chaos_monkey_YYYYMMDD.log    # Детальные логи
├── reports/
│   ├── concurrent_config_*.md        # Отчет по тесту
│   ├── invalid_normalization_*.md
│   ├── ai_failure_*.md
│   ├── large_data_*.md
│   └── chaos_test_summary_*.md        # Сводный отчет
```

## 🎓 Рекомендации

1. **Всегда запускайте диагностику перед тестами:**
   ```bash
   bash diagnostic.sh
   ```

2. **Используйте Python-версию на Windows** - она более надежна

3. **Проверяйте логи** в `chaos_tests/logs/` для детальной информации

4. **Читайте отчеты** в `chaos_tests/reports/` для анализа результатов

## 🔧 Следующие шаги

1. Убедитесь, что сервер запущен и доступен
2. Запустите диагностику: `bash diagnostic.sh`
3. Запустите тесты: `python chaos_monkey.py --test all`
4. Проанализируйте отчеты в `reports/`
5. Исправьте найденные проблемы
6. Повторите тесты после исправлений

## 📝 Примечания

- Тесты могут временно изменить конфигурацию (автоматически восстанавливается)
- Некоторые тесты требуют наличия клиентов и проектов в БД
- Тест `large_data` может занять до 5 минут
- Все изменения конфигурации автоматически откатываются

---

**Статус:** ✅ Реализация завершена
**Дата:** 2025-11-25
**Версия:** 1.0

