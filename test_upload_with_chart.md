# Тестирование загрузки файла с графиком

## Шаги для тестирования

1. **Запустить бэкенд** (если не запущен):
   ```bash
   cd E:\HttpServer
   go run main.go
   ```

2. **Запустить фронтенд** (если не запущен):
   ```bash
   cd E:\HttpServer\frontend
   npm run dev
   ```

3. **Открыть браузер**:
   - Перейти на `http://localhost:3000`
   - Войти в систему
   - Перейти к проекту (например, `/clients/1/projects/3`)

4. **Загрузить файл**:
   - Перейти на вкладку "Базы данных"
   - Перетащить файл `.db` в область загрузки или нажать для выбора файла
   - Наблюдать за метриками во время загрузки:
     - Время загрузки (обновляется в реальном времени)
     - Скорость (обновляется в реальном времени)
     - Размер файла
     - Время начала

5. **Проверить график**:
   - После завершения загрузки должен появиться график скорости загрузки
   - График должен показывать:
     - Линию скорости по секундам
     - Среднюю скорость
     - Максимальную скорость
     - Интерактивный tooltip при наведении

## Проверка данных

### В консоли браузера должны быть логи:
- `[Frontend] 📤 Начало загрузки файла: ...`
- `[Frontend] 📥 Получен ответ от сервера: ...`
- `[Frontend] ✅ Файл успешно загружен за ...`

### В консоли сервера должны быть логи:
- `[handleUploadProjectDatabase] Начало обработки загрузки файла...`
- `[handleUploadProjectDatabase] Файл сохранен: ... скорость: ... MB/s`
- `[handleUploadProjectDatabase] ✅ Успешно завершено...`

## Ожидаемое поведение

1. **Во время загрузки**:
   - Показывается оверлей с анимацией загрузки
   - Отображаются метрики в реальном времени
   - Данные собираются каждую секунду

2. **После загрузки**:
   - Показывается карточка "Новая база данных"
   - Отображаются финальные метрики
   - Показывается график скорости загрузки
   - График содержит данные по всем секундам загрузки

## Возможные проблемы

1. **График не отображается**:
   - Проверить, что `uploadSpeedHistory.length > 0`
   - Проверить консоль браузера на ошибки
   - Убедиться, что компонент `UploadSpeedChart` импортирован

2. **Нет данных в графике**:
   - Проверить, что `lastSecond` обновляется
   - Проверить, что `currentSecond > 0`
   - Убедиться, что загрузка длится больше 1 секунды

3. **Ошибки загрузки**:
   - Проверить, что бэкенд запущен на порту 9999
   - Проверить логи бэкенда
   - Проверить размер файла (максимум 500MB)

