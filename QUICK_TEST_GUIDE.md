# Быстрое руководство по тестированию множественной загрузки

## 🚀 Быстрый старт

### Вариант 1: Запуск всех тестов сразу
```powershell
.\run_all_multiple_upload_tests.ps1
```

Этот скрипт запустит все тесты последовательно и выведет итоговую сводку.

### Вариант 2: Запуск отдельных тестов

#### Unit-тесты (самые быстрые)
```powershell
go test ./server -run TestMultipleUpload -v
```

#### Интеграционные тесты
```powershell
.\test_multiple_database_upload.ps1
```

#### E2E тесты
```powershell
.\test_multiple_upload_e2e.ps1
```

#### Нагрузочные тесты
```powershell
.\test_multiple_upload_load.ps1
```

#### Тесты граничных случаев
```powershell
.\test_multiple_upload_edge_cases.ps1
```

## 📋 Предварительные требования

### Обязательно:
- ✅ Backend сервер должен быть запущен на `http://localhost:9999`
- ✅ Go установлен (для unit-тестов)

### Опционально:
- Frontend сервер на `http://localhost:3000` (для E2E тестов)

## 🔧 Запуск серверов

### Backend
```powershell
# Вариант 1: Через скрипт
.\start-server.ps1

# Вариант 2: Напрямую
go run main.go

# Вариант 3: Без GUI
go run main_no_gui.go
```

### Frontend (для E2E тестов)
```powershell
cd frontend
npm run dev
```

## 📊 Просмотр результатов

### HTML отчет
Откройте в браузере:
```
api_tests/multiple_upload_test_report.html
```

### JSON результаты
После запуска `run_all_multiple_upload_tests.ps1` результаты сохраняются в:
```
multiple_upload_test_results_YYYYMMDD_HHMMSS.json
```

## ⚡ Часто используемые команды

### Только быстрые тесты (unit + интеграционные)
```powershell
go test ./server -run TestMultipleUpload -v
.\test_multiple_database_upload.ps1
```

### Полное тестирование
```powershell
.\run_all_multiple_upload_tests.ps1
```

### Только проверка граничных случаев
```powershell
.\test_multiple_upload_edge_cases.ps1
```

## 🐛 Устранение неполадок

### Backend не отвечает
```powershell
# Проверить статус
Test-NetConnection -ComputerName localhost -Port 9999

# Запустить backend
.\start-server.ps1
```

### Ошибки при выполнении тестов
1. Убедитесь, что backend запущен
2. Проверьте, что порт 9999 свободен
3. Проверьте права доступа к временным директориям

### Тесты падают с ошибками
- Проверьте логи backend сервера
- Убедитесь, что база данных доступна
- Проверьте, что есть достаточно места на диске

## 📝 Примеры использования

### Тестирование конкретного сценария
```powershell
# Только проверка больших файлов
.\test_multiple_upload_load.ps1
```

### Проверка безопасности
```powershell
# Тесты граничных случаев включают проверки безопасности
.\test_multiple_upload_edge_cases.ps1
```

### Полная проверка перед релизом
```powershell
# Запуск всех тестов
.\run_all_multiple_upload_tests.ps1

# Просмотр отчета
Start-Process "api_tests/multiple_upload_test_report.html"
```

## 🎯 Что тестируется

- ✅ Последовательная загрузка нескольких файлов
- ✅ Обработка ошибок
- ✅ Валидация файлов
- ✅ Ограничения размера
- ✅ Переименование дубликатов
- ✅ Производительность
- ✅ Безопасность
- ✅ Граничные случаи

## 📚 Дополнительная документация

- **Полная документация:** `MULTIPLE_UPLOAD_TESTS_README.md`
- **Итоговая сводка:** `MULTIPLE_UPLOAD_TESTS_SUMMARY.md`
- **HTML отчет:** `api_tests/multiple_upload_test_report.html`

