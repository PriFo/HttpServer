# Legacy services

Здесь будут временно храниться сервисные обёртки, вынесенные из `server_*.go` при миграции обработчиков. После завершения фазы 8 планируется перенос в `internal/domain/services`.

