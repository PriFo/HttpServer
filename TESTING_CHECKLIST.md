# Чеклист для тестирования исправлений

## ✅ Предварительная проверка

### 1. Backend запущен
```bash
# Проверка доступности backend
curl http://localhost:9999/health

# Ожидаемый результат:
# {"status":"healthy","time":"2025-..."}
```

Если backend не запущен:
```bash
# Windows
start-backend.bat

# Или вручную
go run main_no_gui.go
```

### 2. Frontend запущен
```bash
cd frontend
npm run dev
```

Frontend должен быть доступен на http://localhost:3000

---

## 🧪 Тестирование исправлений

### Тест 1: Подключение к Backend

**Шаги:**
1. Откройте http://localhost:3000
2. Откройте консоль разработчика (F12)
3. Перейдите на главную страницу

**Ожидаемый результат:**
- ✅ Нет ошибок "Не удалось подключиться к backend серверу"
- ✅ Данные на дашборде загружаются
- ✅ Индикатор статуса backend показывает "Backend" (зеленый)

**Если есть ошибки:**
- Проверьте, что backend запущен: `curl http://localhost:9999/health`
- Проверьте файл `frontend/.env.local`: должен содержать `NEXT_PUBLIC_BACKEND_URL=http://localhost:9999`
- Перезапустите frontend

---

### Тест 2: Дублирующиеся ключи React

**Шаги:**
1. Откройте консоль разработчика (F12)
2. Перейдите на страницу с выбором базы данных
3. Проверьте консоль на наличие предупреждений

**Ожидаемый результат:**
- ✅ Нет предупреждений "Encountered two children with the same key"
- ✅ Список баз данных отображается корректно
- ✅ Все элементы списка имеют уникальные ключи

**Компоненты для проверки:**
- ✅ `DatabaseSelector` - список баз данных
- ✅ `DataSourceSelector` - выбор источника данных
- ✅ `QualityReportTab` - отчеты качества
- ✅ `QualityDuplicatesTab` - группы дубликатов

---

### Тест 3: Работа с дубликатами данных

**Шаги:**
1. Убедитесь, что в системе есть базы данных с одинаковыми именами
2. Откройте компонент выбора базы данных
3. Проверьте отображение списка

**Ожидаемый результат:**
- ✅ Дубликаты фильтруются автоматически
- ✅ При наличии дубликатов приоритет отдается текущей БД
- ✅ Все элементы отображаются корректно
- ✅ Нет ошибок в консоли

---

### Тест 4: API запросы

**Шаги:**
1. Откройте консоль разработчика (F12)
2. Перейдите на вкладку Network
3. Выполните различные действия в приложении

**Ожидаемый результат:**
- ✅ Все запросы к `/api/*` идут через Next.js API routes
- ✅ Запросы к backend идут на `http://localhost:9999`
- ✅ Нет ошибок 404 или 500
- ✅ Таймауты работают корректно (7 секунд)

---

### Тест 5: Health Check

**Шаги:**
1. Откройте консоль разработчика (F12)
2. Проверьте индикатор статуса backend
3. Остановите backend и проверьте индикатор

**Ожидаемый результат:**
- ✅ Индикатор показывает "Backend" когда сервер доступен
- ✅ Индикатор показывает "Нет связи" когда сервер недоступен
- ✅ Health check использует endpoint `/health` (не `/api/health`)

---

## 🐛 Известные проблемы (если есть)

На данный момент известных проблем нет.

---

## 📝 Логи для отладки

### Если backend не подключается:

1. Проверьте логи backend:
   ```bash
   # Если запущен через go run
   # Логи будут в терминале
   
   # Если запущен через docker
   docker-compose logs backend
   ```

2. Проверьте переменные окружения:
   ```bash
   # Frontend
   cat frontend/.env.local
   
   # Должно содержать:
   # NEXT_PUBLIC_BACKEND_URL=http://localhost:9999
   ```

3. Проверьте порты:
   ```bash
   # Windows PowerShell
   netstat -ano | findstr :9999
   
   # Должен быть процесс, слушающий порт 9999
   ```

### Если есть дублирующиеся ключи:

1. Проверьте консоль браузера на наличие предупреждений
2. Убедитесь, что все компоненты используют уникальные ключи
3. Проверьте, что фильтрация дубликатов работает

---

## ✅ Критерии успешного тестирования

- ✅ Backend доступен и отвечает на health check
- ✅ Frontend корректно подключается к backend
- ✅ Нет ошибок в консоли браузера
- ✅ Нет предупреждений React о дублирующихся ключах
- ✅ Все списки отображаются корректно
- ✅ Дубликаты данных обрабатываются правильно

---

## 📚 Дополнительная информация

См. также:
- `BACKEND_CONNECTION_FIX.md` - Детали исправления подключения
- `DUPLICATE_KEYS_FIX.md` - Детали исправления ключей
- `FIXES_SUMMARY.md` - Краткая сводка
- `COMPREHENSIVE_FIXES_REPORT.md` - Полный отчет
