# Руководство по тестированию API контрагентов

**Дата**: 2025-01-21  
**Статус**: ✅ **ГОТОВО К ИСПОЛЬЗОВАНИЮ**

---

## 📋 Доступные тестовые скрипты

### 1. `test_counterparties_all_api.ps1`
**Описание**: Тестирование API получения всех контрагентов клиента

**Использование**:
```powershell
.\test_counterparties_all_api.ps1 -ClientID 1
.\test_counterparties_all_api.ps1 -ClientID 1 -BaseUrl "http://localhost:9999"
```

**Тестируемые функции**:
- ✅ Получение всех контрагентов клиента
- ✅ Пагинация
- ✅ Фильтр по источнику (database/normalized)
- ✅ Поиск по имени
- ✅ Фильтр по проекту
- ✅ Валидация ошибок

### 2. `test_counterparties_export_api.ps1`
**Описание**: Тестирование API экспорта контрагентов

**Использование**:
```powershell
.\test_counterparties_export_api.ps1 -ClientID 1
.\test_counterparties_export_api.ps1 -ClientID 1 -BaseUrl "http://localhost:9999"
```

**Тестируемые функции**:
- ✅ Экспорт в CSV
- ✅ Экспорт в JSON
- ✅ Экспорт с фильтром по источнику
- ✅ Экспорт с поиском
- ✅ Валидация ошибок

**Результаты**: Тестовые файлы сохраняются в директории `test_exports/`

---

## 🔍 Ручное тестирование через curl

### Получение всех контрагентов

```bash
# Базовый запрос
curl "http://localhost:9999/api/counterparties/all?client_id=1"

# С пагинацией
curl "http://localhost:9999/api/counterparties/all?client_id=1&offset=0&limit=10"

# С фильтром по источнику
curl "http://localhost:9999/api/counterparties/all?client_id=1&source=database"
curl "http://localhost:9999/api/counterparties/all?client_id=1&source=normalized"

# С поиском
curl "http://localhost:9999/api/counterparties/all?client_id=1&search=ООО"

# С фильтром по проекту
curl "http://localhost:9999/api/counterparties/all?client_id=1&project_id=1"

# С сортировкой
curl "http://localhost:9999/api/counterparties/all?client_id=1&sort_by=name&order=asc"
```

### Экспорт контрагентов

```bash
# Экспорт в CSV
curl "http://localhost:9999/api/counterparties/all/export?client_id=1&format=csv" -o counterparties.csv

# Экспорт в JSON
curl "http://localhost:9999/api/counterparties/all/export?client_id=1&format=json" -o counterparties.json

# Экспорт с фильтрами
curl "http://localhost:9999/api/counterparties/all/export?client_id=1&format=csv&source=normalized" -o normalized.csv
curl "http://localhost:9999/api/counterparties/all/export?client_id=1&format=json&search=ООО" -o search_results.json
```

---

## 📊 Ожидаемые результаты

### Успешный ответ (200 OK)

```json
{
  "counterparties": [
    {
      "id": 1,
      "name": "ООО Рога и Копыта",
      "source": "database",
      "project_id": 1,
      "project_name": "Проект 1",
      "database_id": 1,
      "database_name": "База данных 1",
      "tax_id": "1234567890",
      "quality_score": 0.95
    }
  ],
  "total": 100,
  "offset": 0,
  "limit": 20,
  "projects": [
    {
      "id": 1,
      "name": "Проект 1"
    }
  ],
  "stats": {
    "processing_time_ms": 150,
    "databases_processed": 2,
    "projects_processed": 1
  }
}
```

### Ошибка валидации (400 Bad Request)

```json
{
  "error": "client_id is required and must be positive"
}
```

---

## ✅ Чек-лист тестирования

### Базовые функции:
- [ ] Получение всех контрагентов клиента
- [ ] Пагинация работает корректно
- [ ] Фильтр по источнику работает
- [ ] Поиск работает
- [ ] Фильтр по проекту работает
- [ ] Сортировка работает

### Экспорт:
- [ ] Экспорт в CSV работает
- [ ] Экспорт в JSON работает
- [ ] Экспорт учитывает фильтры
- [ ] Файлы скачиваются корректно

### Валидация:
- [ ] Ошибка при отсутствии client_id
- [ ] Ошибка при неверном формате экспорта
- [ ] Ошибка при неверных параметрах

### Производительность:
- [ ] Запросы выполняются быстро (< 1 сек для малых данных)
- [ ] Пагинация работает для больших объемов
- [ ] Экспорт работает для больших объемов

---

## 🐛 Отладка

### Проблемы и решения

**Проблема**: Запрос возвращает 500 ошибку
- **Решение**: Проверьте логи сервера, убедитесь что база данных доступна

**Проблема**: Пустой ответ
- **Решение**: Проверьте наличие данных в базе для указанного клиента

**Проблема**: Экспорт не работает
- **Решение**: Проверьте формат (должен быть "csv" или "json"), проверьте права на запись

**Проблема**: Фильтры не работают
- **Решение**: Проверьте правильность параметров (source должен быть "database" или "normalized")

---

## 📝 Примеры использования

### PowerShell

```powershell
# Получение контрагентов
$response = Invoke-RestMethod -Uri "http://localhost:9999/api/counterparties/all?client_id=1"
$response.counterparties | Format-Table

# Экспорт в CSV
Invoke-WebRequest -Uri "http://localhost:9999/api/counterparties/all/export?client_id=1&format=csv" -OutFile "counterparties.csv"
```

### JavaScript (Fetch API)

```javascript
// Получение контрагентов
fetch('http://localhost:9999/api/counterparties/all?client_id=1')
  .then(response => response.json())
  .then(data => console.log(data));

// Экспорт
fetch('http://localhost:9999/api/counterparties/all/export?client_id=1&format=json')
  .then(response => response.blob())
  .then(blob => {
    const url = window.URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = 'counterparties.json';
    a.click();
  });
```

---

## ✅ Итоговое заключение

**Все тестовые скрипты готовы к использованию!**

Для запуска тестов:
1. Убедитесь, что сервер запущен на `http://localhost:9999`
2. Запустите тестовые скрипты
3. Проверьте результаты

**Статус**: ✅ **ГОТОВО К ИСПОЛЬЗОВАНИЮ**

---

**Дата создания**: 2025-01-21

