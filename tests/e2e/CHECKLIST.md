# Чек-лист перед запуском E2E тестов

## ✅ Предварительные проверки

### Окружение

- [ ] Go-бэкенд запущен на `http://127.0.0.1:9999`
  ```bash
  # Проверка
  curl http://127.0.0.1:9999/health
  ```

- [ ] Next.js фронтенд запущен на `http://localhost:3000`
  ```bash
  # Проверка
  curl http://localhost:3000
  ```

- [ ] Тестовая база данных существует в одном из стандартных мест:
  - [ ] `1c_data.db`
  - [ ] `data/1c_data.db`
  - [ ] `test-data.db`
  - [ ] `data/test-data.db`
  - [ ] `tests/data/test-data.db`

### Зависимости

- [ ] Установлены все npm зависимости
  ```bash
  npm install
  cd frontend && npm install
  ```

- [ ] Установлен Playwright
  ```bash
  npx playwright install
  ```

- [ ] Установлен @axe-core/playwright
  ```bash
  npm list @axe-core/playwright
  ```

### Конфигурация

- [ ] Файл `frontend/playwright.config.ts` существует и настроен
- [ ] Переменные окружения установлены (опционально):
  - [ ] `BACKEND_URL`
  - [ ] `NEXT_PUBLIC_BASE_URL`

## 🧪 Проверка перед запуском конкретных тестов

### Тесты управления данными

- [ ] Бэкенд имеет доступ к директории `data/backups` для создания бэкапов
- [ ] Есть права на запись в директорию бэкапов
- [ ] Тестовая БД содержит данные для тестирования

### Тесты управления качеством

- [ ] Тестовая БД содержит дубликаты для тестирования слияния
- [ ] Сервис качества данных доступен и работает
- [ ] API эндпоинты `/api/quality/*` доступны

### Тесты обработки ошибок

- [ ] Бэкенд может быть временно недоступен для тестирования
- [ ] Сетевые ошибки могут быть симулированы

### Тесты доступности

- [ ] @axe-core/playwright установлен
- [ ] Страницы приложения загружаются корректно
- [ ] JavaScript включен в браузере

## 🚀 Быстрая проверка работоспособности

### Минимальный тест

```bash
# Запустить один простой тест для проверки окружения
npx playwright test tests/e2e/data-management.spec.ts -g "Валидация формата"
```

### Проверка подключения

```bash
# Проверить доступность бэкенда
curl http://127.0.0.1:9999/api/v1/health

# Проверить доступность фронтенда
curl http://localhost:3000
```

## ⚠️ Известные проблемы и решения

### Проблема: Тесты не находят бэкенд

**Решение:**
1. Убедитесь, что бэкенд запущен: `go run main.go`
2. Проверьте порт: `netstat -an | findstr 9999` (Windows) или `lsof -i :9999` (Linux/Mac)
3. Проверьте переменную окружения: `echo $BACKEND_URL`

### Проблема: Тесты не находят фронтенд

**Решение:**
1. Убедитесь, что фронтенд запущен: `cd frontend && npm run dev`
2. Проверьте порт: `netstat -an | findstr 3000`
3. Откройте браузер и проверьте: `http://localhost:3000`

### Проблема: Тесты не находят тестовую БД

**Решение:**
1. Поместите БД в один из стандартных путей
2. Или обновите функцию `findTestDatabase()` в `utils/api-testing.ts`
3. Или создайте символическую ссылку на БД

### Проблема: Тесты падают из-за таймаутов

**Решение:**
1. Увеличьте таймауты в `playwright.config.ts`
2. Проверьте производительность системы
3. Убедитесь, что нет других процессов, нагружающих систему

### Проблема: Ошибки доступности (a11y)

**Решение:**
1. Проверьте отчет axe-core в консоли
2. Исправьте критические нарушения
3. Для некритичных нарушений можно добавить исключения в тест

## 📊 После запуска тестов

- [ ] Проверены результаты всех тестов
- [ ] Просмотрен HTML отчет (если есть падения)
- [ ] Исправлены найденные проблемы
- [ ] Обновлена документация при необходимости

## 🔄 Регулярное обслуживание

- [ ] Обновлять зависимости раз в месяц
- [ ] Проверять актуальность тестов при изменении UI
- [ ] Обновлять тестовые данные при изменении схемы БД
- [ ] Рефакторить тесты при изменении структуры приложения

