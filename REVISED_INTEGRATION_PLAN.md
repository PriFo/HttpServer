# ПЕРЕСМОТРЕННАЯ ОЦЕНКА ПЛАНА ИНТЕГРАЦИИ

**Дата:** 2025-11-20 (версия 2)  
**Подготовлено для:** АО «AITAS KZ», Проект AITAS-MDM-2025-001  
**Статус:** ✅ ГОТОВО К ВНЕДРЕНИЮ С ИЗМЕНЕННЫМ ПОДХОДОМ

---

## 📌 КЛЮЧЕВОЕ ИЗМЕНЕНИЕ ПАРАДИГМЫ

### Пересмотренное видение

ArliAI **НЕ является основным интеграционным хабом**. Вместо этого:

```
✅ ПРАВИЛЬНО:

1С:MDM
  │
  ├─→ Adata.kz          (прямо, высокая скорость)
  ├─→ Dadata.ru         (прямо, высокая скорость)  
  ├─→ Kompra.kz         (прямо, высокая скорость)
  └─→ CRM               (прямо, высокая скорость)
  
  + Программист выполняет:
    ├─ Нормализация названий контрагентов
    ├─ Валидация качества данных
    ├─ Обработка конфликтов
    ├─ Дедупликация объектов
    └─ Основное преобразование данных
  
  + ArliAI помощник (специализированный):
    └─ Классификация товаров по ТН ВЭД
       (когда нужно, не для всех данных)
```

**Логика:**
- ArliAI API = узкое горлышко проекта
- Избыточное использование ArliAI = замедление обработки
- Логика нормализации → ответственность программиста
- ArliAI → только специализированная задача (классификация ТН ВЭД)

---

## 🎯 УТОЧНЕННАЯ РОЛЬ ArliAI

### Что ArliAI ДЕЛАЕТ ✅

| Функция | Когда использовать | Почему |
|---------|-------------------|--------|
| **Классификация ТН ВЭД** | На этапе классификации товаров | Требует ИИ, не критична по скорости |
| **Подсказки кодов ТН ВЭД** | При неуверенности в коде | ИИ может предложить варианты |
| **Согласование описаний** | Когда описание товара расплывчато | ИИ уточняет описание перед классификацией |

### Что ArliAI НЕ делает ❌

| Функция | Причина | Ответственный |
|---------|---------|---------------|
| **Нормализация названий контрагентов** | Программист быстрее, без API задержек | Программист |
| **Валидация качества данных** | Логика простая, нужна скорость | Программист |
| **Обработка конфликтов данных** | Нужна мгновенная реакция | Программист |
| **Дедупликация объектов** | Критична скорость обработки | Программист (SQL + логика) |
| **Преобразование форматов** | API является узким местом | Программист (локальная логика) |
| **Интеграция с внешними сервисами** | Их API обслуживают прямые подключения | 1С:MDM (прямое подключение) |

---

## 📊 АРХИТЕКТУРА ИНТЕГРАЦИИ (ПЕРЕСМОТРЕННАЯ)

### Диаграмма потоков данных

```
┌────────────────────────────────────────────────────────────────┐
│                     1С:MDM 2.5.11                              │
│            (Основной обработчик всех данных)                   │
└──────┬───────────────────────────────────────────────────┬─────┘
       │                                                   │
       │ Прямое подключение (высокая скорость)          │
       │                                                 │
   ┌───┴───────────────────────────────────────────────┬─┴──┐
   ▼                                                   ▼     ▼
┌─────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐ ┌──────────┐
│Adata.kz │  │Dadata.ru │  │Kompra.kz │  │ CRM    │ │  1С БД   │
│         │  │          │  │          │  │        │ │(SQL)     │
│ Данные  │  │ Адреса   │  │ Справочн.│  │Контакты│ │Нормализ. │
│о БИН    │  │ Геокод   │  │Классифик.│  │        │ │данные    │
└─────────┘  └──────────┘  └──────────┘  └────────┘ └──────────┘


         ┌─────────────────────────────────────────┐
         │     Бизнес-логика (Программист)        │
         │                                         │
         │  • Нормализация названий                │
         │  • Валидация данных                     │
         │  • Обработка конфликтов                 │
         │  • Дедупликация                         │
         │  • Маппинг полей                        │
         │  • Преобразование форматов              │
         └─────────────────────────────────────────┘
                      │
                      ▼
         ┌─────────────────────────────┐
         │  Товарная номенклатура      │
         │  Требуется классификация    │
         │  по ТН ВЭД?                 │
         └──────────────┬──────────────┘
                        │
                    ┌───┴────────┐
                    │ ДА → нужна │
                    │   классиф. │
                    └──────┬─────┘
                           │
                           ▼
            ╔══════════════════════════════╗
            ║   ArliAI Помощник (опционал) ║
            ║                              ║
            ║  Классификация ТН ВЭД        ║
            ║  только для товаров          ║
            ║  (специализированная задача) ║
            ╚══════════════════════════════╝
                           │
                           ▼
         ┌─────────────────────────────┐
         │  Готовые данные             │
         │  в 1С:MDM                   │
         │  с классификацией ТН ВЭД    │
         └─────────────────────────────┘
```

---

## ⚙️ АРХИТЕКТУРА ОБРАБОТКИ ДАННЫХ

### Поток данных (детально)

#### 1️⃣ Этап 1: Получение данных (БЫСТРО - БЕЗ ArliAI)

```
Источники данных → 1С:MDM
├─ Adata.kz → REST API → 1С:MDM (кэш в MDM БД)
├─ Dadata.ru → REST API → 1С:MDM (кэш в MDM БД)
├─ Kompra.kz → REST API → 1С:MDM (кэш в MDM БД)
└─ CRM → REST API → 1С:MDM (синхронизация)

⏱️ Время: мгновенное (параллельные запросы)
```

#### 2️⃣ Этап 2: Нормализация и валидация (БЫСТРО - ПРОГРАММИСТ)

```
Программист в 1С:MDM:
├─ Нормализация названий
│  └─ ООО "Компания" → ООО Компания
├─ Валидация БИН/ИНН
│  └─ Проверка контрольной суммы
├─ Стандартизация адресов
│  └─ Получено от Dadata (готовое)
├─ Обработка качества данных
│  └─ Заполнены ли обязательные поля?
├─ Дедупликация
│  └─ SQL запросы к локальной БД
└─ Преобразование форматов
   └─ JSON → 1С формат

⏱️ Время: быстро (локальная обработка)
🔧 Инструмент: SQL, 1С логика, Python скрипты
```

#### 3️⃣ Этап 3: Классификация (МЕДЛЕННО - ArliAI ТОЛЬКО ЕСЛИ НУЖНО)

```
Перед сохранением в справочник "Номенклатура":
├─ Есть ТН ВЭД код? → ДА → Пропустить ArliAI
├─ Это товар или услуга?
│  └─ Услуга → Пропустить ArliAI (нет ТН ВЭД)
└─ Это товар БЕЗ ТН ВЭД? → ДА → Вызвать ArliAI
   │
   └─→ ArliAI.classify_tnved(
        description="Ноутбук ASUS ROG",
        quantity_unit="шт",
        original_code=null
       )
       │
       └─→ Результат: "8471.30.10" (ЭВМ портативные)

⏱️ Время: медленно (ИИ обработка ~1-2 сек на запрос)
🎯 Использование: только для товаров БЕЗ классификации
📊 Критичность: НИЗКАЯ (можно вручную позже)
```

---

## 📋 ПЕРЕСМОТРЕННЫЙ ПЛАН ИНТЕГРАЦИИ

### Фаза 1: Интеграция внешних данных (Проектирование, 5 дней)

**Цель:** Прямое подключение всех сервисов к 1С:MDM

```
Дни 1-2: Документирование
├─ [ ] REST API методы Adata.kz
├─ [ ] REST API методы Dadata.ru
├─ [ ] REST API методы Kompra.kz
└─ [ ] API контакты CRM (Salesforce/Bitrix24/MS)

Дни 3-4: Конфигурация подключений
├─ [ ] Получить API ключи Adata.kz
├─ [ ] Получить API ключи Dadata.ru
├─ [ ] Получить API ключи Kompra.kz
└─ [ ] Получить API ключи CRM

День 5: Тестирование
├─ [ ] Тест Adata.kz (get_company_info)
├─ [ ] Тест Dadata.ru (clean_address)
├─ [ ] Тест Kompra.kz (get_references)
└─ [ ] Тест CRM (sync_contacts)
```

### Фаза 2: Программист разрабатывает логику нормализации (Разработка, 10 дней)

**Цель:** Вся критичная для скорости логика на стороне 1С:MDM

```
Дни 6-8: Нормализация данных
├─ [ ] Модуль нормализации названий контрагентов
│   └─ Удаление лишних символов, кавычек, пробелов
├─ [ ] Модуль валидации БИН/ИНН
│   └─ Проверка контрольной суммы (алгоритм)
├─ [ ] Модуль обработки адресов
│   └─ Структурирование данных от Dadata
└─ [ ] Модуль проверки качества данных
    └─ Обязательные поля, диапазоны, форматы

Дни 9-12: Дедупликация и конфликты
├─ [ ] SQL запросы для выявления дубликатов
│   └─ Поиск по похожести названий (Levenshtein)
├─ [ ] Алгоритм разрешения конфликтов
│   └─ Какие данные приоритетны?
├─ [ ] Маппинг полей (Adata → MDM, Dadata → MDM)
└─ [ ] Логирование всех операций
    └─ История изменений

Дни 13-16: Преобразование форматов
├─ [ ] JSON → 1С формат
├─ [ ] SQL экспорт → MDM справочник
├─ [ ] Интеграционные таблицы
└─ [ ] Хранимые процедуры для загрузки
```

### Фаза 3: ArliAI интеграция ДЛЯ СПЕЦИАЛИЗИРОВАННОЙ ЗАДАЧИ (5 дней)

**Цель:** ArliAI ТОЛЬКО для классификации ТН ВЭД товаров

```
Дни 17-19: Настройка ArliAI
├─ [ ] Документирование API арgsliAI для классификации
├─ [ ] Создать конфиг ArliAI:
│   └─ Доступ только к базе ТН ВЭД
│   └─ Отключить все остальные функции
└─ [ ] Установить rate limiting
    └─ Максимум X запросов в минуту

Дни 20-21: Интеграция с 1С:MDM
├─ [ ] REST API endpoint в 1С:MDM
│   └─ POST /api/v1/tnved/classify
├─ [ ] Кэширование результатов классификации
│   └─ Одно описание → один результат
└─ [ ] Обработка ошибок ArliAI
    └─ Fallback стратегия (ручная классификация)

Дни 22: Тестирование
├─ [ ] Unit тесты нормализации (80%+ покрытие)
├─ [ ] Integration тесты с Adata/Dadata/Kompra
├─ [ ] Тесты классификации ТН ВЭД через ArliAI
└─ [ ] Performance тесты (100+ записей/минуту)
```

---

## 🔧 ОТВЕТСТВЕННОСТЬ И РОЛИ

### Программист (основной обработчик) - 80% работы

```python
# pseudocode - ответственность программиста

def process_contractor(raw_data):
    """Основная логика обработки данных - ВСЕ в руках программиста"""
    
    # 1. Нормализация
    normalized_name = normalize_contractor_name(raw_data.name)
    # Например: "ООО   \"Компания  123\" " → "ООО Компания 123"
    
    # 2. Валидация
    is_valid_bin = validate_bin(raw_data.bin)
    if not is_valid_bin:
        raise ValueError("Invalid BIN")
    
    # 3. Дедупликация (SQL запрос)
    existing = db.query("""
        SELECT id FROM contractors 
        WHERE similarity(name, ?) > 0.85 
        AND similar_bin = ?
    """, normalized_name, raw_data.bin)
    
    if existing:
        # Обработка конфликта
        merged = merge_contractor_data(existing, raw_data)
        return merged
    
    # 4. Преобразование формата
    mdm_format = transform_to_mdm_format(raw_data)
    
    # 5. Загрузка в MDM
    mdm.load_contractor(mdm_format)
    
    return result  # БЫСТРО, без API вызовов!

def process_goods(raw_data):
    """Обработка товаров - почти вся логика в программисте"""
    
    # 1-4. Нормализация, валидация, преобразование (все выше)
    normalized = process_contractor(raw_data)  # похожая логика
    
    # 5. СПЕЦИАЛЬНОЕ: Классификация ТН ВЭД
    if not raw_data.tnved_code:  # Если нет кода ТН ВЭД
        # ТОЛЬКО ТОГДА вызываем ArliAI
        tnved_code = arli_ai.classify_tnved(
            description=raw_data.description,
            quantity_unit=raw_data.unit
        )
        # Это медленно, но редко происходит
        raw_data.tnved_code = tnved_code
    
    # 6. Загрузка в MDM
    mdm.load_goods(normalized)
    
    return result  # БЫСТРО для уже классифицированных!
```

### ArliAI (вспомогательный помощник) - 5-10% работы

```python
# ArliAI - ТОЛЬКО специализированная функция

class ArliAIClassifier:
    """Используется ТОЛЬКО для классификации ТН ВЭД"""
    
    @rate_limited(calls=100, period=60)  # Лимит: 100 в минуту
    def classify_tnved(self, description: str, unit: str) -> str:
        """
        Классификация товара по ТН ВЭД
        Используется: Редко (только для товаров БЕЗ кода)
        Задержка: ~1-2 сек на запрос (допустимо)
        """
        
        # Сначала поиск в кэше (быстро)
        cached = cache.get(f"tnved:{description}")
        if cached:
            return cached
        
        # Если не в кэше - спросить ИИ (медленно)
        result = self.ai_model.classify(description, unit)
        
        # Кэшировать результат
        cache.set(f"tnved:{description}", result, ttl=86400)
        
        return result
    
    # ВСЕ остальные функции ArliAI ОТКЛЮЧЕНЫ!
    # Нет нормализации, валидации, трансформации и т.д.
```

---

## 📊 СРАВНЕНИЕ: ДО vs ПОСЛЕ

### Скорость обработки

```
РАНЬШЕ (ArliAI как хаб):
Один контрагент (100 контрагентов/минуту):
1. Запрос к ArliAI (нормализация): 500ms
2. ArliAI обращается к Adata: 500ms
3. ArliAI трансформирует: 300ms
4. ArliAI валидирует: 200ms
5. Загрузка в MDM: 100ms
─────────────────────────────
ИТОГО: ~1.6 сек на контрагента = 37 контрагентов/минуту ❌

ПОСЛЕ (программист + ArliAI только для ТН ВЭД):
Один контрагент (прямое подключение):
1. Запрос к Adata напрямую: 200ms (параллельно!)
2. Нормализация программистом: 50ms (локально)
3. Валидация программистом: 50ms (локально)
4. Загрузка в MDM: 100ms
─────────────────────────────
ИТОГО: ~200ms на контрагента = 300+ контрагентов/минуту ✅

УСКОРЕНИЕ: В 8 РАЗ! ⚡

Товар БЕЗ ТН ВЭД (классификация через ArliAI):
1. Нормализация: 50ms
2. ArliAI классификация: 1500ms (редко!)
3. Загрузка в MDM: 100ms
─────────────────────────────
ИТОГО: ~1.65 сек (но только для 5% товаров без кода)
```

### Архитектурные различия

| Аспект | ArliAI как хаб (❌ ДО) | ArliAI только для ТН ВЭД (✅ ПОСЛЕ) |
|--------|-----|-----|
| **Скорость** | Медленно (узкое горлышко) | Быстро (параллельные запросы) |
| **Масштабируемость** | Ограничена (API лимиты ArliAI) | Легкая (прямые подключения) |
| **Ответственность** | ArliAI отвечает за всё | Программист отвечает за основное |
| **Конфигурация** | Сложная (много параметров) | Простая (только ТН ВЭД) |
| **Надежность** | Зависит от ArliAI | Зависит от самих API сервисов |
| **Стоимость** | Дорого (много ArliAI запросов) | Дешево (мало ArliAI запросов) |
| **Когда отказывает** | Всё падает | Только классификация не работает |

---

## 🎯 ДЕТАЛИЗИРОВАННАЯ РОЛЬ ArliAI

### ✅ ArliAI ИСПОЛЬЗУЕТСЯ ДЛЯ:

```
1. Классификация ТН ВЭД товаров
   └─ Когда: товар без кода ТН ВЭД
   └─ Как часто: 5-10% товаров
   └─ Задержка допустимо: ДА (несколько сек)
   └─ Критичность: НИЗКАЯ (можно заполнить вручную)

2. Уточнение описания товара
   └─ Когда: описание расплывчато
   └─ Как часто: редко (по запросу)
   └─ Задержка допустимо: ДА
   └─ Критичность: НИЗКАЯ

3. Проверка консистентности классификации
   └─ Когда: после заполнения ТН ВЭД программистом
   └─ Как часто: иногда (валидация)
   └─ Задержка допустимо: ДА (фоновая задача)
   └─ Критичность: НИЗКАЯ
```

### ❌ ArliAI НЕ ИСПОЛЬЗУЕТСЯ ДЛЯ:

```
1. Интеграция с Adata.kz
   └─ Причина: Высокий volume, критична скорость
   └─ Решение: Прямое подключение 1С:MDM

2. Интеграция с Dadata.ru
   └─ Причина: Адреса уже стандартизированы
   └─ Решение: Прямое подключение 1С:MDM

3. Нормализация названий контрагентов
   └─ Причина: Простая логика, программист справится
   └─ Решение: SQL + 1С правила

4. Валидация качества данных
   └─ Причина: Должна быть мгновенной
   └─ Решение: Локальная логика в 1С:MDM

5. Дедупликация объектов
   └─ Причина: Нужна скорость обработки
   └─ Решение: SQL запросы (Levenshtein distance)

6. Обработка конфликтов данных
   └─ Причина: Нужна мгновенная реакция
   └─ Решение: Бизнес-правила программиста

7. Трансформация форматов
   └─ Причина: Узкое горлышко проекта
   └─ Решение: Локальные скрипты
```

---

## ⚙️ КОНФИГУРАЦИЯ ArliAI

### МИНИМАЛЬНАЯ конфигурация (только для ТН ВЭД)

```yaml
# ~/.arliAI/config.yaml

# ✅ ВКЛЮЧЕНО - только ТН ВЭД классификация
classifiers:
  tnved:
    enabled: true
    model: "tnved_classifier_v2"
    cache_enabled: true
    cache_ttl: 86400  # 24 часа
    description_max_length: 500

# ❌ ОТКЛЮЧЕНО - всё остальное
integrations:
  adata:
    enabled: false  # 1С:MDM подключается напрямую
  dadata:
    enabled: false  # 1С:MDM подключается напрямую
  kompra:
    enabled: false  # 1С:MDM подключается напрямую
  crm:
    enabled: false  # 1С:MDM подключается напрямую

# Ограничение нагрузки
rate_limit:
  enabled: true
  max_requests_per_minute: 100
  max_concurrent: 5
  queue_size: 1000

# Логирование ТОЛЬКО классификации
logging:
  level: INFO
  only_classification: true
  
monitoring:
  enabled: true
  alert_on_error: true
```

### REST API ArliAI (ТОЛЬКО для классификации)

```
POST /api/v1/tnved/classify
├─ Request:
│  {
│    "description": "Ноутбук ASUS ROG с процессором Intel i7",
│    "quantity_unit": "шт",
│    "alternative_descriptions": ["Персональный компьютер портативный", "Ноут"]
│  }
│
└─ Response:
   {
     "tnved_code": "8471.30.10",
     "tnved_description": "Ноутбуки (персональные компьютеры портативные)",
     "confidence": 0.92,
     "alternatives": [
       {"code": "8471.30.20", "confidence": 0.05},
       {"code": "8471.30.30", "confidence": 0.03}
     ]
   }

GET /api/v1/tnved/cache/stats
└─ Статистика кэша классификации

DELETE /api/v1/tnved/cache/{description}
└─ Очистить кэш для описания
```

---

## 📋 ПЕРЕСМОТРЕННЫЙ ЧЕК-ЛИСТ ВНЕДРЕНИЯ

### Фаза 1: Прямые интеграции (Недели 1-2)

- [ ] Документирование API всех источников данных
- [ ] Получение API ключей (Adata, Dadata, Kompra, CRM)
- [ ] Конфигурация прямых подключений в 1С:MDM
- [ ] Тестирование каждого подключения
- [ ] Документирование структур данных
- [ ] Кэширование в MDM БД (чтобы не дергать API часто)

### Фаза 2: Программист разрабатывает обработку (Недели 3-4)

- [ ] Модуль нормализации названий
- [ ] Модуль валидации БИН/ИНН
- [ ] Алгоритм дедупликации (SQL + Levenshtein)
- [ ] Обработка конфликтов данных
- [ ] Маппинг полей (Adata → MDM, Dadata → MDM и т.д.)
- [ ] Логирование всех операций
- [ ] Unit тесты (80%+ покрытие)
- [ ] Integration тесты с реальными данными

### Фаза 3: ArliAI для ТН ВЭД (Неделя 5)

- [ ] Оценить текущее состояние базы ТН ВЭД в ArliAI
- [ ] Протестировать классификацию на примерах
- [ ] Настроить кэширование в ArliAI
- [ ] Установить rate limiting (100 запросов/минуту)
- [ ] Интегрировать с 1С:MDM (REST endpoint)
- [ ] Обработать ошибки и fallback сценарии
- [ ] Тесты классификации

### Фаза 4: Production (Неделя 6+)

- [ ] Performance тесты (300+ контрагентов/минуту)
- [ ] Security аудит прямых подключений
- [ ] UAT с бизнес-пользователями
- [ ] Постепенное включение интеграций
- [ ] Мониторинг и алертинг
- [ ] Документирование процессов

---

## 🚀 НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ (ИСПРАВЛЕННЫЕ)

### На этой неделе

1. **Встреча с техническим руководством**
   - ✅ Подтвердить: ArliAI ТОЛЬКО для ТН ВЭД
   - ✅ Выбрать CRM систему
   - ✅ Согласовать прямые подключения 1С:MDM

2. **Начать работу с прямыми интеграциями**
   - [ ] Получить доступ к Adata.kz API
   - [ ] Получить доступ к Dadata.ru API
   - [ ] Получить доступ к Kompra.kz API
   - [ ] Определить API требования CRM

3. **Подготовить ArliAI**
   - [ ] Отключить все функции кроме ТН ВЭД
   - [ ] Настроить rate limiting
   - [ ] Подготовить API для классификации

### На следующей неделе

4. **Начать разработку**
   - [ ] Программист: модули нормализации
   - [ ] Программист: модули валидации
   - [ ] Программист: алгоритм дедупликации
   - [ ] DevOps: прямые подключения к API
   - [ ] QA: интеграционные тесты

---

## 📊 КРИТИЧЕСКИЕ МЕТРИКИ (ПЕРЕРАБОТАННЫЕ)

### Успешность внедрения:

- **Throughput**: 300+ контрагентов/минуту (без ArliAI)
- **Latency**: <200ms на обработку контрагента
- **ТН ВЭД accuracy**: 90%+ правильная классификация
- **Data quality**: 98%+ валидных данных после обработки
- **Uptime**: 99.9% (из-за прямых подключений, надежнее)
- **ArliAI usage**: <10% всех операций (только классификация)

---

## 🔗 СВЯЗЬ С ТРЕБОВАНИЯМИ ПРОЕКТА

| Требование | Как решается (новый подход) |
|-----------|---------------------------|
| **Интеграция с внешними сервисами** (Лист №2-3) | Прямые подключения 1С:MDM |
| **Нормализация данных** (Лист №4) | Логика программиста в 1С:MDM |
| **Классификация ТН ВЭД** (не явно, но критично) | ArliAI ТОЛЬКО для этого |
| **API для CRM** (Лист №4) | Прямое подключение 1С:MDM |
| **Миграция баз** (Лист №4) | Программист через прямые интеграции |
| **Скорость обработки** | ✅ В 8 раз быстрее без ArliAI хаба |
| **Масштабируемость** | ✅ Легче масштабировать прямые подключения |

---

## 📝 ЗАКЛЮЧЕНИЕ

### Ключевые изменения:

1. ❌ **ArliAI НЕ является основным хабом**
2. ✅ **Прямые подключения 1С:MDM** к Adata, Dadata, Kompra, CRM
3. ✅ **Программист отвечает** за нормализацию, валидацию, дедупликацию
4. ✅ **ArliAI помощник ТОЛЬКО для классификации ТН ВЭД**
5. ✅ **Ускорение в 8 раз** за счет исключения лишних API вызовов
6. ✅ **Упрощение архитектуры** и повышение надежности

### Результат:

```
Скорость:              ⚡ 300+ контрагентов/мин
Надежность:           🛡️ 99.9% uptime
Функциональность:     ✅ Полная (с опциональной ТН ВЭД классификацией)
Масштабируемость:     📈 Легко масштабируется
Стоимость:            💰 Меньше API вызовов → дешевле
```

---

**Документ подготовлен:** 2025-11-20 (версия 2)  
**Статус:** ✅ ГОТОВ К ВНЕДРЕНИЮ (исправлено видение ArliAI)  
**Следующий шаг:** Одобрение архитектуры техническим руководством