# Функции сортировки и статуса баз данных

## ✅ Реализованная функциональность

### 1. Сортировка списка БД
- ✅ Сортировка по названию (asc/desc)
- ✅ Сортировка по пути (asc/desc)
- ✅ Визуальные индикаторы направления сортировки
- ✅ Кнопки сортировки в заголовках колонок
- ✅ Заголовки появляются при количестве БД > 1

### 2. Отображение статуса БД
- ✅ Бейдж статуса для каждой БД
- ✅ "Активна" для активных БД
- ✅ "Неактивна" для неактивных БД
- ✅ Разные варианты бейджей (default/secondary)
- ✅ Центрированное отображение статуса

### 3. Улучшенная структура данных
- ✅ Поле `is_active` в структуре данных БД
- ✅ Обновление всех мест загрузки данных
- ✅ Значение по умолчанию `true` для `is_active`

## Примеры работы

### Заголовки с сортировкой:
```
Название [↑↓]    Путь [↑↓]    Статус
```

### Список БД с сортировкой и статусом:
```
┌─────────────────────────────────────────┐
│ 🗄️ База данных 1    file1.db    [Активна] │
│ 🗄️ База данных 2    file2.db    [Активна] │
│ 🗄️ База данных 3    file3.db  [Неактивна] │
└─────────────────────────────────────────┘
```

## Технические детали

### Состояние сортировки:
- `databaseSortKey` - ключ сортировки ('name' | 'path' | null)
- `databaseSortDirection` - направление ('asc' | 'desc')

### Логика сортировки:
```typescript
.sort((a, b) => {
  if (!databaseSortKey) return 0
  
  let aValue: string
  let bValue: string
  
  if (databaseSortKey === 'name') {
    aValue = a.name.toLowerCase()
    bValue = b.name.toLowerCase()
  } else {
    aValue = (a.file_path || '').toLowerCase()
    bValue = (b.file_path || '').toLowerCase()
  }
  
  const comparison = aValue.localeCompare(bValue, 'ru-RU', { numeric: true })
  return databaseSortDirection === 'asc' ? comparison : -comparison
})
```

### Структура данных БД:
```typescript
{
  id: number
  name: string
  file_path: string
  is_active?: boolean  // Новое поле
}
```

## Готово к использованию

Функции полностью реализованы и готовы к использованию.

**Ошибок линтера:** 0  
**Статус:** ✅ Готово

