Исправь все ошибки компиляции в файле `internal/server/container.go`.

## Ошибки компилятора:

Ошибка 1:
  Строка: 16, Колонка: 2
  Сообщение: "httpserver/nomenclature" imported and not used

## Объяснение типов ошибок:

Ошибка "imported and not used" означает, что пакет импортирован, но не используется в коде файла.
Go компилятор требует, чтобы все импортированные пакеты использовались в коде.
Возможные решения:
- Удалить неиспользуемый импорт
- Использовать импортированный пакет в коде (если он действительно нужен)
- Если пакет нужен для side-effects (например, init функции), можно использовать пустой идентификатор: _ "package"

## Важные примечания:

- Некоторые зависимые файлы могли создаваться отдельно, т.е. Handler может зависеть от repository, но repository мог не существовать на момент написания handler
- Могут быть простые ошибки в наименовании файлов при импорте
- Но может быть действительно отсутствующий файл - убедись на 150%, что файл действительно отсутствует, прежде чем создавать его
- Проверь все импорты и убедись, что они указывают на существующие файлы с правильными именами
- Проверь после исправления компиляцию файла напрямую и, если есть ошибки - исправь их. Делай циклы "Проверка компиляции - исправление ошибки" пока не будет ошибок. Если после компиляции файла остались ошибки - сообщи об этом.

## Задача:

Проанализируй каждую ошибку, определи её причину и исправь. Не исправляй файл построчно - исправь все ошибки в файле за один раз. Убедись, что после исправления файл компилируется без ошибок.

