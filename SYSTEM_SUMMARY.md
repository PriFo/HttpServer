# Система автоматической генерации плана исправления ошибок компиляции

## 📦 Обзор

Полностью автоматизированная система для сборки Go проекта, анализа ошибок компиляции и генерации пошагового плана исправления с детальными промптами для каждого файла.

## 🎯 Основные возможности

✅ **Автоматическая сборка** - собирает проект и фиксирует все ошибки  
✅ **Умный парсинг** - автоматически определяет Python или использует PowerShell fallback  
✅ **Детальные промпты** - каждый промпт содержит объяснения типов ошибок  
✅ **Параллельное выполнение** - шаги можно выполнять параллельно  
✅ **Множественные способы запуска** - через скрипты или AI промпт  

## 📁 Структура файлов

### Основные скрипты

- **`build-and-fix.ps1`** - полностью автоматический скрипт (сборка + генерация плана)
- **`generate-build-fix-plan.ps1`** - генерация плана из существующего файла ошибок
- **`generate_build_fix_plan.py`** - Python скрипт парсинга ошибок
- **`parse-go-errors.ps1`** - PowerShell fallback парсер (если Python не найден)

### Промпты и документация

- **`BUILD_FIX_PROMPT_COPY.txt`** - универсальный промпт для AI (копировать в новый чат) ⭐ **РЕКОМЕНДУЕТСЯ**
- **`build_fix_prompt_short.txt`** - альтернативный промпт
- **`BUILD_FIX_PROMPT.md`** - подробная документация промпта
- **`BUILD_FIX_README.md`** - общая информация о системе
- **`BUILD_FIX_USAGE.md`** - полное руководство по использованию
- **`QUICK_START.md`** - быстрый старт

### Результаты работы

- **`build_errors_full.log`** - все ошибки компиляции
- **`build_fix_plan.md`** - полный план с промптами
- **`build_fix_prompts/`** - отдельные промпты для каждого шага

## 🚀 Способы использования

### 1. Полностью автоматический (рекомендуется)

```powershell
.\build-and-fix.ps1
```

### 2. Только генерация плана

```powershell
.\generate-build-fix-plan.ps1
```

### 3. Через AI промпт

Скопируйте `build_fix_prompt_short.txt` → вставьте в новый чат → AI выполнит все автоматически

## 🔧 Технические детали

### Автоматическое определение Python

Скрипты автоматически ищут Python в следующем порядке:
1. `python`
2. `python3`
3. `py`
4. `python.exe`
5. `python3.exe`

### Fallback механизм

Если Python не найден:
- Автоматически используется PowerShell парсер `parse-go-errors.ps1`
- Функциональность не теряется
- Результаты идентичны

### Формат ошибок

Система поддерживает стандартный формат ошибок Go:
```
# package_name
file.go:line:column: error message
```

### Типы ошибок

Система автоматически определяет и объясняет:
- `undefined` - неопределенный символ
- `imported and not used` - неиспользуемый импорт
- `too many arguments` - слишком много аргументов
- `too few arguments` - недостаточно аргументов
- `cannot use` - несовместимость типов
- `not declared` - необъявленная переменная

## 📊 Формат промптов

Каждый промпт содержит:

1. **Файл для исправления** - точный путь к файлу
2. **Ошибки компилятора** - все ошибки с номерами строк и колонок
3. **Объяснение типов ошибок** - детальные объяснения и тонкости
4. **Важные примечания** - о зависимых файлах, импортах, проверке
5. **Задача** - инструкции по исправлению

## ✅ Преимущества

- **Не требует готовых решений** - промпты содержат только объяснения, AI сам думает над решением
- **Параллельное выполнение** - каждый шаг независим
- **Детальные объяснения** - понимание причин ошибок
- **Автоматизация** - минимум ручной работы
- **Гибкость** - работает с Python или без него

## 📚 Документация

- **QUICK_START.md** - начните отсюда
- **BUILD_FIX_USAGE.md** - полное руководство
- **BUILD_FIX_README.md** - общая информация
- **BUILD_FIX_PROMPT.md** - документация промпта

## 🎓 Примеры использования

См. `BUILD_FIX_USAGE.md` для подробных примеров.

