# План улучшений реализации методов обнаружения дублей

## Дата создания: 2025-01-20

---

## 📊 Текущее состояние

**Общая оценка**: 85% методов реализовано

### ✅ Сильные стороны
- Все базовые алгоритмы нечеткого поиска работают
- Полный набор метрик оценки качества
- Хорошая интеграция с API
- Кэширование и оптимизация производительности

### ❌ Пробелы
- Нет моделей глубокого обучения (Seq2Seq, BERT, BiLSTM)
- Нет классификаторов ML (Random Forest, Gradient Boosting)
- Частичная лемматизация (только стемминг)
- Ограниченный NER

---

## 🎯 План улучшений

### Этап 1: Улучшение существующих алгоритмов (Приоритет: Высокий)

#### 1.1 Полная лемматизация
**Цель**: Заменить стемминг на полноценную лемматизацию

**Шаги**:
1. Интегрировать библиотеку для морфологического анализа русского языка
   - Вариант 1: Использовать Go-обертку для pymorphy2
   - Вариант 2: Интегрировать через Python API
   - Вариант 3: Использовать готовую Go библиотеку (если есть)

2. Создать `Lemmatizer` интерфейс и реализацию
   ```go
   // normalization/algorithms/lemmatizer.go
   type Lemmatizer interface {
       Lemmatize(word string) string
       LemmatizeTokens(tokens []string) []string
   }
   ```

3. Обновить `NameNormalizer` для использования лемматизации
4. Добавить тесты

**Оценка времени**: 2-3 дня
**Файлы для изменения**:
- `normalization/algorithms/lemmatizer.go` (новый)
- `normalization/name_normalizer.go`
- `normalization/duplicate_analyzer.go`

---

#### 1.2 Улучшенный NER (Named Entity Recognition)
**Цель**: Полноценное распознавание именованных сущностей

**Шаги**:
1. Создать структуру для NER с BIO-тегированием
   ```go
   // normalization/algorithms/ner.go
   type NEREntity struct {
       Type     string // PER, ORG, LOC, MATERIAL, SIZE, COLOR, etc.
       Text     string
       Start    int
       End      int
       Confidence float64
   }
   ```

2. Реализовать rule-based NER для основных типов:
   - Материалы (сталь, дерево, пластик)
   - Размеры (20x30, 100мм)
   - Цвета (белый, черный)
   - Типы (многожильный, одножильный)

3. Интегрировать с `ExtractAttributes()`
4. Добавить ML-based NER (опционально, через внешний API)

**Оценка времени**: 3-4 дня
**Файлы для изменения**:
- `normalization/algorithms/ner.go` (новый)
- `normalization/name_normalizer.go`

---

### Этап 2: Интеграция моделей глубокого обучения (Приоритет: Средний)

#### 2.1 Seq2Seq модель для нормализации
**Цель**: Реализовать архитектуру Encoder-Decoder с Attention

**Подход**: Использовать внешний сервис или предобученную модель

**Шаги**:
1. Создать интерфейс для Seq2Seq нормализации
   ```go
   // normalization/seq2seq_normalizer.go
   type Seq2SeqNormalizer interface {
       Normalize(input string) (string, float64, error)
       BatchNormalize(inputs []string) ([]string, []float64, error)
   }
   ```

2. Варианты реализации:
   - **Вариант A**: Интеграция с Python сервисом (Flask/FastAPI)
     - Создать Python сервис с обученной Seq2Seq моделью
     - Использовать gRPC или REST API для связи
   
   - **Вариант B**: Использовать готовый MLaaS (MLflow, Seldon)
   
   - **Вариант C**: Использовать предобученные модели через ONNX Runtime
     - Конвертировать PyTorch/TensorFlow модель в ONNX
     - Использовать Go binding для ONNX Runtime

3. Интегрировать в `NSINormalizer` как опциональный метод
4. Добавить fallback на существующие методы

**Оценка времени**: 1-2 недели (в зависимости от варианта)
**Файлы для создания**:
- `normalization/seq2seq_normalizer.go`
- `normalization/seq2seq_client.go` (если через API)
- `python_services/seq2seq_service/` (если вариант A)

**Рекомендация**: Начать с варианта A (Python сервис) как наиболее гибкого

---

#### 2.2 BERT для семантического понимания
**Цель**: Использовать BERT для извлечения семантических признаков

**Шаги**:
1. Создать интерфейс для BERT эмбеддингов
   ```go
   // normalization/bert_embedder.go
   type BERTEmbedder interface {
       GetEmbedding(text string) ([]float64, error)
       GetEmbeddings(texts []string) ([][]float64, error)
       Similarity(text1, text2 string) (float64, error)
   }
   ```

2. Варианты реализации:
   - **Вариант A**: Использовать sentence-transformers через Python API
     - Модель: `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
   
   - **Вариант B**: Использовать готовые эмбеддинги через API (OpenAI, Cohere)
   
   - **Вариант C**: Локальный BERT через ONNX Runtime

3. Интегрировать в `UniversalMatcher` как новый метод `bert_similarity`
4. Использовать для улучшения semantic duplicates

**Оценка времени**: 1 неделя
**Файлы для создания**:
- `normalization/bert_embedder.go`
- `python_services/bert_service/` (если вариант A)

---

#### 2.3 BiLSTM для обработки последовательностей
**Цель**: Двунаправленная обработка для лучшего понимания контекста

**Примечание**: BiLSTM обычно используется внутри Seq2Seq или BERT, поэтому отдельная реализация может быть избыточной. Рекомендуется интегрировать через Seq2Seq модель.

**Оценка времени**: Включено в этап 2.1

---

### Этап 3: Классификаторы машинного обучения (Приоритет: Средний)

#### 3.1 Random Forest для категоризации
**Цель**: Автоматическая категоризация номенклатуры

**Шаги**:
1. Создать интерфейс классификатора
   ```go
   // normalization/ml_classifier.go
   type MLClassifier interface {
       Classify(text string) (string, float64, error)
       ClassifyBatch(texts []string) ([]string, []float64, error)
   }
   ```

2. Реализация через Python сервис:
   - Обучить Random Forest на размеченных данных
   - Создать REST API для классификации
   - Использовать scikit-learn или XGBoost

3. Интегрировать в `Categorizer`
4. Добавить fallback на keyword-based классификацию

**Оценка времени**: 1 неделя
**Файлы для создания**:
- `normalization/ml_classifier.go`
- `python_services/ml_classifier_service/`

---

#### 3.2 Gradient Boosting для определения дублей
**Цель**: Использовать ML для улучшения точности поиска дублей

**Шаги**:
1. Создать модель, которая принимает признаки:
   - Расстояние Левенштейна
   - Jaccard индекс
   - N-граммы схожесть
   - Фонетическая схожесть
   - Совпадение кодов
   - Длина строк

2. Обучить на размеченных данных (дубли/не дубли)
3. Интегрировать в `DuplicateAnalyzer` как дополнительный метод
4. Использовать для финальной фильтрации результатов

**Оценка времени**: 1-2 недели
**Файлы для создания**:
- `normalization/ml_duplicate_detector.go`
- `python_services/ml_duplicate_service/`

---

### Этап 4: Оптимизация производительности (Приоритет: Низкий)

#### 4.1 Префиксная фильтрация
**Цель**: Ускорить поиск дублей для больших справочников

**Шаги**:
1. Реализовать префиксный индекс
   ```go
   // normalization/prefix_index.go
   type PrefixIndex struct {
       prefixes map[string][]int // prefix -> item IDs
   }
   ```

2. Использовать для предварительной фильтрации перед точным сравнением
3. Интегрировать во все алгоритмы поиска дублей

**Оценка времени**: 2-3 дня

---

#### 4.2 Индексация для быстрого поиска
**Цель**: Создать индексы для ускорения поиска

**Шаги**:
1. Создать индексы:
   - Индекс по нормализованным именам
   - Индекс по фонетическим кодам
   - Индекс по N-граммам

2. Использовать для быстрого поиска кандидатов
3. Интегрировать в базу данных

**Оценка времени**: 3-4 дня

---

## 📋 Приоритизация задач

### Критичные (сделать в первую очередь)
1. ✅ Полная лемматизация (2-3 дня)
2. ✅ Улучшенный NER (3-4 дня)
3. ✅ Префиксная фильтрация (2-3 дня)

**Итого**: ~1.5 недели

### Важные (следующий спринт)
4. ✅ BERT для семантики (1 неделя)
5. ✅ Random Forest для категоризации (1 неделя)

**Итого**: ~2 недели

### Желательные (когда будет время)
6. ✅ Seq2Seq нормализация (1-2 недели)
7. ✅ Gradient Boosting для дублей (1-2 недели)
8. ✅ Индексация (3-4 дня)

**Итого**: ~3-4 недели

---

## 🔧 Технические детали реализации

### Архитектура интеграции ML моделей

```
┌─────────────────┐
│   Go Backend    │
│  (HttpServer)   │
└────────┬─────────┘
         │
         │ gRPC/REST
         │
┌────────▼─────────┐
│  Python Service  │
│  (ML Models)     │
│  - Seq2Seq       │
│  - BERT          │
│  - Random Forest │
│  - XGBoost       │
└──────────────────┘
```

### Структура Python сервиса

```
python_services/
├── seq2seq_service/
│   ├── app.py
│   ├── model.py
│   ├── requirements.txt
│   └── models/
│       └── seq2seq_model.pth
├── bert_service/
│   ├── app.py
│   ├── embedder.py
│   └── requirements.txt
└── ml_classifier_service/
    ├── app.py
    ├── classifier.py
    └── models/
        └── rf_model.pkl
```

### Пример интеграции

```go
// normalization/ml_integration.go
type MLServiceClient struct {
    baseURL string
    client  *http.Client
}

func (c *MLServiceClient) NormalizeSeq2Seq(text string) (string, error) {
    req := map[string]string{"text": text}
    resp, err := c.client.Post(c.baseURL+"/normalize", "application/json", ...)
    // ...
}
```

---

## 📈 Метрики успеха

### Для лемматизации
- ✅ Улучшение точности поиска дублей на 5-10%
- ✅ Уменьшение ложных срабатываний на 3-5%

### Для NER
- ✅ Извлечение 90%+ атрибутов из наименований
- ✅ Улучшение качества нормализации на 10-15%

### Для BERT
- ✅ Улучшение semantic similarity на 15-20%
- ✅ Лучшее обнаружение синонимов

### Для ML классификаторов
- ✅ Точность категоризации > 95%
- ✅ Улучшение F1-меры поиска дублей на 10-15%

---

## 🧪 Тестирование

### План тестирования

1. **Unit тесты** для каждого нового компонента
2. **Integration тесты** для проверки взаимодействия
3. **Benchmark тесты** для проверки производительности
4. **E2E тесты** на реальных данных

### Тестовые данные

Создать тестовый набор:
- 1000+ пар дубликатов
- 1000+ уникальных записей
- Различные типы опечаток
- Различные форматы наименований

---

## 📚 Документация

Для каждого нового компонента создать:
1. README с описанием
2. Примеры использования
3. API документацию
4. Описание алгоритма

---

## 🚀 Быстрый старт (MVP)

Если нужно быстро улучшить систему, начать с:

1. **Лемматизация** (2-3 дня) - быстрое улучшение
2. **Улучшенный NER** (3-4 дня) - заметный эффект
3. **BERT интеграция** (1 неделя) - значительное улучшение

**Итого MVP**: ~2 недели работы

---

## 📝 Заключение

Текущая реализация уже покрывает 85% требований и работает хорошо. Предложенные улучшения позволят:

1. Довести покрытие до 95-100%
2. Улучшить точность на 10-20%
3. Снизить количество ложных срабатываний
4. Подготовить систему к масштабированию

Рекомендуется начать с MVP (лемматизация + NER + BERT), а затем постепенно добавлять остальные компоненты.

