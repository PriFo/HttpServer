# 📖 Полное руководство по системе TODO

## 🎯 Введение

Автоматизированная система управления TODO задачами - это комплексное решение для отслеживания, управления и приоритизации задач, найденных в коде.

## 🏗️ Архитектура

### Компоненты системы

```
┌─────────────────────────────────────────┐
│         Smart Scanner                   │
│  (scan-todos.sh)                        │
│  - Сканирование кода                   │
│  - Классификация приоритетов           │
│  - Определение типов                   │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│         Task Manager                    │
│  (tasks.json)                           │
│  - Хранение задач                      │
│  - Метаданные                          │
│  - История                              │
└──────────────┬──────────────────────────┘
               │
       ┌───────┴───────┐
       │               │
       ▼               ▼
┌──────────────┐  ┌──────────────┐
│ Auto-Assign  │  │  Dashboard   │
│ (Python)     │  │  (HTML/JS)   │
└──────────────┘  └──────────────┘
```

## 📋 Формат задач

### Структура задачи

```json
{
  "id": "unique-hash",
  "file": "path/to/file.go",
  "line": 42,
  "type": "TODO",
  "priority": "HIGH",
  "status": "OPEN",
  "description": "Task description",
  "assignedTo": "backend-team",
  "fileType": "backend",
  "createdAt": "2025-01-20T10:00:00Z",
  "updatedAt": "2025-01-20T10:00:00Z",
  "estimatedHours": 2,
  "dependencies": [],
  "relatedFiles": []
}
```

### Типы задач

- **TODO** - Обычные задачи
- **FIXME** - Исправления
- **HACK** - Временные решения
- **REFACTOR** - Рефакторинг

### Приоритеты

- **CRITICAL** - Блокирующие проблемы
- **HIGH** - Важные задачи
- **MEDIUM** - Обычные задачи
- **LOW** - Косметические улучшения

### Статусы

- **OPEN** - Открыта
- **IN_PROGRESS** - В работе
- **RESOLVED** - Решена
- **TESTING** - На тестировании

## 🔍 Поиск и фильтрация

### jq запросы

**Все критические задачи:**
```bash
jq '.tasks[] | select(.priority == "CRITICAL")' .todos/tasks.json
```

**Задачи конкретного файла:**
```bash
jq '.tasks[] | select(.file | contains("server.go"))' .todos/tasks.json
```

**Не назначенные задачи:**
```bash
jq '.tasks[] | select(.assignedTo == "unassigned")' .todos/tasks.json
```

**Задачи по типу:**
```bash
jq '.tasks[] | select(.type == "FIXME")' .todos/tasks.json
```

## 🛠️ Работа с задачами

### Обновление статуса

```bash
# Вручную через jq
jq '.tasks[] | select(.id == "task-id") | .status = "RESOLVED"' .todos/tasks.json
```

### Добавление зависимости

```bash
jq '.tasks[] | select(.id == "task-id") | .dependencies += ["other-task-id"]' .todos/tasks.json
```

### Изменение приоритета

```bash
jq '.tasks[] | select(.id == "task-id") | .priority = "CRITICAL"' .todos/tasks.json
```

## 📊 Аналитика

### Статистика по командам

```bash
jq '.tasks | group_by(.assignedTo) | map({team: .[0].assignedTo, count: length})' .todos/tasks.json
```

### Распределение по файлам

```bash
jq '.tasks | group_by(.file) | map({file: .[0].file, count: length}) | sort_by(.count) | reverse' .todos/tasks.json
```

### Время решения задач

```bash
jq '.tasks[] | select(.status == "RESOLVED") | {id, createdAt, updatedAt, duration: ((.updatedAt | fromdateiso8601) - (.createdAt | fromdateiso8601))}' .todos/tasks.json
```

## 🔄 Workflow

### Ежедневный workflow

1. **Утро:**
   ```bash
   npm run todos:scan      # Обновить список задач
   npm run todos:stats     # Посмотреть статистику
   ```

2. **В течение дня:**
   - Работа над задачами
   - Обновление статусов
   - Отслеживание прогресса

3. **Вечер:**
   ```bash
   npm run todos:report    # Сгенерировать отчет
   npm run todos:workload  # Обновить загруженность
   ```

### Еженедельный workflow

1. **Понедельник:**
   ```bash
   npm run todos:all       # Полное сканирование
   ```

2. **Пятница:**
   ```bash
   npm run todos:cleanup 30  # Очистить старые задачи
   ```

## 🎨 Кастомизация

### Добавление нового типа файла

В `scan-todos.sh` добавьте:
```bash
case "$ext" in
    your-ext)
        echo "your-type"
        ;;
esac
```

### Кастомные приоритеты

В `config.json`:
```json
{
  "priorities": {
    "CUSTOM": {
      "color": "PURPLE",
      "label": "Кастомный",
      "blockCommit": false
    }
  }
}
```

### Кастомные команды

В `team.json`:
```json
{
  "team": {
    "custom-team": ["dev1", "dev2"]
  }
}
```

## 🚨 Troubleshooting

### Проблема: jq не найден

**Решение:**
```bash
# Linux
sudo apt install jq

# Mac
brew install jq

# Windows
choco install jq
```

### Проблема: Пустые результаты

**Проверьте:**
1. Есть ли TODO в коде
2. Не исключены ли файлы
3. Правильные ли пути

### Проблема: Ошибки JSON

**Решение:**
```bash
npm run todos:validate
```

### Проблема: Медленное сканирование

**Оптимизация:**
- Исключите большие директории в скрипте
- Используйте параллельное сканирование
- Кэшируйте результаты

## 📈 Best Practices

1. **Регулярное сканирование** - минимум раз в день
2. **Актуальные статусы** - обновляйте статусы задач
3. **Приоритизация** - решайте критические задачи первыми
4. **Документация** - добавляйте описания к задачам
5. **Очистка** - удаляйте старые решенные задачи

## 🔐 Безопасность

- Не коммитьте чувствительные данные в описаниях задач
- Используйте `.todos/.gitignore` для временных файлов
- Регулярно делайте бэкапы `tasks.json`

## 🎓 Примеры использования

### Пример 1: Найти все FIXME в backend

```bash
jq '.tasks[] | select(.type == "FIXME" and .fileType == "backend")' .todos/tasks.json
```

### Пример 2: Статистика по разработчику

```bash
jq '[.tasks[] | select(.assignedTo == "backend-dev-1")] | length' .todos/tasks.json
```

### Пример 3: Экспорт критических задач

```bash
jq '[.tasks[] | select(.priority == "CRITICAL")]' .todos/tasks.json > critical.json
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи
2. Запустите `test-system.sh`
3. Проверьте документацию
4. Создайте issue в репозитории

---

*Система постоянно развивается. Следите за обновлениями в CHANGELOG.md*

