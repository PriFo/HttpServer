# 🚀 Быстрый старт - TODO System

## Минимальная настройка (5 минут)

### 1. Проверка системы

```bash
bash .todos/scripts/test-system.sh
```

### 2. Первое сканирование

Из корня проекта:
```bash
bash .todos/scripts/scan-todos.sh .
```

Или из frontend:
```bash
cd frontend
npm run todos:scan
```

### 3. Просмотр результатов

**Веб-дашборд:**
```bash
# Откройте в браузере
open .todos/dashboard.html
# или
start .todos/dashboard.html  # Windows
```

**Текстовый отчет:**
```bash
npm run todos:report
cat TODO_REPORT.md
```

### 4. Автоматизация (опционально)

**Полный цикл:**
```bash
npm run todos:all
```

Это выполнит:
- Сканирование кода
- Автоназначение задач
- Обновление загруженности
- Генерацию отчета

## 📊 Что дальше?

1. **Настройте команду** - отредактируйте `.todos/team.json`
2. **Настройте приоритеты** - отредактируйте `.todos/config.json`
3. **Автоматизируйте** - добавьте в cron/Task Scheduler
4. **Интегрируйте** - используйте git hooks (уже настроены)

## 🔧 Решение проблем

**Ошибка: jq не найден**
```bash
# Linux
sudo apt install jq

# Mac
brew install jq

# Windows (через Chocolatey)
choco install jq
```

**Ошибка: bash не найден (Windows)**
- Установите Git Bash или WSL
- Или используйте PowerShell версии скриптов

**Пустые результаты**
- Убедитесь, что в коде есть TODO/FIXME/HACK комментарии
- Проверьте, что файлы не в исключенных директориях

## 📚 Дополнительная документация

- Полная документация: `.todos/README.md`
- Настройка: `SETUP_TODO_SYSTEM.md`
- Changelog: `.todos/CHANGELOG.md`

