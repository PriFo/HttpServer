# 🎯 Возможности системы TODO

## 📋 Основные функции

### 1. Автоматическое сканирование
- ✅ Рекурсивное сканирование всего кодовой базы
- ✅ Поддержка множества языков (Go, TypeScript, JavaScript, Python, Shell)
- ✅ Интеллектуальное определение приоритетов
- ✅ Автоматическая категоризация по типам файлов

### 2. Управление задачами
- ✅ Создание задач из комментариев в коде
- ✅ Отслеживание статусов (OPEN, IN_PROGRESS, RESOLVED)
- ✅ Приоритизация (CRITICAL, HIGH, MEDIUM, LOW)
- ✅ Автоматическое назначение командам

### 3. Визуализация
- ✅ Веб-дашборд с графиками
- ✅ Автоматические отчеты в Markdown
- ✅ Статистика в реальном времени
- ✅ Экспорт в различные форматы

### 4. Автоматизация
- ✅ Git hooks для проверки критических задач
- ✅ Автоматическое назначение задач
- ✅ Отслеживание загруженности команды
- ✅ Очистка старых решенных задач

## 🛠️ Утилиты

### Сканирование
```bash
npm run todos:scan      # Сканирование кода
```

### Отчеты
```bash
npm run todos:report    # Генерация Markdown отчета
npm run todos:stats     # Быстрая статистика
```

### Управление
```bash
npm run todos:assign    # Автоназначение задач
npm run todos:workload  # Обновление загруженности
npm run todos:validate  # Валидация структуры данных
```

### Экспорт и очистка
```bash
npm run todos:export    # Экспорт в CSV/JSON/Markdown
npm run todos:cleanup   # Удаление старых решенных задач
```

### Полный цикл
```bash
npm run todos:all       # Все операции за раз
```

## 📊 Форматы экспорта

### CSV
```bash
node .todos/scripts/export-tasks.js csv
```

### Markdown
```bash
node .todos/scripts/export-tasks.js md
```

### JSON
```bash
node .todos/scripts/export-tasks.js json
```

## 🔍 Поиск и фильтрация

### По приоритету
```bash
jq '.tasks[] | select(.priority == "CRITICAL")' .todos/tasks.json
```

### По статусу
```bash
jq '.tasks[] | select(.status == "OPEN")' .todos/tasks.json
```

### По типу файла
```bash
jq '.tasks[] | select(.fileType == "backend")' .todos/tasks.json
```

### По назначенному
```bash
jq '.tasks[] | select(.assignedTo == "backend-team")' .todos/tasks.json
```

## 📈 Метрики

Система отслеживает:
- Общее количество задач
- Распределение по приоритетам
- Распределение по типам
- Распределение по статусам
- Распределение по командам
- Загруженность разработчиков
- История сканирований

## 🎨 Кастомизация

### Приоритеты
Настройте в `.todos/config.json`:
```json
{
  "priorities": {
    "CRITICAL": {
      "color": "RED",
      "label": "Блокирующие",
      "blockCommit": true
    }
  }
}
```

### Команды
Настройте в `.todos/team.json`:
```json
{
  "team": {
    "backend-team": ["dev1", "dev2"]
  }
}
```

## 🔄 Интеграции

### Git Hooks
Автоматическая проверка критических задач перед коммитом.

### CI/CD
Можно интегрировать в pipeline для автоматической проверки.

### IDE
Интеграция с Cursor через `.cursorrules`.

## 📱 Дашборд

Веб-интерфейс доступен по адресу:
```
.todos/dashboard.html
```

Функции:
- Интерактивные графики
- Фильтрация задач
- Статистика в реальном времени
- Автообновление каждые 5 минут

## 🚀 Расширяемость

Система легко расширяется:
- Добавление новых типов файлов
- Кастомные приоритеты
- Интеграция с внешними системами
- Кастомные экспорты

