# Новые функции UI для контрагентов

## ✅ Реализованные функции

### 1. Обогащение контрагентов
**Описание:** Ручное обогащение данных контрагента через внешние сервисы (Dadata.ru, Adata.kz)

**Реализация:**
- Кнопка обогащения (иконка Sparkles) в таблице контрагентов
- Доступна только для контрагентов с ИНН или БИН
- Показывает индикатор загрузки во время обогащения
- Автоматически обновляет данные контрагента после успешного обогащения

**Файлы:**
- `frontend/app/clients/[clientId]/projects/[projectId]/counterparties/page.tsx`
- `frontend/app/api/counterparties/normalized/enrich/route.ts`

### 2. Управление дубликатами
**Описание:** Просмотр и объединение дубликатов контрагентов

**Реализация:**
- Кнопка "Дубликаты" в фильтрах (доступна только при выбранном проекте)
- Диалог с группами дубликатов
- Выбор мастер-записи для каждой группы
- Объединение дубликатов в одну запись

**Файлы:**
- `frontend/app/clients/[clientId]/projects/[projectId]/counterparties/page.tsx`
- `frontend/app/api/counterparties/normalized/duplicates/route.ts`
- `frontend/app/api/counterparties/normalized/duplicates/[groupId]/merge/route.ts`

### 3. Экспорт контрагентов
**Описание:** Экспорт контрагентов проекта в различных форматах

**Реализация:**
- Кнопки экспорта в фильтрах (JSON, CSV)
- Доступны только при выбранном проекте
- Автоматическое скачивание файла с датой в имени

**Файлы:**
- `frontend/app/clients/[clientId]/projects/[projectId]/counterparties/page.tsx`
- `frontend/app/api/counterparties/normalized/export/route.ts`

## 🎨 UI/UX улучшения

### Анимации
- Плавные анимации при наведении на кнопки
- Индикаторы загрузки для всех асинхронных операций
- Анимации строк таблицы при загрузке

### Обратная связь
- Сообщения об ошибках в Alert компонентах
- Индикаторы состояния операций
- Визуальная обратная связь при успешных операциях

## 📊 Статистика

- **Добавлено новых функций:** 3
- **Создано API routes:** 4
- **Улучшено UI компонентов:** 1 (страница контрагентов)
- **Добавлено обработчиков:** 4

## 🔄 Интеграция с бэкендом

Все новые функции полностью интегрированы с бэкендом:
- ✅ Обогащение через `/api/counterparties/normalized/enrich`
- ✅ Получение дубликатов через `/api/counterparties/normalized/duplicates`
- ✅ Объединение дубликатов через `/api/counterparties/normalized/duplicates/{groupId}/merge`
- ✅ Экспорт через `/api/counterparties/normalized/export`

## 🚀 Готово к использованию

Все функции протестированы и готовы к использованию. Пользователи могут:
1. Обогащать данные контрагентов одним кликом
2. Просматривать и управлять дубликатами
3. Экспортировать данные в различных форматах

