# Система автоматической генерации плана исправления ошибок компиляции

Эта система автоматически собирает Go проект, анализирует ошибки компиляции и создает пошаговый план исправления с отдельными промптами для каждого файла.

## Быстрый старт

> 📌 **Самый простой способ:** Откройте `QUICK_START.md` для краткой инструкции

### Вариант 1: Использование готового промпта (рекомендуется)

1. Откройте файл `BUILD_FIX_PROMPT_COPY.txt`
2. Скопируйте весь текст (Ctrl+A, Ctrl+C)
3. Вставьте в новый чат с AI (Ctrl+V)
4. AI автоматически выполнит все шаги

### Вариант 2: Использование PowerShell скрипта (рекомендуется)

```powershell
# Автоматическая сборка и генерация плана
.\build-and-fix.ps1
```

### Вариант 3: Ручной запуск

```powershell
# 1. Соберите проект и сохраните ошибки
go build ./... 2>&1 | Tee-Object -FilePath build_errors_full.log

# 2. Запустите скрипт генерации плана (автоматически выберет Python или PowerShell)
.\generate-build-fix-plan.ps1

# Или напрямую PowerShell парсер
.\parse-go-errors.ps1

# Или Python скрипт (если доступен)
python generate_build_fix_plan.py

# 3. Откройте созданный план
code build_fix_plan.md
```

## Что создается

После выполнения промпта или скрипта будут созданы:

- **`build_errors_full.log`** - полный лог всех ошибок компиляции
- **`build_fix_plan.md`** - план исправления с промптами для каждого файла
- **`build_fix_prompts/step_XX_*.txt`** - отдельные промпты для каждого шага (удобно для параллельной работы)

## Структура плана

План содержит:
- Общую статистику (количество файлов с ошибками, общее количество ошибок)
- Отдельные шаги для каждого файла с ошибками
- Детальные промпты с:
  - Путем к файлу
  - Списком всех ошибок компилятора
  - Объяснением типов ошибок (без решения)
  - Важными примечаниями о зависимостях
  - Инструкциями по проверке после исправления

## Особенности

### Параллельное выполнение
Шаги можно выполнять параллельно, так как каждый шаг исправляет отдельный файл.

### Полное исправление файла
Каждый промпт требует исправления всех ошибок в файле за один раз, а не построчно.

### Автоматическая проверка
После исправления файла необходимо проверить его компиляцию напрямую:
```powershell
go build ./path/to/file.go
# или
go build ./package/path
```

### Умная обработка зависимостей
Система учитывает, что:
- Некоторые файлы могли создаваться отдельно (handler может зависеть от repository, который еще не был создан)
- Могут быть простые ошибки в наименовании файлов при импорте
- Файл может действительно отсутствовать (нужно убедиться на 150%)

## Формат ошибок

Скрипт поддерживает стандартный формат ошибок Go компилятора:
```
# package_name
file.go:line:column: error message
    additional context
```

## Поддерживаемые типы ошибок

Скрипт автоматически распознает и объясняет:
- `undefined` - неопределенный символ
- `imported and not used` - неиспользуемый импорт
- `too many arguments` - слишком много аргументов
- `too few arguments` - слишком мало аргументов
- `cannot use` - несовместимость типов
- `not declared` - необъявленная переменная
- `too many errors` - слишком много ошибок в файле (компилятор остановился)
- И другие общие ошибки компиляции

## Пример использования

1. **Скопируйте промпт** из `BUILD_FIX_PROMPT_COPY.txt`
2. **Вставьте в новый чат** - AI начнет работу автоматически
3. **Дождитесь завершения** - будет создан план и промпты
4. **Используйте промпты** из `build_fix_prompts/` для исправления ошибок (можно параллельно)

## Если проект собирается без ошибок

Если при сборке не было ошибок, система сообщит об этом и остановится. Никаких файлов создано не будет.

## Файлы системы

### Основные скрипты:
- `build-and-fix.ps1` - **главный скрипт** - автоматически собирает проект и генерирует план
- `generate-build-fix-plan.ps1` - обертка, выбирает Python или PowerShell парсер
- `parse-go-errors.ps1` - PowerShell парсер ошибок (работает без Python)
- `generate_build_fix_plan.py` - Python скрипт генерации плана

### Документация:
- `BUILD_FIX_PROMPT_COPY.txt` - промпт для копирования в новый чат
- `BUILD_FIX_PROMPT.md` - полная документация с промптом
- `BUILD_FIX_README.md` - этот файл с инструкциями
- `QUICK_START.md` - краткая инструкция
- `BUILD_FIX_SYSTEM_FILES.md` - описание всех файлов системы
