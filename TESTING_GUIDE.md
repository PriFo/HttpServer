# Руководство по тестированию улучшений бенчмарка моделей

## Дата: 2025-01-21

## 🧪 Тестирование получение всех моделей

### Тест 1: Проверка получения всех моделей из API

```bash
# Запуск бенчмарка через API
curl -X POST http://localhost:9999/api/models/benchmark \
  -H "Content-Type: application/json" \
  -d '{}'
```

**Ожидаемый результат:**
- В логах должно быть сообщение о получении более 2 моделей
- Ответ API должен содержать поле `statistics.models_available` с количеством всех доступных моделей
- Поле `statistics.models_tested` должно показывать количество протестированных моделей

### Тест 2: Проверка fallback механизмов

```bash
# Проверка внутреннего API
curl "http://localhost:9999/api/workers/models?enabled=all&status=all"
```

**Ожидаемый результат:**
- Должны возвращаться все модели, не только первые 2
- В логах должно быть сообщение о количестве полученных моделей

### Тест 3: Проверка обработки ошибок

```bash
# Тест с неверным API ключом
ARLIAI_API_KEY=invalid_key curl -X POST http://localhost:9999/api/models/benchmark \
  -H "Content-Type: application/json" \
  -d '{}'
```

**Ожидаемый результат:**
- Должно быть информативное сообщение об ошибке API ключа
- В логах должна быть классификация ошибки как `auth`

## 🧪 Тестирование обработки ошибок OpenRouter

### Тест 4: Проверка обработки quota exceeded

**Сценарий:**
1. Использовать OpenRouter с исчерпанным quota
2. Запустить запрос через систему

**Ожидаемый результат:**
- Ошибка должна быть классифицирована как `quota_exceeded`
- В логах должно быть сообщение "Provider quota exceeded - will not retry"
- Система должна fallback на другие провайдеры

### Тест 5: Проверка обработки rate limit (429)

**Сценарий:**
1. Превысить rate limit OpenRouter
2. Запустить запрос

**Ожидаемый результат:**
- Ошибка должна быть классифицирована как `rate_limit`
- Должен быть retry с экспоненциальной задержкой
- В логах должно быть сообщение о retry попытках

## 🧪 Тестирование фронтенда

### Тест 6: Проверка отображения статистики

**Сценарий:**
1. Открыть страницу `/models/benchmark`
2. Запустить бенчмарк
3. Проверить отображение статистики

**Ожидаемый результат:**
- Должна отображаться карточка "Статистика бенчмарка"
- Должны показываться: успешные модели, неудачные модели, доступно моделей, процент успеха
- Если моделей ≤ 2, должно быть предупреждение

### Тест 7: Проверка сообщений об ошибках

**Сценарий:**
1. Запустить бенчмарк без API ключа
2. Проверить сообщение об ошибке

**Ожидаемый результат:**
- Должно быть информативное сообщение на русском языке
- Должно быть указание, где настроить API ключ

## 📋 Чек-лист проверки

### Backend
- [ ] Все модели получаются из API (проверить логи)
- [ ] Fallback механизмы работают
- [ ] Ошибки классифицируются правильно
- [ ] Логирование детальное
- [ ] Статистика в ответе API

### Frontend
- [ ] Статистика отображается в UI
- [ ] Предупреждения показываются правильно
- [ ] Сообщения об ошибках информативные
- [ ] Toast уведомления работают

### Интеграция
- [ ] API возвращает все необходимые поля
- [ ] Фронтенд правильно обрабатывает ответ
- [ ] Ошибки обрабатываются gracefully

## 🔍 Проверка логов

### Ключевые сообщения для проверки:

```
[Benchmark] Cleared Arliai cache to get fresh models
[Benchmark] Attempting to get models with status=all
[Benchmark] Successfully got X models with status=all
[Benchmark] Statistics: X successful models, Y failed models
[OpenRouter] Quota exceeded
[SystemScanner] Panic при обработке БД
```

## 🐛 Отладка проблем

### Проблема: Получено только 2 модели

**Проверка:**
1. Проверить логи сервера - какие query параметры использовались
2. Проверить ответ Arliai API напрямую
3. Проверить переменную окружения `ARLIAI_API_KEY`
4. Проверить конфигурацию воркеров

### Проблема: Ошибки не классифицируются

**Проверка:**
1. Проверить логи - должны быть сообщения с `error_type`
2. Проверить, что ошибки содержат ключевые слова (quota, rate limit, etc.)
3. Проверить обработку ошибок в соответствующих клиентах

### Проблема: Статистика не отображается на фронтенде

**Проверка:**
1. Проверить ответ API - должно быть поле `statistics`
2. Проверить консоль браузера - должны быть логи `[Benchmark] Statistics:`
3. Проверить состояние компонента - `benchmarkStatistics` должен быть установлен
