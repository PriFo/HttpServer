# Анализ использования алгоритмов поиска дублей в API

## Дата анализа: 2025-01-20

---

## 📍 Эндпоинты API

### 1. `/api/quality/duplicates` (GET)

**Описание**: Получение списка групп дубликатов номенклатуры

**Реализация**: `server/server_quality.go:307`

**Используемые алгоритмы**:
- Данные берутся из БД (уже обработанные)
- Используется `db.GetDuplicateGroups()` для получения сохраненных групп

**Параметры**:
- `database` - путь к БД
- `unmerged` - только необъединенные дубли
- `limit`, `offset` - пагинация

**Статус**: ✅ Работает, но использует предобработанные данные

---

### 2. `/api/quality/duplicates/{groupId}` (POST/PUT/DELETE)

**Описание**: Действия с группами дубликатов (объединение, удаление)

**Реализация**: `server/server_quality.go:480`

**Функционал**:
- `POST /merge` - объединение группы
- `PUT` - обновление группы
- `DELETE` - удаление группы

**Статус**: ✅ Реализовано

---

### 3. `/api/counterparties/duplicates` (GET)

**Описание**: Получение дублей контрагентов

**Реализация**: `server/server.go:11927`

**Используемые алгоритмы**:
```go
duplicateAnalyzer := normalization.NewCounterpartyDuplicateAnalyzer()
groups := duplicateAnalyzer.AnalyzeDuplicates(allCounterparties)
```

**Особенности**:
- Использует специализированный анализатор для контрагентов
- Анализирует по ИНН/КПП, БИН
- Работает с несколькими БД проекта

**Статус**: ✅ Полностью реализовано

---

### 4. `/api/counterparties/duplicates/{groupId}/merge` (POST)

**Описание**: Объединение дублей контрагентов

**Реализация**: `server/server.go:12109`

**Функционал**:
- Выбор мастер-записи
- Объединение данных из дубликатов
- Удаление дубликатов из БД

**Статус**: ✅ Реализовано

---

## 🔍 Детальный анализ использования

### CounterpartyDuplicateAnalyzer

**Файл**: `normalization/counterparty_duplicate_analyzer.go`

**Методы**:
- `AnalyzeDuplicates()` - основной метод анализа
- `GetDuplicateSummary()` - статистика по дублям

**Используемые алгоритмы**:
- Exact matching по ИНН/КПП
- Exact matching по БИН
- Fuzzy matching по наименованию (вероятно)

**Статус**: ✅ Специализированная реализация для контрагентов

---

### NSINormalizer.FindDuplicates()

**Файл**: `normalization/nsi_normalizer.go:61`

**Используется в**:
- Прямом вызове (если есть)
- Через `DuplicateAnalyzer` в других местах

**Алгоритмы**:
1. Exact matching (если включен)
2. Fuzzy matching с комбинированными алгоритмами:
   - Levenshtein
   - Damerau-Levenshtein
   - Bigram
   - Trigram
   - Jaccard
   - Soundex
   - Metaphone

**Статус**: ✅ Полностью реализовано, но не всегда используется напрямую в API

---

## ⚠️ Проблемы и рекомендации

### Проблема 1: Разделение логики

**Текущая ситуация**:
- Для номенклатуры: данные предобрабатываются и сохраняются в БД
- Для контрагентов: анализ выполняется на лету

**Рекомендация**:
- Унифицировать подход
- Добавить кэширование результатов анализа
- Использовать единый интерфейс `NSINormalizer` везде

---

### Проблема 2: Нет прямого использования NSINormalizer в API

**Текущая ситуация**:
- `/api/quality/duplicates` использует предобработанные данные
- Нет эндпоинта для анализа на лету

**Рекомендация**:
Добавить эндпоинт:
```
POST /api/quality/duplicates/analyze
Body: {
  "items": [...],
  "config": {
    "use_exact_matching": true,
    "use_fuzzy_matching": true,
    "threshold": 0.85,
    ...
  }
}
```

---

### Проблема 3: Нет сравнения алгоритмов через API

**Рекомендация**:
Добавить эндпоинт:
```
POST /api/quality/duplicates/compare-algorithms
Body: {
  "items": [...],
  "actual_pairs": [...],
  "threshold": 0.85
}
Response: {
  "levenshtein": { "precision": 0.95, "recall": 0.90, ... },
  "jaccard": { ... },
  ...
}
```

---

## ✅ Что работает хорошо

1. **Специализированные анализаторы**: 
   - `CounterpartyDuplicateAnalyzer` хорошо подходит для контрагентов
   - Разделение логики по типам данных

2. **Интеграция с БД**:
   - Сохранение результатов анализа
   - Пагинация и фильтрация

3. **Объединение дублей**:
   - Работает для контрагентов
   - Выбор мастер-записи

---

## 🚀 Предложения по улучшению API

### 1. Универсальный эндпоинт анализа

```go
// server/server_quality.go
func (s *Server) handleAnalyzeDuplicates(w http.ResponseWriter, r *http.Request) {
    var req struct {
        Items  []DuplicateItem            `json:"items"`
        Config DuplicateDetectionConfig   `json:"config"`
    }
    
    nsi := normalization.NewNSINormalizer()
    groups := nsi.FindDuplicates(req.Items, req.Config)
    
    s.writeJSONResponse(w, map[string]interface{}{
        "groups": groups,
        "total": len(groups),
    }, http.StatusOK)
}
```

### 2. Эндпоинт сравнения алгоритмов

```go
func (s *Server) handleCompareAlgorithms(w http.ResponseWriter, r *http.Request) {
    var req struct {
        Items       []DuplicateItem  `json:"items"`
        ActualPairs []Pair           `json:"actual_pairs"`
        Threshold   float64          `json:"threshold"`
    }
    
    nsi := normalization.NewNSINormalizer()
    comparison := nsi.CompareAlgorithms(req.Items, req.ActualPairs, req.Threshold)
    
    s.writeJSONResponse(w, comparison, http.StatusOK)
}
```

### 3. Эндпоинт метрик качества

```go
func (s *Server) handleQualityMetrics(w http.ResponseWriter, r *http.Request) {
    var req struct {
        Predicted []DuplicateGroup `json:"predicted"`
        Actual    []DuplicateGroup `json:"actual"`
    }
    
    nsi := normalization.NewNSINormalizer()
    metrics := nsi.EvaluateAlgorithm(req.Predicted, req.Actual)
    
    s.writeJSONResponse(w, metrics, http.StatusOK)
}
```

---

## 📊 Статистика использования

### Текущие вызовы в коде:

1. **CounterpartyDuplicateAnalyzer**: 4 места
   - `server.go:8371` - статистика проекта
   - `server.go:12012` - получение дублей
   - `server.go:12191` - объединение дублей
   - `server.go:12741` - получение нормализованных дублей

2. **NSINormalizer**: 0 прямых вызовов в API
   - Используется только внутри других компонентов

3. **DuplicateAnalyzer**: Используется в:
   - `normalizer.go:634` - фильтрация дублей из батча
   - Внутри `NSINormalizer`

---

## 🎯 Итоговые рекомендации

### Критичные
1. ✅ Добавить прямой эндпоинт для анализа дублей на лету
2. ✅ Унифицировать использование `NSINormalizer` в API
3. ✅ Добавить кэширование результатов анализа

### Желательные
4. ✅ Добавить эндпоинт сравнения алгоритмов
5. ✅ Добавить эндпоинт метрик качества
6. ✅ Добавить возможность настройки алгоритмов через API

---

## 📝 Заключение

Алгоритмы поиска дублей хорошо реализованы, но их использование в API частично. Рекомендуется:

1. Добавить прямые эндпоинты для использования всех возможностей
2. Унифицировать подход к анализу для разных типов данных
3. Добавить возможность настройки алгоритмов через API

Это позволит:
- Лучше использовать существующие алгоритмы
- Упростить интеграцию с фронтендом
- Дать больше контроля пользователям

