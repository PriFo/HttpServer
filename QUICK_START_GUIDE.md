# Быстрый старт: Использование всех моделей в бенчмарке

## 🚀 Быстрая проверка

### 1. Проверка получения всех моделей

```bash
# Запуск бенчмарка
curl -X POST http://localhost:9999/api/models/benchmark \
  -H "Content-Type: application/json" \
  -d '{}'
```

**Проверьте в ответе:**
- `statistics.models_available` - должно быть больше 2
- `statistics.models_tested` - количество протестированных моделей
- `message` - информативное сообщение

### 2. Проверка логов

Ищите в логах сервера:
```
[Benchmark] Successfully got X models with status=all
[Benchmark] Statistics: X successful models, Y failed models
```

Если видите "Only 2 models available" - проверьте:
1. API ключ Arliai
2. Настройки воркеров
3. Ответ Arliai API напрямую

### 3. Проверка фронтенда

1. Откройте `/models/benchmark`
2. Запустите бенчмарк
3. Проверьте карточку "Статистика бенчмарка"
4. Убедитесь, что показывается правильное количество моделей

## ⚠️ Важные моменты

### MaxWorkers=2
Это ограничение на **параллельные запросы**, НЕ на количество моделей!

### Получение всех моделей
Система автоматически пробует:
1. `status=all`
2. `include=all`
3. `all=true`
4. Без параметров
5. Внутренний API
6. Конфигурация
7. Известные модели

### Обработка ошибок
- Quota exceeded - не повторяется
- Rate limit - повторяется с задержкой
- Timeout - повторяется
- Network - повторяется

## 🔍 Отладка

### Проблема: Только 2 модели

**Решение:**
1. Проверьте логи: `[Benchmark] Got X models`
2. Проверьте API ключ
3. Проверьте настройки воркеров
4. Используйте `/api/workers/models?enabled=all&status=all`

### Проблема: Ошибки quota/rate limit

**Решение:**
1. Проверьте quota в настройках провайдера
2. Увеличьте задержки между запросами
3. Проверьте логи для деталей

---

**Готово!** Система должна работать с всеми доступными моделями.
